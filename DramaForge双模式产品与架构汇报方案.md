# DramaForge 双模式产品与架构汇报方案

**汇报用途**：产品立项 / 架构评审 / 研发启动会  
**版本**：v1.2（与 P0 冻结包及执行路线同步）  
**日期**：2026-07-20  
**决策状态**：HISTORICAL / 双模式本身已纳入 P0，但本文的团队定位与旧排期不再代表当前路线。当前定位以 `DramaForge总开发文档.md` 为准；P0 实现以根目录 `01`–`06` 为受控冻结包。

---

## 使用方式

本文前半部分是 **15–20 分钟汇报主稿**，每一节可直接作为一页 PPT。

后半部分是 **产品与技术答疑附录**，用于回答 Agent、数据一致性、成本、安全、交付和排期问题。

---

# A. 汇报主稿

## 第 1 页｜结论先行：DramaForge 要建设什么

### 一句话结论

DramaForge 是面向个人创作者的 AI 短剧生产工作台。它以 **Production Graph** 管理从创意、分镜、生成、审核到交付的全过程，并以“双模式”服务同一创作者的快速出片与精细控制需求。

### 本次希望确认

1. 将“**一个生产底座、两种工作入口**”批准为一级产品需求。
2. 保持以 Production Graph、增量重跑和一致性引擎为核心的技术路线。
3. 先通过 BOOT-0 仓库启动 Gate，再按 S0–S5 验证风险、打穿主链并完成正式交付。

---

## 第 2 页｜为什么需要 DramaForge

AI 已能生成图片、视频、配音和字幕，但短剧生产仍有四个断点：角色不一致、修改成本高、协作过程失控、成片难以交付。

| 当前痛点 | 业务影响 | DramaForge 的回应 |
|---|---|---|
| 多模型、多工具割裂 | 素材散落，流程靠人工拼接 | 用统一项目与 Production Graph 串联 |
| 修改牵一发动全身 | 反复重生成，成本不可控 | 用输入指纹只重跑受影响节点 |
| 角色与剧情容易漂移 | 成片质量不稳定 | 用参考集、连续性规则和质检闭环控制 |
| 用户能力差异大 | 产品要么复杂，要么能力不足 | 以双模式覆盖“快”和“可控” |

> **汇报话术**：我们不把 AI 当作单点生成工具，而是把它变成一套可管理、可审核、可复用的影视生产系统。

---

## 第 3 页｜一级产品需求：双模式，而不是两套产品

快速出片模式与专业工作台模式必须建立在同一项目、同一资产库、同一任务图和同一成本账上。

```text
               同一项目生产底座
Project / Asset / Script / Story Bible / Production Graph
        Cost / Review / Artifact / Delivery / Audit
                 ↙                 ↘
        快速出片模式             专业工作台模式
```

| 模式 | 面向用户 | 价值主张 | 主要交互 |
|---|---|---|---|
| **快速出片模式** | 普通创作者、运营人员 | 少输入、快得到可审核成片 | 对话式 Agent + 向导卡片 + 阶段确认 |
| **专业工作台模式** | 编导、制片、后期、协作团队 | 精细控制、协同生产、专业交付 | 项目工作区 + 编辑器 + 任务队列 |

### 核心原则

快速模式不等于低配版；专业模式也不是独立后台。两者只是同一生产事实的不同视图。

---

## 第 4 页｜快速出片模式：从文本到成片的受控 Agent 流程

```text
输入创意 / 粘贴剧本 / 选择模板
        ↓
Agent 形成结构化创作简报
        ↓  用户确认
剧情、时长、风格、角色与预算方案
        ↓  用户确认
分镜计划与首帧方案
        ↓
生成、质检、修复、合成
        ↓
审核成片 / 下载 / 进入专业工作台精修
```

### 必备体验

- Chat 是入口，**确认卡片**才是关键决策界面。
- 在付费生成、批量执行和自动修复前，必须展示范围与预算。
- 每一步都能看到进度、成本、产物和失败解释。
- 用户可随时进入专业工作台，继续编辑同一个项目。

> **汇报话术**：快速模式的目标是把复杂能力藏在系统里，而不是把不可控性藏在系统里。

---

## 第 5 页｜专业工作台模式：让团队把作品做细、做稳、交得出

| 工作区 | 团队获得的控制能力 |
|---|---|
| 项目概览 | 进度、预算、风险、待审核项、交付状态 |
| 剧本与知识 | Story Bible、人物关系、剧情事件、连续性约束 |
| 资产库 | 角色、场景、道具、声音、参考图、锁定版本 |
| 分镜与生产图 | Shot 编辑、节点依赖、局部重跑、缓存复用 |
| 任务与质检 | 队列、失败重试、取消、相似度、漂移、人工审核 |
| 成本与模型 | BYOK、Provider、预算、实际费用与降级记录 |
| 交付中心 | MP4、SRT、素材包、时间线 JSON 及后续专业格式 |

专业模式中的 Agent 是辅助入口。它可以帮助拆分剧本、生成分镜和定位问题，但所有结果都必须变成可审阅的项目变更。

---

## 第 6 页｜核心能力：Production Graph 让修改只影响必要部分

每个镜头拆成独立节点：`首帧 → 视频 → 配音 → 字幕 → 合成 → 审核 → 导出`。

每个节点都有 `input_hash`。系统根据输入是否真实改变，判断缓存命中和局部重跑范围。

| 用户修改 | 系统只重跑 |
|---|---|
| 改字幕 | 字幕 → 合成 → 导出 |
| 换视频 | 视频 → 合成 → 导出 |
| 更换角色参考图 | 受影响的首帧、视频与后续质检 |
| 修改镜头或风格 | 受影响节点及其下游节点 |

### 业务价值

更快的修改反馈、更低的生成成本、更清晰的过程审计，以及可恢复的失败处理。

---

## 第 7 页｜总体架构：五层协同，一套生产真相

```text
用户界面层
快速出片模式 / 专业工作台模式 / REST + SSE
                    ↓
项目、资产与一致性层
Project / Script / Shot / Asset / Style Bible / Consistency
                    ↓
Agent 编排与任务层
Creation Experience Module / Production Graph / 状态机
                    ↓
生成执行层
模型适配器 / Arq Worker / FFmpeg / ComfyUI
                    ↓
交付与生态层
MP4 / SRT / 素材包 / 时间线 JSON / 后续专业格式
```

这是面向产品评审的概念视图，不是代码目录或服务边界。上述概念必须映射到 `03_全局目录规范.md` 已定义的 `access`、`creation`、`assets`、`script`、`production`、`runtime`、`consistency` 与 `delivery` 模块；P0 不另建 Knowledge Graph、事件图谱、`project`、`model_router` 或 `render_pipeline` 平行模块。

### 三条架构原则

1. 用户界面可以不同，项目和执行真相必须唯一。
2. Agent 只能通过稳定 Interface 规划或请求执行，不能绕过图和状态机。
3. 数据库是事实来源；实时事件用于提醒和恢复，不替代事实。

---

## 第 8 页｜关键技术决策：为可控生产服务

| 决策 | 选择 | 解决的问题 |
|---|---|---|
| 后端异步栈 | Python + FastAPI + SQLAlchemy async + asyncpg | 统一 AI 调用、数据库与 Web 的异步模型 |
| 任务编排 | Arq 双队列 | 分离 I/O 调用与 FFmpeg、视觉推理等重计算任务 |
| 数据与存储 | PostgreSQL + Redis + MinIO | 强一致数据、事件流与 S3 兼容媒体资产 |
| 实时同步 | Transactional Outbox + Redis Streams + SSE | 防止落库与通知不一致、断线丢事件 |
| 前端 | React + TypeScript + TanStack + Zustand | 支持双模式共享的可靠状态管理 |
| 一致性检测 | InsightFace + 规则引擎 + 视觉质检 | 把“角色像不像、剧情对不对”变成可量化流程 |

### 核心取舍

我们不以模型数量作为护城河，而以“可控生产运行时”作为护城河。

---

## 第 9 页｜可靠性、安全与成本：从 P0 开始内建

### 可靠性

- 图定义、每次执行、供应商任务和产物分为四层，避免状态双重真相。
- 取消、重试、缓存、并发单飞与断点恢复由 Production Graph 统一处理。
- Outbox 保障业务状态与事件投递一致；SSE 断线后可回放并全量刷新。

### 安全与隔离

- 组织、成员、项目三层权限；数据库 RLS 与 Worker 同样受项目范围约束。
- BYOK 密钥加密保存，日志脱敏；Provider 回调只认系统已保存的远端任务关联。
- 上传素材先隔离，检查通过才进入正式资产路径。

### 成本控制

- 每个节点记录预估和实际费用。
- 自动重生成有次数与预算上限，超过阈值进入人工审核。
- 缓存命中复用已有产物，不重复创建供应商任务或重复记账。

---

## 第 10 页｜交付范围：先承诺确定性，再验证高风险格式

### P0 确定交付

```text
MP4 + SRT + 素材包 + 时间线 JSON
```

### 条件性能力

剪映草稿作为候选 P0 功能，必须先完成独立兼容性 Spike。若目标版本、媒体轨道与字幕导入无法稳定复现，则降为 P0.5，不阻塞主闭环。

### 后续演进

DaVinci XML、EDL、AAF 按 P1/P2 推进。PR 工程不纳入当前承诺。

---

## 第 11 页｜实施路径：先证伪，再打穿，再加厚

| 阶段 | 周期 | 关键成果 | 关键 Gate |
|---|---:|---|---|
| BOOT-0 | 按 Gate | 可运行仓库骨架、本地基础设施与质量门禁 | Compose 配置、API/Worker/前端壳、静态检查、测试、构建和目录检查通过 |
| S0-A | 3 天 | 视觉一致性 Spike | 同/异角色与异常样本的 FAR、FRR、耗时、成本可量化 |
| S0-B | 2 天 | 剪映兼容性 Spike | 干净环境导入最小草稿稳定可复现；否则降级 P0.5 |
| S1 | 2 周 | 可信基础骨架 + 双模式应用壳 | RLS、Outbox、Graph 模型、Creation Experience Interface、统一项目路由通过 |
| S2 | 2 周 | 快速模式首帧垂直切片 | 文本 → 确认 → 首帧 → 相似度/成本 → 工作台无损切换；不得视为 P0 完成 |
| S3 | 2.5 周 | Production Runtime 加厚 | 缓存、取消、重试、预算、恢复的幂等测试通过 |
| S4 | 3 周 | 专业工作台与完整生产闭环 | 10 Shot 完成 `shot-p0-v1` 的图像、视频、漂移检查、配音、字幕、合成、连续性检查、审核与局部返工 |
| S5 | 2.5 周 | 交付与集成硬化 | 10 Shot 流程无阻断，部署、恢复、安全回归和正式交付均可复现 |

S0–S5 总周期约 **12–13 周**，BOOT-0 以环境和质量 Gate 通过为准。每一阶段都通过可观测、可复现的 Gate 决定是否继续扩大投入；阶段切片只控制当期实施面，不缩小 P0 最终范围。

缺少真实 Provider Key 时可使用 mock，S4 内部试产也可使用经批准的手工媒体，但手工媒体必须登记为既有 DAG 节点的一次受审计结果，不能跳过节点、审核或下游传播。P0 的角色与剧情一致性检查不能关闭，只能配置阈值、规则等级、自动修复次数和转人工策略。

---

## 第 12 页｜本次决策与下一步行动

### 建议本次批准

1. 将“双模式创作体验”列为 DramaForge 的一级产品需求。
2. 保持“五层架构 + Production Graph 为核心”的技术方向。
3. 先通过 BOOT-0 仓库启动 Gate，再将 S0-A、S0-B 及关键 ADR 作为真实能力接入和 P0 验收的硬 Gate。
4. S2 先完成快速模式首帧切片；S4 补齐固定 Production Graph，S5 完成交付硬化后才能宣布 P0 MVP 完成。

### 立即行动

- 执行 `agent.md` 中的 `Task BOOT-0.1`，先建立可运行仓库、本地基础设施和质量门禁。
- BOOT-0 通过后，启动 S0-A 视觉一致性 Spike 与 S0-B 剪映兼容性 Spike。
- 建立 DAG/缓存、Outbox、隔离/SSE、预算、Provider 回调和产物生命周期 ADR。
- 数据库迁移、API 和界面按 S1–S5 的用户行为竖切扩展；某张表一旦进入阶段，必须逐字段镜像 `04_数据定义全集.md`，不得用临时简化表替代。
- S1 即建立基础 Outbox/队列指标和死信处置 Runbook，S3、S5 再分别补齐运行时与交付运维能力。

---

# B. 产品与技术答疑附录

## B1. 双模式的实施定义

快速模式可以采用 Chat 入口，但 Agent 的输出必须沉淀为正式对象：创作简报、项目计划、角色、场景、分镜、生产图、任务与产物。

专业模式不是后台管理页，而是对同一项目的全量、可编辑、可审计视图。

| 转换场景 | 必须保持不变 |
|---|---|
| 快速模式进入工作台 | 项目 ID、资产、分镜、GraphVersion、NodeRun、成本、产物、审核记录 |
| 专业模式回到快速模式 | 最新项目快照、人工锁定项、预算与待办状态 |
| 团队成员在不同模式协作 | RLS、项目角色、对象访问范围、审计事件与成本归属 |

模式只是用户偏好和当前路由，不是项目类型，也不影响权限模型。

## B2. 创作体验 Module：双模式的收敛点

创作体验 Module 负责把自然语言意图转成结构化计划，并负责 Agent、确认卡片、模式转换、上下文摘要和错误恢复。

```text
start_project(intent, template_id?) -> ProjectSnapshot
refine_brief(project_id, instruction) -> PlanPreview
confirm_plan(project_id, plan_id) -> ProjectSnapshot
request_generation(project_id, scope, budget_confirmed) -> RunSummary
open_workbench(project_id) -> ProjectSnapshot
resume_quick_flow(project_id) -> GuidedStep
```

这个 Module 不可直接改写 GraphNode 或 NodeRun，也不可绕过预算、审核、RLS、BYOK 或 Provider Adapter。

## B3. Production Graph 的四层模型

```text
GraphVersion：图定义，说明应做什么
      ↓
GraphNode：节点定义与期望输入
      ↓
NodeRun：本次执行尝试、状态、重试、成本、取消信息
      ↓
ProviderOperation：供应商侧异步任务、远端 ID、实际模型与计费
      ↓
Artifact：不可变的媒体或中间产物
```

Graph 通过显式边、命名端口和不可变版本管理多媒体依赖。任何模板升级或人工改图都产生新版本，已执行版本不原地修改。

## B4. 关键运行时合同

| 主题 | 设计合同 |
|---|---|
| 缓存 | `input_hash` 在当前项目、图版本和节点作用域内判断有效输入；命中缓存时创建 `cached` Run，但不创建 ProviderOperation |
| 事件 | 业务状态、审计事件和 Outbox 同事务提交；异步投递可重试、可去重、可进入死信 |
| SSE | 前端从 Streams 补事件；断线恢复后以 Graph 快照全量校准 |
| 取消 | 取消请求与完成竞态有明确终态；取消后的结果不得继续触发下游或污染缓存 |
| 预算 | 提交前预占预算，执行后按实际费用结算；超额则阻断或进入审核 |
| 产物 | 上传先隔离，检查后变为可用；引用、冷存储、回温、删除均有审计和哈希校验 |

## B5. 双模式验收清单

| 验收层级 | 关键验证 |
|---|---|
| 双模式体验 | 同一项目无副本切换；人工锁定不被 Agent 覆盖；两个入口显示同一成本与状态 |
| 图与缓存 | DAG 无环、端口完整、局部重跑正确、并发同 Hash 单飞、缓存不重复计费 |
| 可靠性 | 幂等提交、取消/完成竞态、断点恢复、Outbox 重放、SSE 断线恢复、死信清理 |
| 隔离 | RLS 越权拒绝、连接池上下文清理、Worker 跨项目资源拒绝、对象 URL 最小范围 |
| Provider | 创建、轮询、取消、限流、错误归一化、Webhook 签名、重复与乱序回调 |
| 交付 | MP4、SRT、时间线 JSON golden fixture；可选剪映草稿兼容性回归 |

## B6. 主要风险与控制策略

| 风险 | 控制策略 |
|---|---|
| 双模式演化成两套逻辑 | 把模式限定为同一项目的不同视图；共用领域对象和 Production Graph |
| Agent 输出不稳定 | Creative Brief 和计划确认卡片；关键生成与预算必须确认 |
| 一致性阈值不可靠 | S0-A 用同/异角色和异常样本量化 FAR、FRR、耗时与成本 |
| 私有导出格式不稳定 | S0-B 独立 Spike；不通过即降为 P0.5 |
| 回调重复或乱序 | ProviderOperation 关联、条件状态迁移、幂等账本与 Inbox/Outbox |
| 多租户资源泄漏 | RLS、事务上下文、对象路径隔离、限时预签名 URL |

---

## 文档关系

- **本文件**：面向立项、评审和启动会的双模式产品与架构汇报方案。
- **《DramaForge架构决策与技术选型书.md》**：架构决策理由、关键取舍与阶段路线；与冻结包不一致时，以冻结包为准。
- **《01_项目总需求.md》至《06_受控混合Agent运行时规范.md》**：按需求、技术、目录、数据、模块和 Agent 运行时主题组成 P0 唯一实现冻结包。
- **《DramaForge总开发文档.md》**：项目唯一总纲，定义当前目标用户、阶段状态和产品路线。
- **后续 ADR**：冻结 DAG/缓存、Outbox、隔离/SSE、预算、Provider 回调、产物生命周期等不可临场决定的细节。
