# DramaForge 模块落地约束（P0）

**状态：FROZEN / 所有实现、重构和 AI 生成代码必须遵守**  
**同步基线：2026-08-01（个人私有 Workspace 已对齐）**

> 本文是 P0 通用模块与生产执行规则的事实源；受控混合 Agent 的专门状态、授权、调度、写回和物化事实以 `06_受控混合Agent运行时规范.md` 为唯一来源。快速出片模式与专业工作台模式必须通过同一领域 Module、同一 Project 真相、同一权限和同一运行时合同工作；不得把快速模式实现为绕开 Graph、预算或审核的旁路。
>
> 本文定义模块能力、调用方向、胶水代码、异常、缓存、一致性、执行与恢复规则。`04_数据定义全集.md` 定义数据真相；本文定义这些数据如何被安全地改变。任何“简化实现”若破坏本文件的状态、审计、缓存、预算或一致性语义，均视为缺陷而非技术债。

## 0. 双模式创作体验合同

1. `creation` 是快速出片模式唯一领域入口，公开稳定 Interface：`estimate_planning`、`start_project`、`request_brief_draft`、`refine_brief`、`confirm_brief`、`request_plan_draft`、`confirm_plan`、`cancel_agent_run`、`request_generation`、`get_creation_snapshot`、`open_workbench`、`resume_quick_flow`。Route/UI/Agent 只能依赖该 Interface。
2. `start_project` 只创建正式 Project、模式偏好、Brief 聚合与可手工编辑的初始 revision、EventLog/Outbox；项目预算上限不等于文本调用授权，禁止自动调用 Provider。
3. 自然语言输入必须沉淀为不可变 `CreativeBriefRevision`、`CreationPlan` 与正式 Project 对象；对话、前端 Store、LLM 会话记忆均不是真相。
4. `confirm_plan` 是单一 Unit of Work 的事务拥有者：锁定 Project/BriefRevision/Plan，完整校验 MaterializationPlan，调用共享 UoW 的领域 Command（只 flush 不 commit），最后写确认、EventLog、Outbox；任一步失败整体回滚。
5. 专业模式打开同一 ProjectSnapshot；切回快速模式只读取最新快照和待办摘要。不得复制 Project、重复 NodeRun、覆盖人工锁定/审核，或因模式改变 RLS、BYOK、预算和审批。
6. 每次文本规划、真实生成、批量生成和自动修复均需展示范围与最坏可接受费用并取得对应的一次性授权；授权不得跨 AgentRun/操作复用。
## 1. 统一调用链与胶水代码规则

### 1.1 唯一调用方向

```text
React Route / Component
  → TanStack Query hook
  → REST / SSE
  → FastAPI Route
  → Request 依赖：认证、RLS、Pydantic 校验
  → 模块 Service
  → Repository / Domain Engine / Executor
  → PostgreSQL + MinIO + Arq + Provider Adapter
  → Transactional Outbox
  → Redis Streams / SSE
  → 前端失效 Query 并重取快照
```

- Route 只做输入校验、权限依赖、调用一个 Service 和映射输出；不得含 SQL、缓存或 Provider 分支。
- Service 是业务事务边界。需要变更多张业务表时，必须在一个 PostgreSQL 事务内写业务状态、`event_log` 与 `outbox_events`。
- Repository 只封装所属模块的查询和数据库约束；不得发布事件、调用 HTTP、执行 FFmpeg 或决定 UI 文案。
- Executor 只负责一个 Node 类型的实际工作；所有状态转换、预算、成本和事件必须回到 Production/Execution Service。
- Adapter 只负责 Provider 协议转换；不得持有 Project 权限、直接写 NodeRun 或发布 SSE。
- Worker 是薄入口：建立独立 Session、注入 RLS 上下文、调用 Service；不得保存全局 AsyncSession。

### 1.2 禁止的胶水写法

- 禁止 API Route 直接 `session.execute()` 写核心业务表。
- 禁止业务模块直接 `arq.enqueue_job()`；只能调用注册的可靠 Scheduler Interface：NodeRun → `production.scheduler`，AgentRun → `runtime.scheduler`。
- 禁止 Service 直接 `redis.publish()` 发送关键状态；只能写 Outbox。
- 禁止 Provider 回调 HTTP Handler 直接更新 Artifact/NodeRun 或广播 SSE。
- 禁止前端 Store 保存 Project、Shot、Run、Artifact 的权威副本；服务端状态只来自 TanStack Query 快照。
- 禁止通过修改 JSONB 中任意字段逃避 SQL/Pydantic/ORM 的固定字段约束。

## 2. 模块必实现能力

| 模块 | 必须实现 | 不得承担 |
|---|---|---|
| `access` | Cookie 会话、CSRF、User→owned Workspace→Project 所有权、选中 Workspace 上下文、RLS、Workspace BYOK Fernet 加密与脱敏；P0 不含成员、邀请或项目角色。 | Provider 调用、资产删除、图调度、团队协作授权。 |
| `creation` | Brief Revision、CreationPlan、PlanningAuthorization、AgentRun、两级确认、Materialization 幂等、ProjectSnapshot；只经 Command 编排正式对象。 | 自建聊天孤岛、自由 Agent/Tool、直接改生产执行事实、绕过授权/RLS/审核。 |
| `assets` | 剧本、Episode/Scene/Shot、Asset/Character/Reference、Artifact 上传隔离、回温、删除生命周期。 | 缓存命中、成本结算、事件投递。 |
| `production` | GraphVersion 发布、DAG 校验、输入 hash、单飞、NodeRun、拓扑调度、局部重跑。 | Provider SDK、Webhook 验证、SSE 连接。 |
| `execution` | Executor 注册、ProviderOperation、预算预占/结算、取消、重试、Provider Inbox 消费。 | 修改 Graph 定义、直接输出 UI DTO。 |
| `consistency` | 角色七层、剧情四层、Review/Violation、可恢复分类、人工审核决策。 | 绕过预算强制重跑、覆盖已审核 Artifact。 |
| `events` | EventLog、Outbox、死信、Streams、SSE、保留与 Parquet 归档。 | 作为业务真相源、保存完整敏感对象。 |
| `delivery` | SRT、时间线 JSON、FFmpeg 合成、Export/ExportItem、交付包、下载授权。 | PR 工程、私有格式逆向、直接访问未审核 Artifact。 |
| `providers` | 固定 ProviderAdapter 协议、请求/响应规范化、签名校验。 | 业务权限、跨项目路由、数据库状态机。 |
| `runtime` | 已提交 Outbox 的可靠 Arq 派发、稳定 job ID、补偿扫描和 lease 恢复。 | 拥有 AgentRun/NodeRun 状态机、直接改领域表或第二队列。 |

## 2A. 受控混合 Agent 运行时强制规则

1. **状态机**：新 AgentRun 必须以 `queued` 插入，仅允许 `queued → running|cancelled|stale|failed`、`running → succeeded|failed|stale|cancel_requested`、`cancel_requested → cancelled|failed`；Service 使用版本 CAS，数据库触发器拒绝其他迁移和终态复活，用户重试新建 AgentRun。
2. **可靠调度**：创建 AgentRun、EventLog、`runtime.agent_run.dispatch_requested` Outbox 同事务提交；RuntimeScheduler 用 `agent-run:{id}:{dispatch_generation}` 派发，重复 Job 以 CAS 无副作用退出，queued/lease 由补偿扫描恢复；active singleflight 必须包含 project、initiated_by、operation、目标 revision/version、input_hash 与 context_hash。
3. **成本授权**：每个真实文本请求前必须校验未过期且未消费的 PlanningAuthorization 和最坏费用预占；授权签发后不可修改或删除，AgentRun 作为审计记录禁止硬删除，从而保证一次授权至多消费一次；无 Key 返回 `TEXT_PROVIDER_CONFIGURATION_REQUIRED`，不得隐式代付。
4. **不可变来源**：Brief 编辑/refine 创建新 revision，Agent 生成的 revision 必须唯一回链产生它的 AgentRun；Agent 生成的 Plan 必须以唯一 `source_agent_run_id` 回链产生它的 AgentRun，并绑定 `source_brief_revision_id + context_hash`，这些来源字段创建后不可变。succeeded AgentRun 只能回链与 operation/目标来源匹配的唯一结果；晚到输出与目标 revision/version、人工锁定或 context hash 不符时转 `stale`。
5. **执行真相**：AgentRun 1:N ProviderOperation；primary/repair/retry/fallback 各建 attempt。AgentRun 不保存 actual provider/model/token/cost，读取时从 ProviderOperation/CostLedger 聚合。NodeRun 仍为 0..1 ProviderOperation。
6. **取消与未知结果**：取消阻止新 attempt；已送达但结果未知不得盲重试，使用 `PROVIDER_OUTCOME_UNKNOWN`。取消后 Provider 成功仍记费用，但不采纳输出。
7. **受限物化**：模型只能返回 `materialization-p0-v1`；允许 update planning fields、创建 Episode/Scene/Shot draft/Asset/Character/Graph draft 和提出已有实体变更。禁止删除、发布 Graph、发起生成、改执行/成本/审核事实或覆盖人工锁定。
8. **事务与幂等**：`confirm_plan` 是唯一事务拥有者；内部 Command 共用 Session/UoW 且只 flush。`operation_key` 在 Project+Plan 内唯一，重放返回同一实体。
9. **RLS 与上下文**：Worker 只信任 run ID/dispatch generation，从 DB 重建项目、发起者和 RLS；Context 只含当前项目白名单字段与来源版本，不含聊天全文、Key、Embedding、永久 URL。
10. **事件与输出**：事件词汇、错误码、hash、事件 payload 与 Read Model 以 `06` 为准；不输出思维链，SSE 仅触发快照重取。

## 3. 统一异常与状态规则

### 3.1 领域错误格式

所有 API 错误使用 RFC 9457 风格 Problem Details：

```json
{
  "type": "https://dramaforge.local/problems/BUDGET_EXCEEDED",
  "title": "预算不足",
  "status": 409,
  "code": "BUDGET_EXCEEDED",
  "detail": "当前项目可用预算不足以预占本次执行。",
  "trace_id": "..."
}
```

允许的稳定错误码只有：

| 类别 | 错误码 | HTTP | 含义 |
|---|---|---:|---|
| 权限 | `AUTH_REQUIRED`、`FORBIDDEN`、`RLS_CONTEXT_MISSING` | 401/403 | 未认证、无项目权限或缺失事务上下文。 |
| 校验 | `VALIDATION_ERROR`、`GRAPH_INVALID`、`VERSION_CONFLICT` | 400/409 | 请求非法、DAG 非法或乐观锁冲突。 |
| 资产 | `ARTIFACT_UNAVAILABLE`、`ARTIFACT_QUARANTINED`、`ARTIFACT_REFERENCED` | 409/423 | 不可读取、隔离或仍被引用。 |
| 执行 | `NODE_NOT_RUNNABLE`、`IDEMPOTENCY_CONFLICT`、`CANCEL_NOT_ALLOWED` | 409 | Run 状态不允许当前操作。 |
| 预算 | `BUDGET_EXCEEDED`、`BUDGET_RESERVATION_EXPIRED` | 409 | 不足或预占已经失效。 |
| Provider | `PROVIDER_UNAVAILABLE`、`PROVIDER_RATE_LIMITED`、`PROVIDER_REJECTED`、`PROVIDER_TIMEOUT` | 502/503/504 | 可归类的外部失败。 |
| 一致性 | `REFERENCE_REQUIRED`、`REVIEW_REQUIRED`、`CONSISTENCY_BLOCKED` | 409/422 | 缺参考、需审核或规则阻断。 |
| 事件 | `OUTBOX_DELIVERY_PENDING`、`WEBHOOK_INVALID` | 202/401 | 仍待投递或回调签名无效。 |

异常中不得包含 Provider Key、Authorization、完整提示词、Embedding、原始回调 body、内部对象路径或跨项目资源是否存在的信息。

### 3.2 状态迁移强制规则

- NodeRun 只允许 `queued → running → completed|failed|cancel_requested`，或 `queued → cached|blocked_budget|cancelled`。
- `cancel_requested` 只能转为 `cancelled`、`completed_after_cancel` 或 `failed`。
- `completed_after_cancel` 不更新 `GraphNode.latest_successful_run_id`，不进入缓存，不自动下游；只能经有权限审核员显式采纳。
- `cached` 必须有 `reused_from_run_id` 和同一 Artifact；不得有 ProviderOperation、预算预占或真实成本。
- `failed`、`cancelled`、`completed_after_cancel` 不会被自动覆盖；重新执行必须新建 NodeRun 和幂等键。
- 所有终态必须写 `finished_at`，所有失败必须写稳定错误码和脱敏摘要。

## 4. Production Graph、缓存与增量重跑

### 4.1 不可简化的 DAG 规则

1. Graph 定义、GraphVersion、GraphNode/GraphEdge、NodeRun、ProviderOperation、Artifact 是独立层，禁止合并为“任务表”。
2. GraphVersion 发布后不可原地编辑。改变模板、边、端口、参数、人工分镜时必须创建新版本。
3. GraphEdge 必须使用命名 `output_port`、`input_port`、`position`、`required`；禁止 `depends_on UUID[]` 作为运行时唯一依赖。
4. 执行前必须验证：同项目权限、无环、端口完整、required 输入可得、Artifact 可用、预算可预占。
5. 任何成功产生新 Artifact 的强制重跑，必须用新的 `file_hash` 使正确下游 stale；不得伪造旧 hash 维持缓存。

### 4.2 `input_hash` v2 固定材料

Hash 必须由规范化 JSON 和 SHA-256 计算，材料固定为：

```text
project_id + graph_version_id + node_id
node_type + executor_id + executor_version + template_version
每个命名输入的 input_port + position + output_port + upstream artifact.file_hash
规范化 provider/model/采样/提示词/时长/分辨率等有效参数
provider 指纹 + Prompt 模板版本 + 连续性规则版本
```

不得纳入：`created_at`、展示名、排序 UI、`retry_count`、Run ID、前端临时状态等噪声。

### 4.3 缓存命中合同

- 同一 Project、同一 GraphVersion、同一 Node、同一 `input_hash` 才有资格复用；禁止跨项目缓存。
- 命中时必须创建新的 `NodeRun(status=cached)`，写入本次开始/结束时间，并让 `reused_from_run_id` 直指真实源 Run；禁止形成多跳复用链。
- 新 Run 引用源 Run 的同一个不可变 Artifact，不能复制对象或创建伪 Artifact。
- `GraphNode.latest_successful_run_id` 指向该缓存 Run，但成本只归源 ProviderOperation。
- 缓存 Run 的 `provider_cost=0`、`platform_cost=0`；节省金额只能记录在 `avoided_cost_estimate`，不能进入真实 `cost_ledger`。
- 同 hash 并发必须单飞。首 Run 获执行权；其他请求等待、合并或在其成功后创建 cached Run。任何路径最多创建一个真实 ProviderOperation。
- 缓存命中写 `node.cached`，不得伪装为 `node.completed`。
- 仅 `available` Artifact 可命中。`cold` 必须先校验回温，回温失败按未命中处理；`quarantined`、`delete_requested`、`deleted` 永远不可命中。

## 5. 预算、ProviderOperation、取消与回调

### 5.1 预算和成本

- 创建真实 ProviderOperation 前，在同一事务中创建 BudgetReservation。NodeRun 与 AgentRun 都使用执行来源 XOR；AgentRun 的预占覆盖 primary、一次 schema repair 和允许重试的最坏可接受费用。
- 预算不足：NodeRun 进入 `blocked_budget`，写 `node.blocked_budget`，不调用 Provider。
- 成功、失败、取消完成后必须结算：真实费用追加 `cost_ledger`；剩余预占释放；晚到费用、汇率和计价修正追加 adjustment，不改写历史。
- 真实费用超预占时记录超额原因、冻结项目后续真实 Provider 调度并告警；已发生费用如实保留。
- BYOK 的用户承担估算与平台收入不是同一指标；每笔成本保存原始金额/币种/计价快照与统一展示金额。

### 5.2 取消

- `cancel(run_id)` 幂等：首先在事务中置 `cancel_requested`、记录请求人/时间/原因、写 Outbox，并停止新下游调度。
- 未发出 Provider 请求的 Run 可转 `cancelled`；Dispatcher 发请求前必须重查状态。
- 已有远端任务时记录取消尝试、Provider 响应和远端 ID；Provider 支持时必须调用取消接口。
- 取消与轮询/回调竞争，若远端成功已发生则转 `completed_after_cancel`；保留 Artifact 供对账，但不自动采纳、缓存或下游。
- 取消不等于免单。Provider 已产生的费用必须结算。

### 5.3 Provider 回调与轮询

- Webhook Handler 只验证签名、时间戳、重放窗口，按 `provider + provider_event_id` 唯一写 `provider_inbox` 后快速返回。
- 无 Provider 事件 ID 时，使用规范化 payload 哈希去重。原始 payload 加密短期保存，不进普通日志。
- 回调只能以已存 `remote_task_id` 找到 ProviderOperation；绝不相信 payload 声称的 project、user、成本、对象 URL。
- Inbox Worker 与轮询共用同一个带版本条件的状态迁移函数。重复、乱序、终态回调不能重复创建 Artifact、成本分录、Outbox 或下游任务。
- Artifact 下载、哈希、MIME、尺寸/时长和安全扫描通过后，才能在同一业务事务提交 Run 成功、EventLog 和 Outbox。

## 6. 一致性引擎：强制七层角色防线

任何含角色的生成节点必须按以下顺序执行；少一层即为不合格实现。

| 层 | 强制输入/动作 | 阻断或输出 |
|---|---|---|
| 1. Canonical Reference | 每个主角色至少一张经审核 canonical 图。 | 缺失时 `REFERENCE_REQUIRED`，禁止生成。 |
| 2. Reference Set | 按景别、角度、服装、场景选择参考图集合。 | 选择记录写入 Run 请求摘要。 |
| 3. Prompt Lock | 角色锁定描述与禁改字段拼接到生成提示词。 | 用户自由文本不得覆盖 locked prompt。 |
| 4. Seed Policy | 有稳定 seed 的 Provider 使用角色/镜头 seed 策略；不支持时记录 capability。 | 不支持不能伪造 seed 已生效。 |
| 5. Face Embedding | heavy Worker 用 InsightFace ONNX 提取 512 维归一化向量，按角色阈值比较。 | 分类为 `matched`、`below_threshold`、`no_face`、`multiple_faces`、`low_quality`、`provider_error`。 |
| 6. Reference Injection | Adapter 接收结构化 `CharacterReference[]`，注入参考图、锁定 prompt、embedding 摘要和 seed。 | 禁止只把参考图 URL 拼进字符串。 |
| 7. Video Drift | 视频每 5 帧固定采样，检测人脸漂移并生成 Review。 | 漂移率 ≥10% 必须审核或按策略修复。 |

阈值只能由 Reference Set 校准产生：默认策略为样本充分时 `μ - 2σ`，冷启动时使用全局保守阈值并标记 `calibration_state=cold`。阈值、样本数、算法版本必须随 Review 保存。

自动修复只允许 `below_threshold` 和经策略确认的可恢复 Provider 错误；`no_face`、`multiple_faces`、`low_quality` 优先进入人工审核或特定修复策略。每个 Node 的 `max_auto_attempts` 与预算上限必须同时生效。

## 7. 一致性引擎：强制四层剧情检查

| 层 | 必须能力 | 生成时机 | 输出 |
|---|---|---|---|
| 1. Asset State Timeline | 在 Scene/Shot 维度记录角色、服装、道具、伤势、场景状态快照与变化。 | 规划、编辑和运行前。 | 可回溯状态上下文。 |
| 2. Continuity Rules | 持久化项目/剧集/场景规则，含条件、范围、阻断等级和修复模板。 | Graph 发布和运行前。 | `block` / `warn` / `suggest` 判定。 |
| 3. Pre-injection | 将适用规则、资产状态和角色约束以结构化片段注入 Prompt/ProviderRequest。 | 每次真实生成前。 | `injection_snapshot` 与规则版本。 |
| 4. Post-QA | 对生成 Artifact 做视觉/文本质检，生成 Review 与可回链 Violation。 | Artifact 验证后、下游采纳前。 | 通过、警告、阻断、修复建议。 |

- `block` 规则失败时不得创建真实 ProviderOperation。
- `warn` 可继续执行，但必须产生 Review 且交付前需要批准。
- `suggest` 只提供修复文本，不改变状态机。
- 每条 Violation 必须回链 `rule_id`、`shot_id`、可选 `asset_id`、证据摘要、严重级别和建议动作。

## 8. 事件、SSE 和生命周期

### 8.1 Transactional Outbox

1. 业务事务内按顺序写业务状态、EventLog、Outbox，再 COMMIT。
2. Dispatcher 使用 `FOR UPDATE SKIP LOCKED`、`leased_until`、`locked_by` 安全领取。
3. 语义是 at-least-once；消费者必须按 `event_id` 去重。
4. 发布 Redis 后、写 `published_at` 前崩溃允许重复投递，禁止尝试“恰好一次”。
5. 未投递事件永不参与常规清理；达到重试上限原子转 `outbox_dead_letters`，告警并提供人工重放。
6. 已投递事件默认保留 7 天，先导出 Parquet 至 MinIO，再删 PostgreSQL；归档失败不得删除源记录。

### 8.2 SSE Envelope

```json
{
  "id": "event UUID",
  "type": "node.completed",
  "occurred_at": "RFC3339 UTC",
  "project_id": "UUID",
  "entity": {"type": "node_run", "id": "UUID", "version": 3},
  "payload": {"artifact_id": "UUID"}
}
```

payload 只含分发所需最小字段；禁止含 BYOK、提示词全文、Embedding、完整业务对象和永久对象 URL。前端收到关键事件只能失效 Query 并重取快照，不能以事件 payload 作为最终实体状态。

### 8.3 Artifact 生命周期

- 状态固定为 `quarantined`、`available`、`cold`、`delete_requested`、`deleted`。
- 上传对象先隔离，完成 MIME 嗅探、尺寸/时长、哈希和安全扫描后才可 `available`。
- 删除是“逻辑请求→引用/保留期/legal hold 检查→物理删除”三阶段。NodeRun、ExportItem、Review、人工固定引用存在时禁止物理删除。
- 物理删除后保留 tombstone、内容哈希、删除原因和审计；回温、复制、删除都必须重新校验哈希。

## 9. 强制测试门槛

任何模块合并前，至少具备所属层的测试：

- 单元：hash 命名端口顺序、DAG 无环、阈值冷启动、错误脱敏、payload schema。
- 集成：start_project 未授权零文本调用、无 Key 手工路径、Brief revision 不可变、两级确认、AgentRun 调度/lease/取消/stale、1:N ProviderOperation 与成本、Materialization UoW/幂等、NodeRun 缓存单飞与 AgentRun active singleflight、Outbox 重放/死信、SSE 恢复、RLS/Worker 隔离。
- Provider 契约：create/poll/cancel/限流/超时、`safe_to_retry`、一次 schema repair、BYOK 候选内降级、错误归一化/实际成本/Webhook 签名、重放、乱序。
- 生命周期：已引用 Artifact 拒删、冷存储回温失败不假命中、归档失败不删除 Outbox 源记录。
- 回归：固定人脸/视频漂移样本、剧情规则样本、MP4/SRT/时间线 JSON 黄金夹具。
- 双模式 E2E：创建正式 Project→规划费用授权→Brief 生成/手工编辑与确认→Plan 确认→白名单物化→专业工作台→首帧；并覆盖 Agent 取消、无 Key、SSE 恢复及切回后人工锁定/成本/Run/产物/权限/待办一致。

没有以上测试证据时，不得声称模块完成、不得接入真实 Provider、不得扩大 P0 范围。
