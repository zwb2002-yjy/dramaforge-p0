/** @deprecated Generated contract now lives at shared/api/generated.ts (Phase 2 §18.2).
 *
 * This file is a compatibility re-export so any historical import path keeps
 * working. Remove this shim in a later cleanup once all consumers are on
 * `shared/api/generated`.
 */
export * from "../shared/api/generated";
