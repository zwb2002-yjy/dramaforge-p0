# DramaForge 全局目录规范（P0）

**状态：FROZEN / 目录是架构边界，不是建议**
**同步基线：2026-08-01（个人私有 Workspace、本地 TTS 与受控混合 Agent Runtime）**

> 本规范是 P0 冻结包的目录事实源；Agent Runtime 文件职责同时受 `06_受控混合Agent运行时规范.md` 约束。快速出片模式与专业工作台模式必须共享 Project、资产、Production Graph 和运行时 Module；不得为快速模式另建平行前端、后端或数据目录。
>
> 本规范定义 P0 仓库的完整目录、模块归属与文件职责。禁止新增、删除、合并、重命名或跨层移动目录。需要新增源文件时，只能放入本文已列出的目录，并在对应模块的 `__init__.py` 或注册表中显式登记。若现有目录无法容纳职责，必须先走 ADR；不得临时创建 `common`、`helpers`、`misc`、`utils2`、`services2` 或平行模块。

## 1. 根目录

```text
dramaforge/
├── .gitattributes
├── .gitignore
├── 01_项目总需求.md
├── 02_全栈技术栈锁定表.md
├── 03_全局目录规范.md
├── 04_数据定义全集.md
├── 05_模块落地约束.md
├── 06_受控混合Agent运行时规范.md
├── DramaForge架构决策与技术选型书.md
├── DramaForge双模式产品与架构汇报方案.md
├── Agnes-first通用Provider适配规划.md
├── AI短剧工作台完整实施规划.md
├── README.md
├── agent.md
├── AGENT_EXECUTION_PROTOCOL.md
├── .agent-control/
│   └── control.ps1
├── docker-compose.yml
├── docker-compose.gpu.yml
├── .env.example
├── frontend/
├── backend/
├── infra/
├── scripts/
├── fixtures/
└── docs/
```

| 路径 | 唯一职责 | 禁止放入 |
|---|---|---|
| 根目录冻结包（`01`–`06`） | 需求、技术、目录、数据、运行时冻结契约；按主题构成唯一实现规范。 | 代码、临时笔记、重复说明。 |
| 根目录决策与汇报文档 | `DramaForge架构决策与技术选型书.md` 记录决策理由和路线，`DramaForge双模式产品与架构汇报方案.md` 用于汇报，`Agnes-first通用Provider适配规划.md` 记录经官方资料核验的 Provider 合同与适配规划，`AI短剧工作台完整实施规划.md` 为归档历史。 | 覆盖冻结包、保存 API Key 或运行证据。 |
| 根目录开发入口 | `README.md` 说明仓库入口，`agent.md` 定义当前开发任务，`AGENT_EXECUTION_PROTOCOL.md` 定义本地记录和 Git 分支/worktree 规则，`.agent-control/` 隔离开发 Agent 日志工具。 | 第二套需求、数据或产品运行时规范。 |
| `docker-compose*.yml` | 本地/私有化部署服务编排。 | 业务逻辑、环境密钥。 |
| `infra/` | Nginx、Prometheus、Grafana 等基础设施配置。 | 应用代码、迁移。 |
| `scripts/` | 明确可重复执行的开发、fixture、OpenAPI 生成脚本。 | 业务服务、一次性手工脚本。 |
| `fixtures/` | 冻结剧本、图像、视频、SSE 和 Provider mock 测试材料。 | 用户真实数据、生产密钥。 |
| `docs/` | ADR、Spike 报告、运行手册、Agent Runtime 版本说明/脱敏夹具、验收记录。 | 与根目录 6 份基线重复的规范。 |

## 2. 前端目录

```text
frontend/
├── package.json
├── package-lock.json
├── tsconfig.json
├── vite.config.ts
├── tailwind.config.ts
├── eslint.config.js
├── src/
│   ├── main.tsx
│   ├── app.tsx
│   ├── router.tsx
│   ├── routes/
│   │   ├── __root.tsx
│   │   ├── index.tsx
│   │   ├── projects.$projectId.tsx
│   │   ├── projects.$projectId.quick.tsx
│   │   ├── projects.$projectId.assets.tsx
│   │   ├── projects.$projectId.storyboard.tsx
│   │   ├── projects.$projectId.production.tsx
│   │   ├── projects.$projectId.delivery.tsx
│   │   └── projects.$projectId.audit.tsx
│   ├── components/
│   │   ├── ui/
│   │   ├── workstation/
│   │   ├── shared/
│   │   └── sse/
│   ├── features/
│   │   ├── projects/
│   │   ├── creation/
│   │   ├── assets/
│   │   ├── storyboard/
│   │   ├── production/
│   │   ├── review/
│   │   ├── delivery/
│   │   └── audit/
│   ├── hooks/
│   ├── lib/
│   ├── stores/
│   ├── types/
│   └── styles/
└── tests/
    ├── unit/
    └── e2e/
```

### 2.1 前端文件职责

| 文件/目录 | 责任 | 严格禁止 |
|---|---|---|
| `src/main.tsx` | React 根挂载、严格模式、全局样式入口。 | Router/Query 的业务配置。 |
| `src/app.tsx` | QueryClient、RouterProvider、全局错误边界。 | 页面业务、HTTP 请求。 |
| `src/router.tsx` | TanStack Router 实例和 route tree 注册。 | 页面组件实现、权限 API 请求。 |
| `src/routes/*.tsx` | 路由参数校验、页面壳和 feature 组合。 | 直接 fetch、复杂状态机、可复用领域逻辑。 |
| `components/ui/` | shadcn/Radix 基础组件的项目封装。 | 领域组件、API 调用。 |
| `components/workstation/` | 三栏布局、导航、面板容器。 | 资产/图执行的领域逻辑。 |
| `components/shared/` | 跨 feature 的展示型组件：空状态、错误提示、权限提示。 | Store、Query、Provider 细节。 |
| `components/sse/` | SSE 连接状态和事件提示展示。 | 事件持久化、业务状态真相。 |
| `features/<name>/` | 该领域的页面片段、Query hooks、表单、交互组件。 | 其他领域的内部文件引用。 |
| `features/creation/` | 快速入口、规划费用授权、Brief/Plan 两级确认、AgentRun 取消/错误动作、无 Key 手工路径、模式切换与待办摘要；只消费 ProjectSnapshot。 | 自建聊天真相、直接执行 Provider、复制 Project/Run/成本实体或展示思维链。 |
| `hooks/useSSE.ts` | EventSource 生命周期、Last-Event-ID、本地连接状态。 | 复制后端实体到 Zustand。 |
| `lib/api.ts` | 唯一 REST 客户端、错误解码、认证 cookie 请求配置。 | 页面专用 URL 拼接。 |
| `lib/queryKeys.ts` | 唯一 Query Key 工厂。 | 组件 JSX。 |
| `stores/uiStore.ts` | UI 偏好、面板展开、选中 ID、草稿未保存标记。 | Project/Shot/Artifact 服务端数据。 |
| `types/api.ts` | OpenAPI 自动生成类型，禁止人工编辑。 | 领域 UI 类型。 |
| `types/view.ts` | 前端派生展示类型。 | 后端 DTO 副本。 |
| `styles/index.css` | Tailwind 层、全局 token 和极少量全局样式。 | 组件业务样式文件体系。 |

每个 `features/<name>/` 只允许以下文件名模式：`api.ts`、`queries.ts`、`mutations.ts`、`schema.ts`、`types.ts`、`components/*.tsx`。不能在 feature 下新建平行 `services`、`stores`、`utils` 子目录。

## 3. 后端目录

```text
backend/
├── pyproject.toml
├── requirements.lock
├── alembic.ini
├── alembic/
│   ├── env.py
│   ├── script.py.mako
│   └── versions/
├── app/
│   ├── __init__.py
│   ├── main.py
│   ├── config.py
│   ├── api/
│   │   ├── __init__.py
│   │   ├── deps.py
│   │   ├── errors.py
│   │   └── v1/
│   │       ├── router.py
│   │       ├── auth.py
│   │       ├── creation.py
│   │       ├── projects.py
│   │       ├── assets.py
│   │       ├── storyboard.py
│   │       ├── production.py
│   │       ├── reviews.py
│   │       ├── exports.py
│   │       ├── events.py
│   │       └── provider_webhooks.py
│   ├── shared/
│   │   ├── db.py
│   │   ├── base.py
│   │   ├── enums.py
│   │   ├── errors.py
│   │   ├── ids.py
│   │   ├── json.py
│   │   ├── security.py
│   │   └── observability.py
│   ├── access/
│   │   ├── models.py
│   │   ├── schemas.py
│   │   ├── repository.py
│   │   ├── service.py
│   │   ├── rls.py
│   │   └── dependencies.py
│   ├── creation/
│   │   ├── models.py
│   │   ├── schemas.py
│   │   ├── repository.py
│   │   ├── service.py
│   │   ├── planner.py
│   │   ├── agent_runtime.py
│   │   ├── context.py
│   │   ├── materializer.py
│   │   └── snapshot.py
│   ├── assets/
│   │   ├── models.py
│   │   ├── schemas.py
│   │   ├── repository.py
│   │   ├── service.py
│   │   ├── references.py
│   │   └── lifecycle.py
│   ├── production/
│   │   ├── models.py
│   │   ├── schemas.py
│   │   ├── repository.py
│   │   ├── service.py
│   │   ├── templates.py
│   │   ├── planner.py
│   │   ├── runner.py
│   │   ├── cache.py
│   │   ├── scheduler.py
│   │   └── state_machine.py
│   ├── execution/
│   │   ├── models.py
│   │   ├── schemas.py
│   │   ├── repository.py
│   │   ├── service.py
│   │   ├── executor.py
│   │   ├── registry.py
│   │   ├── retry.py
│   │   ├── cancellation.py
│   │   ├── budget.py
│   │   └── provider_inbox.py
│   ├── consistency/
│   │   ├── models.py
│   │   ├── schemas.py
│   │   ├── service.py
│   │   ├── continuity.py
│   │   ├── prompt_injection.py
│   │   ├── post_qa.py
│   │   ├── face.py
│   │   ├── calibrator.py
│   │   ├── reference_selection.py
│   │   └── video_drift.py
│   ├── events/
│   │   ├── models.py
│   │   ├── schemas.py
│   │   ├── service.py
│   │   ├── outbox.py
│   │   ├── dispatcher.py
│   │   ├── streams.py
│   │   ├── sse.py
│   │   └── cleanup.py
│   ├── delivery/
│   │   ├── models.py
│   │   ├── schemas.py
│   │   ├── repository.py
│   │   ├── service.py
│   │   ├── ffmpeg.py
│   │   ├── subtitles.py
│   │   ├── timeline.py
│   │   └── package.py
│   ├── runtime/
│   │   ├── __init__.py
│   │   └── scheduler.py
│   ├── providers/
│   │   ├── base.py
│   │   ├── router.py
│   │   ├── openai.py
│   │   ├── flux.py
│   │   ├── kling.py
│   │   ├── local_tts.py
│   │   ├── azure_tts.py
│   │   └── comfyui.py
│   └── workers/
│       ├── main.py
│       ├── jobs.py
│       ├── default.py
│       └── heavy.py
└── tests/
    ├── conftest.py
    ├── unit/
    ├── integration/
    ├── contract/
    └── regression/
```

### 3.1 后端顶层文件职责

| 路径 | 责任 | 禁止 |
|---|---|---|
| `app/main.py` | 创建 FastAPI、注册 v1 router、异常处理、生命周期和指标路由。 | 业务 SQL、Provider 调用、Arq Job 实现。 |
| `app/config.py` | 读取并校验环境配置。 | 读取数据库数据、运行时临时开关。 |
| `app/shared/db.py` | Engine、sessionmaker、事务和 `SET LOCAL` RLS 上下文。 | 领域查询。 |
| `app/shared/base.py` | SQLAlchemy DeclarativeBase、审计时间 mixin。 | 具体表模型。 |
| `app/shared/enums.py` | 04 文档规定的 Python Enum 唯一来源。 | 业务判断分支。 |
| `app/shared/errors.py` | 统一领域异常和稳定错误码。 | HTTP 细节、日志泄漏。 |
| `app/api/deps.py` | Request 级认证、会话、RLS 上下文依赖组合。 | Domain Service。 |
| `app/api/errors.py` | 将领域异常映射为 Problem Details。 | 创建新异常类别。 |
| `app/api/v1/*.py` | 参数/权限入口、调用一个 Application Service、返回 Pydantic schema。 | SQL、Provider HTTP、Arq 直接投递。 |
| `<module>/models.py` | 该模块拥有的 SQLAlchemy ORM 类。 | 其他模块表的写操作。 |
| `<module>/schemas.py` | 该模块公开的 Pydantic 结构。 | ORM Session、HTTP 请求。 |
| `<module>/repository.py` | 该模块的数据查询与受约束写入。 | 跨模块编排、Provider 调用。 |
| `<module>/service.py` | 事务边界内的用例编排与权限前置；被外层共享 Unit of Work 调用的 Command 只 `flush`，不得自行 `commit`。 | 直接 FFmpeg/HTTP/Redis、嵌套事务或内部 commit。 |
| `creation/planner.py` | CreativeBrief/CreationPlan 的版本化 prompt、严格输入/输出 Schema 与能力声明。 | Provider SDK/HTTP、状态迁移、数据库写入或自由 Tool。 |
| `creation/agent_runtime.py` | AgentRun 领取、Context 编译、授权/预算、ProviderOperation attempts、严格校验、一次 repair 和条件写回。 | 自由工具、跨项目检索、直接物化正式对象。 |
| `creation/context.py` | 白名单 CreationContext、稳定排序/hash、来源版本和 compiler version。 | 聊天全文、任意文件读取、BYOK/Embedding/永久 URL。 |
| `creation/materializer.py` | 校验并解释 `materialization-p0-v1`，按 operation_key 幂等调用领域 Command。 | 自由 SQL/路径/工具名、发布 Graph、发起生成。 |
| `production/scheduler.py` | 仅负责 NodeRun 的 Outbox→Arq 可靠派发、稳定 job ID、queued 补偿与执行恢复。 | 派发 AgentRun、拥有 NodeRun 状态机或业务 Module 直接 enqueue。 |
| `runtime/scheduler.py` | 仅负责 AgentRun 的 Outbox→Arq 可靠派发、稳定 job ID、queued 补偿、lease/dispatch_generation 恢复。 | 派发 NodeRun、拥有 AgentRun 状态机或业务 Module 直接 enqueue。 |
| `providers/*.py` | 单一 Provider Adapter 实现。 | 领域状态迁移、直接 SSE。 |
| `workers/*.py` | Arq Settings 与薄 Job 入口；转调 Service/Executor。 | 自建状态机、共享 Session。 |

### 3.2 领域文件归属

- `access` 唯一拥有 User、Workspace、Project、`UserProjectPreference` 存储、工作区 BYOK 凭据边界和 RLS 上下文；P0 不实现成员资格、邀请或团队角色。
- `creation` 唯一拥有 CreativeBrief/Revision、CreationPlan、PlanningAuthorization、AgentRun、MaterializationOperation、模式偏好用例、确认门槛与 ProjectSnapshot；模式偏好持久化只能通过 `access` 公开接口，只通过公开 Command 编排正式对象，不得直接改变 NodeRun 状态。
- `assets` 唯一拥有 Episode、Scene、Shot、Asset、Character、CharacterReference、Artifact 的业务读取与生命周期。
- `production` 唯一拥有 Graph、GraphVersion、GraphNode、GraphEdge、NodeRun、缓存和增量调度。
- `execution` 唯一拥有 ProviderOperation、预算预占、成本结算、取消、Provider Inbox。
- `consistency` 唯一拥有连续性规则、状态时间线、Review、Violation、人脸校准及视频漂移判断。
- `events` 唯一拥有 EventLog、Outbox、Streams、SSE 和清理作业。
- `runtime` 只实现可靠 Scheduler 技术适配，不拥有任何领域状态；AgentRun 属于 `creation`，NodeRun 属于 `production`。
- `delivery` 唯一拥有 Export、ExportItem、时间线 JSON、SRT、FFmpeg 合成和交付包。

跨模块协作只允许通过公开的 `service.py`/Command Interface 与 `schemas.py`；Repository 只能服务所属模块，禁止以跨模块 Repository 写入规避领域 Command。禁止直接 import 其他模块私有函数或将 ORM 对象穿透 3 个以上模块。

## 4. 基础设施、脚本、夹具与文档

```text
infra/
├── nginx/nginx.conf
├── prometheus/prometheus.yml
└── grafana/provisioning/

scripts/
├── generate_openapi_types.ps1
├── run_s0_face_spike.py
├── run_s0_jianying_spike.py
├── export_outbox_parquet.py
└── seed_acceptance_fixture.py

fixtures/
├── scripts/p0_10_shots.md
├── images/character_canonical/
├── media/
├── providers/
├── graph/
└── expected/

docs/
├── MVP能力延期台账.md
├── 产品阶段与效果路线图.md
├── 开发执行检查点.md
├── P0开发验收执行方案-Agnes中国站.md
├── adr/
├── agent-runtime/
├── spikes/
├── runbooks/
└── acceptance/
```

- `run_s0_*` 是唯一允许脱离主应用的 Spike 脚本，输出必须写入 `docs/spikes/`。
- `.agent-control/control.ps1` 只负责向本机 `.agent-control/PROGRESS.jsonl` 追加任务事实，以及查看尾部和未闭合任务；日志被 Git 忽略，不属于产品运行时，也不得被产品代码读取。并行写入由 Git 分支、GitHub PR 和独立 `.worktrees/` 隔离。
- `fixtures/expected/` 存放黄金输出的元数据、哈希和审计预期，不存真实 BYOK 或受限生物特征数据。
- `docs/adr/` 中的 ADR 不得覆盖根目录冻结文件；修改冻结文件时 ADR 只记录理由和迁移路径。
- `docs/agent-runtime/` 只保存 prompt/schema/context compiler 版本说明、脱敏契约夹具和运行手册，不保存 Key、完整 prompt/response 或第二套规范。
- `docs/MVP能力延期台账.md` 是 P0 外能力的唯一登记入口，记录能力状态、前置领域事实、准入 Gate 和验收条件；不得用它覆盖 `01`–`06` 的冻结契约。

## 5. 目录强制检查

CI 必须执行目录清单检查：

1. 不允许未登记目录。
2. 不允许 `__pycache__`、`.pytest_cache`、构建产物或密钥进入仓库。
3. 不允许从 `api/` 直接 import `providers/`、`workers/` 或 `repository.py` 的私有函数。
4. 不允许前端 `features/` 之间相互引用内部组件；跨 feature 组件必须上移到 `components/shared/`。
5. 新文件必须有明确单一职责；若职责无法归入现有目录，先提交 ADR，而不是新建文件夹。
