# Historical 720bde4 release evidence

This archive integrates the previously unmerged `agent/release-evidence-720bde4` branch as historical evidence only. It is not a current Release Candidate, fresh Golden, current Goal completion, new product plan or permission for paid calls.

- Original branch commit: `1b2a6a2c129bfa09e9fcc0ec1ce971aab216d6d3`.
- The two Golden JSON files are stored byte-for-byte.
- Four original Markdown files are preserved as UTF-8 strings in `source-documents.json`, including their exact line endings and Markdown hard breaks. This avoids presenting stale COMPLETE/READY text as current live Task/Goal documents. Decode a document's `content_utf8` as UTF-8 to recover its original bytes.
- `source-manifest.json` records each original repository path and SHA-256. All six source snapshots remain additionally reachable through the retained Git merge parent.
- Current Goal, G7/G8 contracts and the current release report retain their pre-integration content. Refer to the current program and task-specific evidence for present status.

No Provider request, production write or deployment was performed to create this archive. The initial local credential/signed-URL scan reported no flags; exact-source CI Security remains required.
