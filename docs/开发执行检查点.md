# DramaForge 开发执行检查点

**状态：产品竖切已落地 / 真 PG 集成依赖 Docker 稳定性 / P0 MVP Gate 未全部关闭 / 不进入 P1/P2**

**最近更新：2026-07-21（产品路径纠偏交付）**

**唯一开发工作区：`D:\调研\dramaforge`**

## 1. 当前定位

| 项目 | 事实 |
|---|---|
| 产品阶段 | S1 基础 + S2 产品路径 + S4/S5 服务层已实现并有测试；**未宣称 P0 MVP 完成** |
| 当前任务 | 稳定 Docker 上跑通 PG integration；可选 FFmpeg MP4 哈希 |
| P1/P2 | 未开始 |

## 2. 本轮产品路径交付（非 LEGACY_SPIKE）

| 能力 | 位置 | 测试 |
|---|---|---|
| RLS 迁移 FORCE + app 角色 | `alembic/versions/20260721_0005_rls_policies.py` | 结构断言 + PG 可选 |
| SET LOCAL RLS 上下文 | `app/shared/db.py` | API deps |
| start_project + Brief | `app/creation/service.py` | unit + API |
| 首帧 Worker 路径 | `app/execution/product_path.py` | `test_product_path_shipped` |
| Scheduler | `app/runtime/scheduler.py` | unit drain queued |
| Arq jobs | `workers/jobs.py` execute_node_run/dispatch_outbox | 结构 + health |
| MinIO/内存对象存储 | `app/storage/minio_store.py` | unit store hash/size |
| 9 节点 10 Shot + 锁 | `app/execution/shot_p0.py` | unit |
| 导出 timeline/SRT/package/FFmpeg | `app/delivery/export_service.py` | unit；FFmpeg 显式错误 |
| API | creation + production snapshot/execute/export | unit API |
| 前端工作流 | 登录建项 + 快速 Brief/Plan/执行 + 专业快照 | vitest 3 |

## 3. 验证证据

- 后端 unit + integration：**73 passed**（scratch `p0-backend-pytest.txt`，含真 PG）
- 前端 vitest：**3 passed**（`p0-frontend.txt`）
- PG：`test_s1_db_migration_pg` + `test_p0_product_path_pg`（首帧 Worker 路径 + 10-shot + RLS 抽样）
- Gate 摘要：`{SCRATCH}/p0-gate-summary.txt`
- 提交：`dcda7d2`

## 4. 明确未宣称 / 残留

- **不宣称 P0 MVP 完成**（`01` §3.1 全 18 条未在稳定集成环境全绿）
- Docker Desktop 本机易断（内存紧张）→ PG RLS 连续证明不稳
- FFmpeg 缺失时 MP4 为 `FFMPEG_SKIPPED`/`NOT_FOUND`，非静默占位成功
- S0-A 人脸 FAR/FRR 样本仍 BLOCKED_BY_FIXTURE
- LEGACY_SPIKE `pipeline.py` 仍存在但禁止作为 Gate 证据

## 5. 恢复

```powershell
docker compose up -d postgres redis minio
cd backend; .\.venv\Scripts\python.exe -m alembic upgrade head
.\.venv\Scripts\python.exe -m pytest -q
cd ..\frontend; npm.cmd run test
```
