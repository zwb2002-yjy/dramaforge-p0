# DramaForge 开发执行检查点

**状态：产品竖切加深（共享 ObjectStore + 双源人脸 + 剧本导入 + 黄金路径）/ unit 85 passed / PG integration 因 Docker 守护进程未就绪跳过 / 不写「P0 MVP 完成」**

**最近更新：2026-07-21**

## 本轮完成（可验证）

| 项 | 证据 |
|---|---|
| 进程内共享 ObjectStore | `get_object_store()` / `reset_object_store_for_tests()`；Worker 与 Export 同单例 |
| 双源人脸审核 | `face_review_images(probe, canonical)`；同图 pass / 异图 block；禁止仅自比对 |
| 内容敏感 embedding | `image_embed` 以 hash 为主，不同 Fake PNG 可区分 |
| P0 Gate 矩阵 | `tests/unit/test_p0_gate_matrix.py`：start_project、enqueue→Worker、canonical 拒绝、budget、10-shot+lock、export 哈希、跨项目拒绝、剧本导入、cache/cancel |
| 剧本导入 | `assets/script_import` + migration `0007`；黄金样本 `fixtures/scripts/p0_10_shots.md`（3 场 10 Shot 主角 Lin Xia） |
| 角色 canonical | `register_lead_character` + `require_canonical_for_shot` |
| 黄金路径 | `run_golden_p0_path`：import → character → 10×9 节点 → export |
| 前端专业台 | 导入黄金剧本 + 列出 Shot + 导出按钮 |
| unit | **85 passed**（`pytest tests/unit`） |
| frontend build | `tsc --noEmit` + `vite build` OK |

## §3.1 可关闭行（本地 fake Adapter / SQLite unit）

| 条件 | 状态 |
|---|---|
| 1 项目/成员/画幅 | partial（budget 字段有；成员分配 API 有限） |
| 2 start_project 零文本 ProviderOp | **pass**（matrix） |
| 3 Brief/Plan 修订链 | partial（手工路径） |
| 4 独立 ProviderOperation | partial（primary 有） |
| 5 Redis/Outbox 补偿 | partial（本地 outbox 测） |
| 6 materialization 白名单 | partial |
| 7 无 Key 手工 | **pass** |
| 8 剧本导入 Episode/Scene/Shot | **pass**（fixture + unit） |
| 9 canonical 拒绝 | **pass** |
| 10 ≥10 Shot Graph | **pass**（golden） |
| 11 人脸 512 维比对 | **pass**（hash stand-in；InsightFace 真模型 = S0-A） |
| 12 连续性检查 | partial（四层中有基础规则） |
| 13 字幕局部重跑 | **pass** |
| 14 cache NodeRun | **pass**（matrix） |
| 15 单飞 | **pass**（single_flight_claim unit；PG 并发仍需） |
| 16 budget / cancel | budget **pass**；completed_after_cancel 信号 **pass** |
| 17 SSE Last-Event-ID | partial（hub 单元） |
| 18 导出哈希可追溯 | timeline/srt/package **pass**；MP4 依赖 FFmpeg/帧，fail-closed |

## 残留（故不盖「P0 MVP 完成」章）

1. **Docker Desktop 守护进程未就绪** → 本机 PostgreSQL/Redis/MinIO 集成与 RLS 演练未重跑  
2. **S0-A FAR/FRR** 合法人脸样本不足 → `BLOCKED_BY_FIXTURE`  
3. **Playwright** 浏览器 E2E  
4. **真实 multi-provider BYOK 浸泡**（需用户 Key 与费用授权）  
5. Agent primary/repair/retry 分录、并发单飞 CAS、冷存储回温、死信 runbook 全量演练仍需 PG  

## 验证命令

```text
cd backend
.\.venv\Scripts\python.exe -m pytest tests/unit -q
# 85 passed

cd frontend
npm run build
# tsc + vite OK
```

## 唯一下一步

1. 启动 Docker Desktop → `docker compose up -d postgres redis minio` → alembic upgrade → `pytest tests/integration`  
2. 补并发单飞与 RLS 越权 PG 证明  
3. 不宣称 P0 MVP 完成，直到非外部 §3.1 行 + PG Gate 全绿  
