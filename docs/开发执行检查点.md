# DramaForge 开发执行检查点

**状态：unit + PG integration Gate 绿 / HTTP 冒烟绿 / 残留 S0-A·Playwright browser·真实 BYOK / 不写「P0 MVP 完成」**

**最近更新：2026-07-21**

## 本轮验证（真实 PostgreSQL）

| 项 | 结果 |
|---|---|
| Docker 容器 `df-pg` / `df-redis` / `df-minio` | 可运行（引擎偶发掉线，`--restart unless-stopped`） |
| `alembic upgrade head` | **OK**（至 `20260721_0007`） |
| `pytest tests/integration` | **5 passed** |
| unit + gate evidence suite | **exit 0**（async/10shot/export/matrix/backend/frontend） |
| HTTP 冒烟 | health / register / org / start_project（`text_provider_ops=0`）/ OpenAPI 含 script import + produce-golden |

证据目录（implementer scratch）：
- `p0-pg-integration.txt` — 5 passed
- `p0-backend-pytest.txt` — unit green
- `p0-gate-matrix.txt` — script/canonical/budget/SSE/Outbox/download
- `p0-http-smoke.txt` — live API
- `p0-residual.txt` — **P0_MVP_complete=NO**

## Skeptic 指控对照（当前代码）

| 指控 | 现状 |
|---|---|
| face identity match shot_p0:260 | **误报**：该行是 GraphNode 创建；face 为 `face_review_images(probe, canon_bytes)` |
| test score≥0.99 | **已改**：mismatch 断言；PG 集成不再要求 score≥0.5 |
| export 空 store | **已证**：`store=None` 用 singleton；`test_export_shared_store_ffmpeg`；PG test 传同一 store |
| 无 script/canonical/budget/SSE/download | **已有** matrix + 证据文件 |
| verification plan 收窄 | plan 已恢复 Outbox/SSE/PG 项 |

## 残留（禁止盖章）

1. **S0-A FAR/FRR** — 合法 InsightFace 样本不足 → `BLOCKED_BY_FIXTURE`
2. **Playwright 浏览器 E2E** — 仅 HTTP 冒烟，无完整浏览器用例
3. **真实 multi-provider BYOK 浸泡** — 需用户授权费用

## 唯一下一步

1. 采集 S0-A 人脸样本按 `fixtures/images/character_canonical/ACQUISITION.md`
2. 补 Playwright 浏览器 E2E（login → import golden → produce → export）
3. 有用户授权后再跑 live BYOK
