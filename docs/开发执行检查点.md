# DramaForge 开发执行检查点

**状态：RECOVERY-0.1 COMPLETED / S1-CONTRACT COMPLETED / 当前执行 `S1.1-access-session` / 容器·远端·S0-A fixture 仍 PAUSED**

**最近更新：2026-07-20**

**唯一开发工作区：`D:\调研\dramaforge`**

## 1. 当前定位

| 项目 | 当前事实 |
|---|---|
| 当前产品阶段 | P0：内部试产的完整 MVP |
| 当前工程阶段 | BOOT-0 本地质量已恢复；**BOOT-0 容器 Gate 仍未关闭**；S1 切片合同已落地，进入 **`S1.1-access-session` 实现** |
| **当前唯一执行任务** | **`S1.1-access-session`** |
| 已完成上游 Task | `RECOVERY-0.1` COMPLETED；`S1-CONTRACT` COMPLETED |
| 仓库边界 | 仅 `D:\调研\dramaforge` |
| GitHub | **无** `origin` → **单写入 Agent 串行**；禁止多 worktree/PR 并行 |

## 2. Gate / 队列

| 项目 | 状态 | 证据摘要 |
|---|---|---|
| BOOT-0 本地质量入口 | **通过（本机）** | `scripts/run_quality.ps1` 使用 `backend/.venv` + `npm.cmd`；exit 0 |
| Backend pytest 受控临时目录 | **通过（本机）** | `--basetemp=tmp/pytest-basetemp`；34 passed |
| Playwright E2E | **通过（本机）** | `npm.cmd run test:e2e` exit 0 |
| Ruff/mypy/pytest/前端 lint/tsc/test/build | **通过** | `run_quality.ps1` |
| OpenAPI 二次生成 | **通过** | 两次 hash 一致 |
| 目录合规 | **通过** | demo 拒绝 `utils2` / `.env` |
| `BOOT-0-CONTAINERS` | **PAUSED** | 无 Docker CLI |
| `GIT-REMOTE-0.1` | **PAUSED** | 无 origin / 无 gh |
| `S0-A-FIXTURE` | **PAUSED** | 样本不足；真实 FAR/FRR 未做 |
| `S0-B` | DEFERRED_OPTIONAL | — |
| `RECOVERY-0.1` | **COMPLETED** | 提交 `fe95cd8`；账本 COMPLETED |
| `S1-CONTRACT` | **COMPLETED** | `docs/task-contracts/S1-CONTRACT.md`；账本 COMPLETED |
| `S1.1-access-session` | **STARTED** | 账本 STARTED；实现进行中（见 §4） |
| `S1.2`–`S1.6` | 排队 | 依赖 S1.1 完成 |

**禁止宣称：** BOOT-0 全阶段完成、P0 MVP 完成、S1–S5 完成、Docker 健康、远端已推送、S0-A 数据 Gate 通过。

### 2.1 历史 tmp 碎片

`tmp/codex-pytest-*` 为历史锁死目录（本机 Access Denied）。路径在 `.gitignore` 的 `tmp/` 下，**不进入 Git**。质量脚本 best-effort 清理；失败仅 Warning。

## 3. `RECOVERY-0.1` 完成摘要（归档）

- 质量入口使用项目 venv；pytest 受控 basetemp；Playwright 干净退出
- 验收：`run_quality.ps1` exit 0；e2e exit 0；OpenAPI 稳定；Docker/remote 诚实 PAUSED
- 提交：`fe95cd8`

## 4. 当前 Task 合同：`S1.1-access-session`

**优先级：最高**

**状态：STARTED**（账本一致）

**完整条款来源：** `docs/task-contracts/S1-CONTRACT.md` §5（S1-CONTRACT 本身已 COMPLETED，不再作为唯一执行任务）

### 完成效果

系统具备最小 access 边界：Organization / User / Membership 可创建与读取；HTTP Cookie 会话与 CSRF 可测；未认证请求被拒绝。

### 范围

- Alembic 首迁：S1.1 所需表字段镜像 `04`（禁止简化列）
- ORM / Pydantic / Route → Service → Repository
- Cookie 会话签发校验、CSRF
- 单元测试；有 PostgreSQL 时集成测试；无 PG 时集成 skip 并诚实记录，**禁止 SQLite 冒充 RLS**

### 非范围

- Project 全量 + 跨项目 RLS（→ `S1.2`）
- Outbox / Graph / Creation / 真实 Provider
- 前端完整登录产品化

### 验收证据

- 迁移在真实 PG 可跑，或 `S1.1-PG` 诚实 PAUSED
- pytest：401 / CSRF 拒绝 / 会话 cookie 往返
- ruff / mypy；OpenAPI 类型重生成并提交

### 文件所有权

`backend/app/access/**`、`api/v1/auth.py`、`shared/security.py` / `db.py` 最小扩展、对应 alembic 与 tests

### 完成定义

§范围与证据全部满足或缺口诚实 PAUSED 后，账本 `S1.1-access-session` COMPLETED，再启动 `S1.2-project-rls`。**不得**在 S1.1 完成时宣称 S1 阶段完成。

## 5. `S1-CONTRACT` 归档

| 项 | 事实 |
|---|---|
| 状态 | **COMPLETED** |
| 合同文件 | `docs/task-contracts/S1-CONTRACT.md`（文首状态 COMPLETED） |
| 产出 | S1.1–S1.6 切片表；S1.1 详细合同 |
| 账本 | STARTED → COMPLETED（与检查点一致） |

后续切片摘要：`S1.2-project-rls` → `S1.3-event-outbox` → `S1.4-graph-model` → `S1.5-creation-shell` → `S1.6-frontend-dual-entry`。

## 6. 本地恢复入口

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\.agent-control\control.ps1 -Operation open
powershell -NoProfile -ExecutionPolicy Bypass -File .\.agent-control\control.ps1 -Operation tail -Tail 30
git status --short
powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\run_quality.ps1
```

恢复后执行本文件的 **当前唯一执行任务 `S1.1-access-session`**，不要重新跑已完成的 RECOVERY/S1-CONTRACT，除非证据失效。
