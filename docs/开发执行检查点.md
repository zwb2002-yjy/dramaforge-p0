# DramaForge 开发执行检查点

**日期 / HEAD：2026-08-26 / MS5-IDENTITY-C 已完成（七方案执行中，准备进入 Phase 1 Workspace Foundation）**

## 今日唯一目标

按 `docs/plans/professional-program-v2/README.md` 的唯一顺序完成 MS5-IDENTITY 三段（A/B/C）：credential immutable revision → ProviderConnectionRevision 冻结 → 完整 secret-free Execution Identity Freeze；随后进入七方案 §4 Phase 1（Workspace State / Scene-Shot 专业字段 / Shot Design API / Project Shell），因为 07 §15 的 Phase 4 Merge Gate 要求 Professional Phase 1/2/3 全部完成。

## 完成证据

- 已新增 ADR：`docs/adr/0006-professional-workbench-reframe.md`，记录“专业工作台取代四阶段向导作为产品主形态”的用户确认；旧四阶段向导降级为兼容入口。
- 已完成 MS1-R + MS1-C：唯一 `ExecutionModelResolver` / `ExecutionModelResolution`，ModelSelectionService 与统一 Professional execution path 消费同一解析结果；Profile X 不可用时 fail-closed，不提交 Provider 请求。
- 已完成 MS2：canonical role vocabulary 与 `UnsupportedInputSlotError`；plural request containers 归一化为 singular role，strict validator 在 Provider submission 前校验未知 slot、cardinality、resolved MIME 与数量一致性。
- 已完成 MS3：LegacyAdapterBridge `translate_v2(list[ResolvedReference])`，3 个同 role reference、混合 role、顺序/fingerprint 传输均有测试。
- 已完成 MS4-LITE：`InputModeSpec` / `CapabilitySpec.modes`，mode_id 贯通 V3 request → intent → SelectionPlan → ExecutionModelResolution → NodeRun / ProviderOperation evidence。
- 已完成 MS5-R：binding-based `resolve_runtime_for_model_binding`，校验 concrete binding/catalog identity；同 Provider 多模型不再取 first seed。
- 已完成 MS5-IDENTITY-A（迁移 `20260826_0041`）：credential 转为 immutable revision（`revision_no`/`supersedes_id`，移除 workspace/provider 单行唯一约束）；`store_credential()` 始终 INSERT 新 revision；`read_credential_by_id` 与 Professional runtime/connection settings 按具体 `credential_id` 严格读取，缺失/跨 workspace fail-closed；key rotation 保持 encryption revision 语义分离。提交：`b5fa93c`、`3e6ba83`、`f399f13`。
- 已完成 MS5-IDENTITY-B（迁移 `20260826_0042`）：新增 immutable `ProviderConnectionRevision`（id/connection_id/revision_no/provider_type/protocol_profile/base_url/credential_revision_id/created_at，唯一 (connection_id, revision_no)、RLS、backfill rev 1）；`ProviderOperation.provider_connection_revision_id` 可空带索引 RESTRICT FK；base_url/credential/protocol 变化创建新 revision，仅 display/enabled 变化不创建；新 Professional operation 首次提交前冻结 revision，retry 保留旧 revision。提交：`595abd9`、`db7f188`、`366f7c3`。
- 已完成 MS5-IDENTITY-C：新增 typed、frozen、JSON-safe、secret-free 的 `ExecutionIdentitySnapshot`（requested/resolved model、resolution source、binding/catalog/model_revision/manifest_hash/invoke、connection/connection_revision/credential_revision、capability、mode_id、effective_options、resolved_references、translation_report、request_fingerprint；`model_copy()` 强制过 validation，evidence key 拒绝 secret/password/api_key/authorization/ciphertext/header/bearer/download_url/grant）；统一路径首次提交前把同一 identity 写入 NodeRun snapshot、ProviderOperation selection_plan 与 request_summary；resume/poll/cancel 通过 `resolve_runtime_for_identity` 按冻结 `ProviderConnectionRevision.id` + `read_credential_by_id` 重建 runtime，不重新提交、不重选模型；missing/mismatched identity fail-closed 且零 Provider 调用；legacy 无 identity 旧 operation 保留兼容路径。提交：`1ef5b80` + 本检查点对应 MS5-C 提交。

## 验证结果

- 后端全量 unit：`717 passed / 1 warning`；MS5-IDENTITY-C focused（execution_identity + unified_path + runtime_model_resolution）：`32 passed / 1 warning`。
- `ruff check app tests alembic/versions`：All checks passed；`mypy app`：Success（185 source files）；`git diff --check`、`compileall`、`scripts/check_directory_compliance.py --root .`：通过。
- 迁移链单 head、offline `alembic upgrade head --sql` 生成/编译通过；MS5-C 范围 head 为 `20260826_0042`（工作树中已含 P1 在途迁移 `20260826_0043`）。
- PostgreSQL 集成：`TEST_PG_ENABLED` 未设置且 `127.0.0.1:5432` 不可达，integration 结果为 `6 passed / 17 skipped`（PG 用例如实 skip，未伪称通过）。
- 真实 Provider 付费调用：未授权，全程零网络假 runtime 验证；`paid_provider_calls=0`。

## 已完成领域闭环（沿用此前专业工作台事实）

- 画布编辑已落到 `CanvasRevision` 持久化合同；AssetVersion、ExperimentBranch、ReviewAnnotation、OpenCut v2、DirectorBoardState 已落库并接入 UI。
- 专业镜头启动/局部重跑显式绕开旧预算 Gate；正式/实验 NodeRun 共享 Production Graph，正式线不消费未采纳实验产物。
- 旧 Director 预算 API/快照/表保留仅为历史回放兼容，专业入口不可达且不展示。

## P0/P1 缺陷与外部证据

- 当前未发现由专业切片引入的类型、构建或既有回归阻断。
- 加密备份已生成；数据库 dump 已验证可在隔离 PostgreSQL 中恢复；MinIO 对象恢复需在真实部署网络/端口条件下复核。
- Provider 真实付费媒体黄金样本、OpenCut 实际应用导入、离线/跨平台和三名目标用户无人代操作仍是外部发布证据，需绑定同一干净 release commit。

## 真实费用与外部调用

专业 live proof 明确 `paid_provider_calls=0`；真实付费媒体黄金样本仍需用户明确允许并绑定同一 release commit。

## 合同或 ADR 变化

新增 ADR 0006；新增迁移 0032–0039、0041–0042（0043 为 Phase 1 在途）；MS5-IDENTITY-A/B/C 合同已按七方案 §9/§14/§15 完成并记录证据。

## 下一唯一目标

进入 Phase 1（Professional Workspace Foundation）：`workspace_state` 持久化与恢复、Scene/Shot 专业字段与迁移、Shot Design API（乐观并发 409）、Professional Project Shell 路由；完成后审计 Phase 2/3 与 07 §15 Gate Test 清单，再进入**单独 Gate 审计**的 Phase 4 Merge Gate（不得自行宣布 merge gate 通过）。

## 当前 Task Contract

`docs/plans/professional-program-v2/task-contracts/MS5-IDENTITY-C-EXECUTION-IDENTITY-FREEZE.md` 已完成并记录证据。下一项 READY 合同为 `P1-PROFESSIONAL-WORKSPACE-FOUNDATION.md`（Phase 1，已建 IN PROGRESS，待 MS5-C 收口后正式 STARTED），继续沿七方案顺序串行推进。