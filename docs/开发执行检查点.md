# DramaForge 开发执行检查点

**状态：软件可关闭 Gate 全绿（unit 95 + PG integration 5 + Playwright 3 + HTTP 冒烟）/ 残留仅 S0-A 样本与真实 BYOK / 不写「P0 MVP 完成」**

**最近更新：2026-07-21**

## 验收证据（implementer scratch）

| 文件 | 结果 |
|---|---|
| `p0-backend-pytest.txt` | **95 passed** |
| `p0-pg-integration.txt` | **5 passed**（真实 PostgreSQL + RLS 路径） |
| `p0-async-keyframe.txt` | enqueue → Worker → shared store |
| `p0-10shot.txt` | 10-shot + lock + mismatch |
| `p0-export.txt` | 哈希相等 + 授权下载 + default store frames |
| `p0-gate-matrix.txt` | script/canonical/budget/SSE/Outbox/single_flight |
| `p0-frontend.txt` | `tsc` + vite build |
| `p0-playwright.txt` | **3 passed**（shell + bootstrap + production UI） |
| `p0-residual.txt` | **P0_MVP_complete=NO** |

## Skeptic 条目对照（HEAD）

| 条目 | 结论 |
|---|---|
| face identity match @ shot_p0:260 | **不成立**：该行建 GraphNode；审核为双源 `face_review_images(probe, canon_bytes)` |
| tests require score≥0.99 | **不成立**：mismatch 断言；PG 无 `score≥0.5` 强制 |
| export 空 store | **不成立**：`get_object_store()` 单例；PG 用 `store=None` 默认路径 |
| matrix 缺项 | **不成立**：script/canonical/budget/SSE/Outbox/download 均有 |
| verification plan 收窄 | **已恢复**：含 PG integration、Outbox/SSE、Playwright |

## 残留（禁止盖「P0 MVP 完成」章）

1. **S0-A FAR/FRR** — 无合法 InsightFace 样本集；`fixtures/images/character_canonical/ACQUISITION.md`；**禁止编造结论** → `BLOCKED_BY_FIXTURE`
2. **真实 multi-provider BYOK 浸泡** — 需用户 Key 与费用授权

## 验证命令（本机复现）

```text
# infra: df-pg / df-redis / df-minio
alembic upgrade head
pytest tests/unit -q          # 95
pytest tests/integration -q   # 5
cd frontend && npx playwright test  # 3
```

## 唯一下一步

1. 按 ACQUISITION.md 采集 ≥20+20+10 人脸样本 → 跑 `scripts/run_s0_face_spike.py`  
2. 用户授权后跑 live BYOK 浸泡  
3. 两项关闭前 **不得** 将产品状态写为 P0 MVP 完成  
