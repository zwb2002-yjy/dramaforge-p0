# DramaForge 开发执行检查点

**状态：纠偏中 / S1 未完成 / S2–S5 产品链路未启动 / P0 MVP Gate 未通过 / 不进入 P1/P2**

**最近更新：2026-07-21（TRUTH-0.1 更正）**

**唯一开发工作区：`D:\调研\dramaforge`**

## 0. 真相更正（TRUTH-0.1）

此前检查点将 `FirstFramePipeline`、`multi_shot`、`export_local` 等 **helper/模拟器** 记为 S2–S5 COMPLETED，并声称「无本地 READY 剩余」。该记录**错误**。

按 `agent.md` 完成定义：

- 薄骨架与 helper 单测 **不等于** 阶段 Gate。
- S2 必须形成 **API → Graph → Outbox → Arq → Adapter → Artifact → Review → SSE** 可重放全链。
- 当前真实状态是：BOOT-0/S1 **部分**基础设施 + 若干 LOCAL-SPIKE；**正式产品链路未形成**；前端为 BOOT-0 路由壳。

历史 `PROGRESS.jsonl` 中的 COMPLETED 行不改写；以本检查点与后续更正记录为准。

## 1. 当前定位

| 项目 | 事实 |
|---|---|
| 当前唯一执行任务 | **`S1-RLS-0.1`**（`TRUTH-0.1` + `S1-DB-0.1` 已完成） |
| 产品阶段 | S1 **INCOMPLETE**；S2–S5 产品 **NOT STARTED** |
| P0 MVP | **未完成、不可宣称完成** |
| P1/P2 | **未开始** |
| 前端 | BOOT-0 壳 + 说明文字；**无**登录/Brief/生产工作流 UI |
| Provider 冒烟 | Agnes 免费 hub 脚本可达（`docs/acceptance/agnes_e2e_real_latest.json`）；**≠** S2 Gate |

## 2. 已落地（有限、诚实）

| 能力 | 状态 | 说明 |
|---|---|---|
| BOOT-0 目录/Compose 合同/质量脚本 | 部分 | Docker Desktop 已装；PG/Redis/MinIO 可起；API 镜像构建曾因内存失败，现宿主机 uvicorn |
| S1.1 Cookie 会话/CSRF/Org/User/Member | 部分 | API 与单测存在；非完整 Gate |
| S1.2 Project/成员/偏好 | 部分 | 有表与 API；`create_project` 中途 commit 破坏 UoW |
| S1 EventLog/Outbox/死信（进程内 publisher） | 部分 | **非** Redis Streams 生产路径 |
| S1 production_graphs / graph_versions 迁移 | 部分 | 字段迁移 0003；**无** graph_nodes/node_runs/… 迁移 |
| RLS | **未落地** | 仅有 `current_user_id()` 等函数，无 POLICY / SET LOCAL |
| Agnes Adapter 脚本冒烟 | SPIKE | 证明 hub 可调用，非产品运行时 |

## 3. LOCAL-SPIKE / LOCAL-ALGO（明确 **非** 产品 Gate）

| 模块 | 原错误标签 | 正确标签 | 原因 |
|---|---|---|---|
| `execution/pipeline.py` FirstFramePipeline | 曾标 S2 COMPLETED | **LEGACY_SPIKE** | 请求线程直调 Provider；假 Brief/AgentRun；URL 当 object_key；自比人脸 |
| `execution/multi_shot.py` | 曾标 S4 COMPLETED | **LOCAL-ALGO** | 仅 5 节点；1 字节 Artifact；硬标 `review_passed` |
| `execution/runtime_invariants.py` | 曾标 S3 COMPLETED | **LOCAL-ALGO** | cache/stale 算法试探，非完整运行时 |
| `delivery/export_local.py` | 曾标 S5 COMPLETED | **LEGACY_SPIKE** | `mp4_placeholder_key`；无 FFmpeg/Export 持久化 |
| 进程内 Outbox list / SSE deque | 曾作 S1 完成证据 | **TEST-DOUBLE** | 单测可用；非多进程/重启恢复合同 |
| Agnes E2E 脚本 | 曾暗示解锁 S2 | **PROVIDER-SMOKE** | 无 API/Worker/SSE 产品链 |

上述文件可保留作算法/冒烟，**禁止**作为 S2–S5 COMPLETED 证据。

## 4. READY 队列（本地可推进，依赖 Docker PG/Redis/MinIO）

按纠偏计划严格串行：

| 顺序 | Task ID | 完成效果（可观察） |
|---|---|---|
| 0 | `TRUTH-0.1` | **COMPLETED** — 检查点与 LEGACY 标注一致 |
| 1 | `S1-DB-0.1` | **COMPLETED** — 迁移 `20260721_0004`；真 PG `upgrade head`；integration 2 passed |
| 2 | `S1-RLS-0.1` | 逐表 RLS + 请求/Worker SET LOCAL；跨项目拒绝 |
| 3 | `S1-CREATE-0.1` | start_project 单事务 + 初始 Brief Revision；零文本 Provider |
| 4 | `S1-OUTBOX-REDIS-0.1` | Outbox → Redis Streams；PG 行锁 claim |
| 5 | `S1-SSE-0.1` | SSE 从 PG 快照/`Last-Event-ID` 恢复 |
| 6 | `S2-RUNTIME-0.1` | Scheduler + Arq job 执行 NodeRun；MinIO 真对象 |
| 7 | `S2-API-0.1` | 手工 Brief/Plan/confirm/materialize/snapshot API |
| 8 | `S2-UI-0.1` | 登录→建项→确认→状态/Artifact（非 PPT） |
| 9 | `S2-FAKE-E2E-0.1` | 全链 fake Adapter + Playwright |

**可选（用户授权）：** `S2-AGNES-EVIDENCE-0.1`

**后置 / PAUSED：** S0-A fixture、S0-B、S3 全量、S4 九节点审核、S5 FFmpeg 交付、P0-E2E-18、Git remote、容器内 API 镜像（内存允许时）

## 5. 已知合同缺口（阻塞产品）

1. ~~ORM/迁移脱节~~ → **S1-DB-0.1 已补** `graph_nodes/edges`、`node_runs`、`artifacts`、`provider_operations`、briefs/plans/agent_runs 等（迁移 0004）。**RLS 仍无。**
2. **无异步运行时**：`scheduler.dispatch_pending → 0`；Worker 仅 `health_ping`。
3. **无主业务 API**：无 Brief/Plan/Shot/资产/Run/审核/导出路由。
4. **start_project** 无初始 Brief；UoW 不完整。
5. **前端** 无业务 feature 实现。

## 6. 明确未做的宣称

- 未宣称 S1 阶段 Gate 通过
- 未宣称 S2–S5 产品 Gate 通过
- 未宣称 P0 MVP 完成
- 未打开 P1/P2
- **撤回**「无本地 READY 剩余」

## 7. 提交索引（历史，不代表 Gate）

- `98a4773` S1.1 session/CSRF/org
- `1b4e096` S1.2 projects
- `8f60d3b` S1.3–S1.5 shells
- `287933c` residual + local spikes
- `f2f825e` multi-shot algo
- `d1bffa4` Agnes provider smoke

## 8. 恢复

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\.agent-control\control.ps1 -Operation open
powershell -NoProfile -ExecutionPolicy Bypass -File .\.agent-control\control.ps1 -Operation tail -Tail 20
docker compose up -d postgres redis minio
cd backend; .\.venv\Scripts\python.exe -m alembic upgrade head
.\.venv\Scripts\python.exe -m uvicorn app.main:app --host 127.0.0.1 --port 8000
```
