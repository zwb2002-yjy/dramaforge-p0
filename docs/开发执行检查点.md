# DramaForge 开发执行检查点

**状态：P0 功能候选版 · 非 Docker 正式栈已接通 · 禁止宣称完整 P0 MVP（Docker/S5 延后）**

**最近更新：2026-07-21（Goal：非 Docker 全栈 + 真 Worker 链路 + 无静默假成功 + 诚实 Gate）**

## 0. 诚实基线

| 事实 | 说明 |
|---|---|
| 产品标签 | **P0 功能候选版**。Docker Compose / S5 部署 Gate 未完成 → **不是**「完整 P0 MVP」。 |
| 正式本地栈 | `scripts/start_p0_wsl_stack.sh`：WSL 内 PG+Redis+MinIO+API+default/heavy Arq+Outbox dispatcher；**不**设置 `DRAMA_FORCE_MEMORY_STORE=1`。Windows 只跑前端。 |
| 假 Adapter | 仅 `APP_ENV=test` / pytest；正式路径 fail-closed `PROVIDER_NOT_CONFIGURED`。 |
| S0-A InsightFace | 已接入可选 InsightFace 路径；本机模型下载不完整时 `backend=hash_placeholder`，Gate **BLOCKED**（非零退出），不得当验收。 |

## 1. 本轮完成对照（Goal 1–8）

| # | 项 | 状态 |
|---|---|---|
| 1 | 非 Docker 正式栈 | **DONE**（脚本+实机 PG/Redis/MinIO/API/Workers/dispatcher 存活；无 FORCE_MEMORY） |
| 2 | 10 Shot Worker 链路 | **PARTIAL→代码完成**：`shot_p0` 默认 test 内联、正式 `execute_inline=False` 仅排队；`run_p0_full_product` 不再切 `APP_ENV=test` |
| 3 | 禁止静默假成功 | **DONE**：flux/kling fail-closed；import 不再 Fake canonical；lead 超时/错误可控 |
| 4 | 逐 Shot 审核返工 | **DONE（API+UI 绑定）**：`shot_ops` start/approve/reject/lock/rerun/manual-media + 生产板按钮 |
| 5 | 正式交付 | **PARTIAL**：按 approved GraphVersion 过滤；zip 含媒体字节；SRT 2s 粒度；需真视频后 MP4 才完整 |
| 6 | 队列/Outbox | **DONE**：无 `local:*` 成功语义；lease reclaim + SKIP LOCKED claim；稳定 job id |
| 7 | 一致性 Gate | **PARTIAL**：InsightFace 入口已接；样本/完整模型下载失败时诚实 BLOCKED |
| 8 | Gate 数学 | **DONE**：`p0_mvp_complete` 由结果计算；FAIL/BLOCKED → exit 2；health 要求 `db=up` |

## 2. 启动（用户）

```powershell
# WSL 正式栈（推荐）
wsl -d Ubuntu-24.04 -- bash /mnt/d/调研/dramaforge/scripts/start_p0_wsl_stack.sh start

# 或
powershell -ExecutionPolicy Bypass -File scripts\start_p0_stack.ps1

# Windows 仅前端
cd frontend; npm run dev -- --host 127.0.0.1 --port 5173
```

健康：`http://127.0.0.1:8010/health` → `status=ok` 且 `db=up`。

## 3. 口径冲突（保留）

- `01` / 产品路线图仍写 Docker Compose 与 S5 部署回归。
- 本阶段 **P0-DOCKER-DEPLOY = DEFERRED**；功能上 Redis/MinIO/Worker **不**因 Docker 延后而延后。
- 因此只可称 **P0 功能候选版**。

## 4. 当前唯一执行任务

```text
Task ID：P0-REAL-10SHOT-LIVE
完成效果：在正式栈上对导入的 10 Shot 跑通 enqueue→Worker→Artifact→审核通过→导出 zip/SRT
（需 BYOK 可用或受审计手工媒体；Gate 仍可能因 S0-A/S5 保持非零）
```
