# DramaForge 开发执行检查点

**状态：产品路径（S1–S5 竖切）已实现并经 73 项测试 / 完整 `01`§3.1 18 条未全部签核 / 不进入 P1/P2**

**最近更新：2026-07-21（skeptic 修复：Worker 分离、真审核、Export 持久化、RLS 断言）**

**提交：`dcda7d2` 及后续**

## 1. 当前定位

| 项目 | 事实 |
|---|---|
| 产品路径 | **可用**：Brief/Plan → enqueue → Worker → Artifact → Export |
| P0 MVP 标签 | **不写「P0 MVP 完成」**直至 §4 残留关闭 |
| 测试 | unit+integration **73 passed**（真 PG） |

## 2. Skeptic 修复要点

| 问题 | 修复 |
|---|---|
| API 请求线程跑 Adapter | 删除 `/execute` 执行路径；仅 `/enqueue` + Worker `/worker/tick` 或 Arq |
| face/continuity 硬编码 | `face_review_hook` 余弦分；`continuity_check` 规则 |
| 10-shot 纯 stub | keyframe 走 `WorkerRuntime` + product_path |
| 内存 lock | `shot_human_locks` 表 |
| Export 无持久化 | `exports`/`export_items` 迁移 0006 + 回链 |
| RLS `assert or True` | 断言 own 可见、other 不可见 |

## 3. 残留（阻止「P0 MVP 完成」标签）

1. S0-A 人脸样本 FAR/FRR  
2. 剧本导入完整 UI + 资产 canonical Reference 拒绝路径的长稳 E2E  
3. Playwright 浏览器全链路  
4. 持续 Docker 多 Worker 压测  
5. 真实 Azure TTS / 全节点真实 Provider（现 keyframe Worker + fake 其余）  

## 4. 验证命令

```powershell
docker compose up -d postgres redis minio
cd backend
.\.venv\Scripts\python.exe -m alembic upgrade head
$env:DATABASE_URL="postgresql+asyncpg://dramaforge:dramaforge@127.0.0.1:5432/dramaforge"
$env:PYTHONPATH=(Get-Location).Path
.\.venv\Scripts\python.exe -m pytest tests/unit tests/integration -q
```
