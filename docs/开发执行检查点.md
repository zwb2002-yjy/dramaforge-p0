# DramaForge 开发执行检查点

**状态：软件可关闭 Gate 全绿 / S0-A = BLOCKED_BY_FIXTURE / 真实 BYOK 未授权 / 明确不写「P0 MVP 完成」**

**最近更新：2026-07-21**

## 验收证据（`{SCRATCH}` implementer）

| 步骤 | 文件 | 结果 |
|---|---|---|
| unit | p0-backend-pytest.txt | **95 passed** |
| PG integration | p0-pg-integration.txt | **5 passed** |
| async keyframe | p0-async-keyframe.txt | **pass** |
| 10-shot | p0-10shot.txt | **pass** |
| export | p0-export.txt | **pass** |
| gate matrix | p0-gate-matrix.txt | **pass**（含 script/canonical/budget/SSE/Outbox/download） |
| frontend | p0-frontend.txt | **pass** |
| playwright | p0-playwright.txt | 见文件 |
| S0-A spike | p0-s0a-spike.txt | **BLOCKED_BY_FIXTURE**（0 样本，exit 2） |
| skeptic 对照 | p0-skeptic-disproof.txt | 旧行号指控与 HEAD 不符 |

## Skeptic 条目 vs HEAD（反驳）

1. **`shot_p0.py:260` 不是人脸自比对** — 该行是 `GraphNode(...)`；人脸在 L310+：`face_review_images(probe, canon_bytes)`。  
2. **测试无 `score≥0.99` 强制** — 现有 mismatch 断言。  
3. **export 非空 store** — `export_service` 使用 `store or get_object_store()`；PG 用例 `store=None` 走单例。  
4. **matrix 已覆盖** script import、canonical reject、budget、SSE、Outbox、授权下载。  
5. **verification plan 已恢复** PG / Outbox / SSE / Playwright。

## 残留（禁止盖章）

| 残留 | 状态 | 解除条件 |
|---|---|---|
| S0-A FAR/FRR | `BLOCKED_BY_FIXTURE` | 按 ACQUISITION.md 放入 ≥20+20+10 样本后重跑 spike |
| Live multi-provider BYOK | 需用户授权 | 用户提供 Key 与费用确认 |

产品路径人脸使用内容派生 512-d 双源比对（fake Adapter 可测）；**InsightFace 阈值校准 Gate 未通过**。

## 唯一下一步

用户提供 S0-A 合法样本目录（或授权采集）→ 重跑 `scripts/run_s0_face_spike.py` → 若状态离开 `BLOCKED_BY_FIXTURE` 再评估 InsightFace 接入与 P0 盖章。
