# DramaForge 开发执行检查点

**状态：LIVE / P0 功能候选版（发布验收未关闭）**

**最近更新：2026-08-12**

产品总路线、目标用户、壁垒和阶段 Gate 见 [`../DramaForge总开发文档.md`](../DramaForge总开发文档.md)。
文档职责见 [`README.md`](README.md)。2026-08-01 的完整审计、能力矩阵和需求冲突保留在
[`项目现状与需求对齐-20260801.md`](项目现状与需求对齐-20260801.md)，仅作历史快照。

## 0. 诚实基线

| 事实 | 说明 |
|---|---|
| 产品标签 | **P0 功能候选版**。当前代码基线为 `dev@9e79ba4`。`41def2f` 的 Compose、黄金样本和交付证据已早于 Provider A+B、Model Plugin V3、Production Model Profile 和 LiteLLM Proxy 迁移，只能作历史对照。不得声明完整 P0 MVP。 |
| 产品方向 | 首批目标用户是假设中的高频多模型独立 AI 短剧主创或小型工作室单一制作操作者。P1 默认路线为候选治理、个人故事板/返工/资产和受控精剪；只有通过设计伙伴验证后才进入 P1。成员、邀请、共享、评论与指派未排期。 |
| 正式本地栈 | `docker compose up -d`：Docker PostgreSQL、Redis、MinIO、迁移、API、Outbox dispatcher 与 Arq；**不**设 `DRAMA_FORCE_MEMORY_STORE=1`。 |
| 当前运行栈 | 尚未针对 `9e79ba4` 重新构建和核验 `/health.source_commit`。此前 `211ecaa` 与 `41def2f` 的运行栈均为历史开发证据，只能用于诊断。 |
| Docker Compose / S5 私有化部署演练 | Docker PostgreSQL、Redis、MinIO、迁移、API、dispatcher、default/heavy Arq Worker 和 LiteLLM Proxy 的定义均在当前 Compose 中；完整媒体交付、备份恢复、BYOK 轮换及故障演练尚未形成 `9e79ba4` 的同源报告。 |
| 假 Adapter | 仅 `APP_ENV=test` unit；正式路径 fail-closed 或受审计手工媒体。 |
| InsightFace | 2026-07-30 已重新构建 `api`、`migrate`、`dispatcher`、`worker-default` 与 `worker-heavy` 镜像。镜像内 `FaceAnalysis(name="buffalo_l", providers=["CPUExecutionProvider"]).prepare(ctx_id=-1)` 通过，应用状态为 `available=true`、`backend=insightface+onnx`、`embedding_dim=512`；见 `docs/acceptance/insightface_status_latest.json`。20/20/10 S0-A 校准与最终阈值 `0.60` 仍以 `docs/spikes/s0a-face-consistency.md` 为准。 |
| Graph 与人脸 | GraphEdge 已持久化，Worker 已按上游 Artifact/Review 强制顺序；历史干净 10 Shot 已证明 90 Node/80 Edge 和静态构图样本 10/10 face 首轮通过。旧 `graph_edges=0` 与顺序缺陷仅保留在历史审计段落。当前迁移后的提交仍需重验。 |
| Video Drift 与审核 | `mean >= 0.40` 保持已批准；分布证据已增强。`blocked/needs_human` 均阻断下游和 approve，不提供人工覆盖 API，只能通过新的 video attempt 解决。 |
| 最新 10 Shot | `41def2f` 的静态构图黄金样本后来已达到 10/10 face、10/10 drift、10/10 全链和四项交付，但该结果早于当前 Provider/模型配置迁移。ADR-0004 同时记录：动态镜头可能在末帧变脸，现有 `mean >= 0.40` 未充分拦截这类错误。 |
| 质量验证 | 最新实现报告记录：backend unit `537 passed`、PG + real LiteLLM Proxy integration `20 passed`、ruff/mypy 通过；此前的 `281/12/15` 结果为历史基线。上述均为开发质量证据，必须在当前干净候选上复跑，且不替代正式 P0 Gate。 |
| 当前正式 Gate | `a8feda0` 的 `25 PASS / 0 FAIL / 0 BLOCKED` 与 `41def2f` 的黄金样本均为历史证据。当前 `9e79ba4` 没有同源 formal、真实 Playwright、Section 3.1 Gate 和 ops 报告。 |
| 下一阶段 | **不进入 P1 功能开发**。先完成当前源码 P0 重验、人物一致性扩展评估和设计伙伴验证；P1.1 合同只可准备，禁止创建迁移、API、隐藏路由或 UI。 |

## 1. 历史验收证据

以下记录仅说明 2026-07-21 的运行结果，不代表当前 `main` 已通过：

| 证据 | 结果 |
|---|---|
| `GET /health` | `status=ok` `db=up` |
| `scripts/prove_p0_mvp_formal.py --n 10` | **ok=true**：10 Shot 全必需节点、approve_ok=10、failed=0、package.zip 哈希一致、真实 MP4 |
| `scripts/run_p0_section31_gate.py --evidence multi_shot_chain.json` | **pass=23 fail=0 blocked=0 `p0_mvp_complete=true`** |
| 后端 integrity unit | 16 passed |
| 前端 typecheck / vitest / build | 全绿 |
| InsightFace | `docs/acceptance/insightface_status_latest.json`：2026-07-30 Compose 构建期与容器运行期均为 `backend=insightface+onnx`、`embedding_dim=512` |

产物哈希（formal proof）：见 `docs/acceptance/multi_shot_chain_latest.json` 与 SCRATCH `export_hashes.txt`。

## 2. 本轮关键实现

- commit-then-enqueue；假 queued 禁止
- APPROVE_GATE / EXPORT_GATE / 角色门禁 / package.zip 元数据
- 手工媒体完成 NodeRun（零 Provider 成本）
- pure review 节点无需 Provider
- Artifact 同 hash 复用（uq 冲突）
- §3.1 gate：formal evidence + unit 证明 + InsightFace 状态文件；当前镜像 smoke 已更新，但正式 Gate 仍须按候选提交重跑
- FFmpeg 真实 MP4 导出

## 3. 口径

- **当前只可标记：P0 功能候选版**。
- 正式报告必须绑定精确源码提交且工作树干净；任意 `FAIL` 或 `BLOCKED` 都不能声明 P0 MVP 完成。
- Docker/S5 运维演练继续延期；Docker Compose 正式栈是发布前 Gate。

## 3.1 历史 P0 Gate 状态（2026-08-01）

最近一次干净、绑定源码的正式结果来自 `56db2e4`：

- `21 PASS / 1 FAIL / 2 BLOCKED`，`p0_mvp_complete=false`。
- API、账号/Workspace、Project、Agent Brief/Plan、10 Shot 物化、Canonical、
  Outbox 调度及定向 unit Gate 通过。
- 正式媒体链路等待 Provider 时超时，10 Shot x 9 节点和审核交付被阻断。
- 人脸策略未形成可接受的真实评分证据。
- 当时 `HEAD=6fbe119` 比该证据多一个主角图片条件化修复提交，尚未重跑完整
  formal proof；此段只解释 2026-08-01 的判断。

证据：
`tmp/p0-evidence/56db2e4926a9a77ade274f2497bb3e4db8f1a0a9/`。

## 3.2 历史深度审计（2026-07-31 按 `4792f29` 实测）

### 已在该历史提交上验证通过

在 Docker Compose 正式栈、真实 Playwright 浏览器、项目
`af92411e-64d2-4637-8e6e-489691d12f04` 上实测：

1. 真实 Agent 链路：`agent_runs` 有 `draft_brief` 与 `draft_plan` 各 1 条且
   `succeeded`，对应 `provider_operations` 为 `openai/text-llm` 的
   `text.brief.generate` 与 `text.plan.generate`。
2. 真实媒体 Provider：`flux/agnes-image-2.1-flash` 关键帧 10 次、
   `kling/agnes-video-v2.0` 视频 10 次、`local_tts/espeak-ng` 语音 10 次，全部
   `succeeded`，共 32 条 ProviderOperation 无失败。
3. 90 个 NodeRun 血缘：九类节点各 10 个 Run、10 个独立 Artifact、10 个独立
   object key，`failed=0`，每个 Artifact 的 `produced_by_run_id` 回指其 Run，
   `output_summary.source_commit` 全部绑定 `4792f29`。
4. 10 个 Shot 均为 `review_passed`；导出 `9739f793` 状态 `completed`，
   `export_items` 恰好 90 条且引用 90 个不同源 Artifact。
5. 交付哈希：package `12e6d977…`、MP4 `72f4d79a…`、SRT `6be0b551…`、
   timeline `a721bef7…` 均在 manifest 中留存；经浏览器 download-grant 下载
   ZIP 共 39,503,931 字节，现场 SHA-256 与记录一致。
6. 前端可用性：登录、生产台、分镜、交付页面均正常渲染，无 page error。

### 仍未关闭的阻塞项

7. **人脸一致性 Gate 未被证明（新发现，最严重）**。模型与像素都没问题：
   InsightFace 可用（`backend=insightface+onnx`、512 维），5 张 canonical 全部检出
   1 张人脸，10 张生成关键帧中 7 张检出人脸——**其中包含全部 5 个
   `lead_identity_required=true` 分镜的关键帧**。
   但 **10 个 `face_review` 的 `face_score` 全为 `null`**（5 个
   `not_applicable` + 5 个 `needs_human`），无一产生真实分数。运行期根因是第 8 项的
   顺序缺陷：`face_review` 早于上游 `keyframe` 完成即启动，
   `_complete_pure_node()` 取不到 probe 字节，直接落到 `needs_human`。
   `assert_shot_approvable()` 只拦截 `{blocked, fail, failed, reject}`，
   `needs_human` 可直接通过审核门禁。
   离线用正确的「本分镜关键帧 vs 其 canonical」重放（阈值 0.35），5 个主角分镜得分为
   `+0.3843 passed`、`+0.3709 passed`、`+0.3247 blocked`、`+0.2804 blocked`、
   `+0.2364 blocked`——**5 个中有 3 个应被拦截**，而它们当前均为 `review_passed`。
   即：单独修复顺序缺陷会让当前黄金样本**不通过**，两项必须一起处理。
8. **Production Graph 依赖顺序未强制执行（新发现）**。`SHOT_PIPELINE_EDGES` 声明了
   `keyframe -> face_review -> video`，但 `graph_edges` 全库 **0 行**——
   `GraphService.create_graph()` 只把 edges 写进 `graph_versions.definition` JSON。
   `produce_shots_p0()` 一次性把九个 NodeRun 全部入队，Worker
   （`execute_node_run`）只检查 `composite_inputs_pending()`，即仅 `composite`
   等待上游。实测 **10/10 个 Shot 的 `face_review` 都早于其上游 `keyframe` 完成时间启动**
   （平均提前约 30 秒），因此 face_review 读到的并非本 Shot 的关键帧。这是第 7 项
   之外另一个独立的失效原因。
9. **已盖章阈值未进入运行期（新发现）**。`docs/spikes/s0a-face-consistency.md`
   的 `final_threshold: 0.60`（`approval_id=USER-APPROVED-2026-07-25-P0-S0A`）在代码中
   **完全不存在**：`face_review.py:30/47`、`product_path.py:243/267`、
   `shot_p0.py:210/366`、`pipeline.py:248`、`characters.py:35` 全部硬编码 `0.35`；
   仅 `face.py:133` 的校准网格里出现过 0.60。按已批阈值 0.60 重算，上述 5 个主角分镜
   **无一通过**。阈值应从单一来源下发，不应散落字面量。
10. `/api/v1/projects/{id}/snapshot` 不返回 NodeRun 的 `started_at`/`finished_at`，
   前端无法自证执行顺序，顺序结论只能来自数据库。
11. `character_references.embedding_model_version` 记为 `content-hash-v1`，
   未反映实际使用的 InsightFace 版本。
12. 备份恢复、密钥轮换仍未在当前提交的正式栈上留存端到端记录。

**结论：交付链路（A 类）在当前提交上成立；人脸一致性与图依赖顺序（B/C 类）
未通过。不得声明 P0 MVP 完成。**

现场验收证据：`tmp/p0-evidence/4792f295e2e525d58662637f48b7e13dce3ec101/acceptance-v2/`
（含 `acceptance_v2.json` 与 5 张浏览器截图）。

## 3.3 2026-08-04 干净 10 Shot 全链状态

前置缺口（3.2 的第 7/8/9 项与 §22.5 的样本构图、poll-429、Drift 批准）已逐一关闭：

- **样本构图根因已修复**（`eb35fdb`）：canonical 实验确认 5 个失败 shot 是构图不以主角
  脸为主体，重写文案后干净 10 Shot **10/10 face 首轮 passed（0.68-0.92）**。
- **poll 429/5xx 瞬时处理已修**（`9d49f67`）：视频 poll 不再被瞬时限流终态失败，保留
  同任务续查；unit 277 + PG integration 12 全绿。
- **Video Drift 已批准**（`bee7ace`）：用户批准 `mean >= 0.40`
  （policy `P0-VIDEO-DRIFT-2026-08-04`），真实 6 视频分布支撑；干净 10 Shot
  **9/10 drift passed**。
- **干净 10 Shot 全链**（栈绑定 `211ecaa`，项目 `20cbccec`）：10/10 keyframe、
  10/10 video、**9/10 全链绿**（video->drift->composite->continuity）。
- **已知人工复核项**：shot 6（暗光手机特写）两次独立 video 均 drift 0.31 < 0.40，
  按 plan §12.3 记人工复核，不自动交付。

证据：`tmp/provider-probe/frozen-sample6-clean.json`（face 10/10、video 10/10）、
`tmp/provider-probe/drift-rerun-1.json`（shot 4 返工清除、shot 6 仍阻断）、
`tmp/drift_samples.txt`（6 条真实 drift 分布）。仍不得声明 P0 MVP 完成——正式
Gate/导出、shot 6 人审、人工验收与运维签字未做。

## 3.4 2026-08-04 最新 Gate 加固

在 `211ecaa` 的 10 Shot 运行之后，代码继续完成四项 fail-closed 加固：

- `10f6865`：approve Gate 显式校验 `video_drift_review`；`blocked`、`needs_human`
  和非法 `not_applicable` 均不能批准。
- `37583e9`：保证 scored/unscorable 数量一致，并输出最小/最大分数与过阈值帧数；
  决策规则仍为已批准的 `mean >= 0.40`。
- `9f5049a`：Worker 崩溃时保留瞬时 poll 错误证据，避免丢失故障归因。
- `8299db6`：局部 Drift 重跑报告按节点输出最新 attempt，避免旧失败遮蔽新结果。

这些修复都有定向测试，但它们改变了发布候选源码。`a8feda0` 的历史全绿 Gate 和
`211ecaa` 的 10 Shot 运行都不能替代 `8299db6` 之后的新正式验收。

## 3.5 2026-08-05 前端中文化 + 收口推进

- **前端全量中文化**（`41def2f`）：登录/工作台/快速与专业生产板/Provider 面板的
  标签、按钮、状态、错误码与结构化错误摘要全部中文化；`frontend/src/lib/zh.ts`
  统一映射（节点名、status、error_code、上游/审核错误整句翻译）。API 契约值
  （error_code/status/node_key/capability）保持不变，仅显示层映射。e2e
  （p0_bootstrap / p0_real_ten_shot）断言同步到新中文文案，smoke 全绿。
- **Compose 已重建**绑定候选 `41def2f`（API `/health.source_commit` 与 Worker 同源）。
- **ADR-0003 已批准**（2026-08-05）：主黄金样本 = 真实文本/图像/视频 Provider +
  `local_tts` + 受控本地执行器；费用逐轮授权。
- **测试项目诊断**（`New short drama` / `creator@example.com`，项目 `2b85ff30`）：
  「制作报错、不能生成最终视频」根因 = **剧本《林知夏》8/10 镜头不含主角正脸**
  （特写手/背身/远景/配角）→ 0.60 face gate 阻断 → video 全部 UPSTREAM_TERMINAL_FAILURE。
  改构图重跑一轮后仅 shot 4/10 全链通过（drift 0.441/0.557），其余 face 仍低于 0.60
  （-0.09~0.58 波动），shot 1 柔光后反降 0.626→0.517 —— 属真实 Agnes 生成方差 +
  剧本镜头设计问题，**非代码缺陷**（gate 按设计 fail-closed）。已按产品负责人决定
  停止该测试项目投入，转回 P0 黄金样本收口。
- **黄金样本 shot 6 已解决**（2026-08-05）：shot 6（原「暗光手机屏特写」）三次 video
  drift 稳定 0.31-0.34（<0.40）。按产品负责人决定改内容为「正面近景 + 均匀亮光 +
  手机举在胸前」后重跑：**face 0.923、drift 0.726、composite/continuity 全过**。
  至此**黄金样本 10/10 face / 10/10 drift / 10/10 全链通过**。
- **黄金样本交付已产出并校验**：10/10 shot 全部 `review_passed`；导出
  `33822ca9`（export_item_count=100），四项交付哈希全部与导出响应一致：
  `program.mp4=a1f17011…`（16.7MB 真实 MP4）、`package.zip=303454c1…`（35.7MB、
  含 24 个分镜 MP4）、`subtitles.srt=6be0b551…`、`timeline.json=697292f3…`。
- **运维演练（2026-08-05，免费项）**：
  - 备份恢复 ✅：`pg_dump -Fc` 全库备份（2.9MB）→ `pg_restore` 到临时库，关键表行数
    与原库完全一致（node_runs 2828 / artifacts 2227 / exports 7 / shots 320），临时库已清理。
  - BYOK 加密存储 ✅：`encrypted_provider_credentials` 凭证为密文存储（无明文泄露）、
    密钥版本追踪；API 只写入、不回读。
  - Outbox/死信、取消竞态、SSE 恢复、冷存储失败：已由单元/集成测试覆盖
    （`OUTBOX_PUBLISH_FAILED` fail-closed、`completed_after_cancel` 竞态、`Last-Event-ID`
    续传、Artifact 缺失 fail-closed），正式栈实机演练待 Agnes 恢复后与同源 Gate 一起做。
- **同源 formal proof 推进（2026-08-05）**：修复 3 个 proof 脚本真实缺陷
  （`281c7f2` face 重工顺序、`f4f8ed7` face 重工 attempt 错配、`dcc5a60` drift 重工、
  `c0941d5` 客户端超时 600→900s）。face-first + 柔和光线 idea 下 10/10 face 首轮通过
  （0.644-0.743）；proof 曾推进到 approve 阶段，仅剩 shot 6 drift blocked（0.161，prompt
  为高对比戏剧性打光，drift 重工已加入）。**当前阻塞：Agnes 图像生成降级至 15+ 分钟/张**
  （§3.2 目标 ≤2 分钟），同步 canonical API 请求超 900s，proof 无法可靠完成。累计真实
  调用：Agnes 关键帧 44 + 视频 18 + canonical 6，flux 关键帧 9（历史）。已暂停继续重试。
- **Agnes 恢复 + video 间歇故障（2026-08-05 晚）**：canonical 图像延迟降至 21s（恢复），
  但 **video.create 间歇性返回 `PROVIDER_SUBMISSION_UNKNOWN`**（近 2 小时：20 keyframe
  succeeded + 8 video succeeded + 4 video unknown_submission + 2 canonical succeeded，
  视频失败率约 30%，单次 4-25s 快速失败）。proof 对提交未知按 fail-closed 硬失败（正确，
  防重复计费），间歇抖动下完整 proof 连续两轮在 video 阶段中止。已停止付费重试，等
  Agnes video 接口稳定。图像接口正常、视频接口抖动是 Provider 外部故障，非代码缺陷。
- **官方合同修复 Video 首帧传输（`2082ee5`，2026-08-05）**：核验
  `wiki.agnes-ai.cn/zh-Hans/docs/agnes-video-v20.md` + 厂商支持答复确认——
  **Video V2.0 的 `image` 接受公网 URL 或裸 Base64 本体，不接受 `data:...;base64,` 前缀**
  （413 错误码文档亦建议用 URL 而非大 base64）。原 Adapter 用带前缀 Data URI（`0dc23a1`），
  导致视频 create 间歇失败而图像（官方示例即带前缀 data URI）稳定。已改为裸 Base64
  （`reference_transport=base64_raw`）并补 9:16 画幅（height 1280 / width 720，官方默认
  为 1152×768 横屏）。**修复后视频提交全量成功**：官方合同首轮 9/9 video 被接受
  （0 unknown_submission，此前 data_uri 约 30%）。黄金样本 10/10 视频为 data_uri 且成功，
  说明前缀多数时候可用但非官方合同、间歇失败。
- **正式 proof 仍被 Agnes 网络层秒级断连阻塞（2026-08-05 深夜）**：官方合同修复后
  完整 proof 第 2 轮 9/9 视频提交成功、9/10 face 首轮 passed，但 rework 阶段 keyframe
  遇 `WORKER_ERROR: Server disconnected`（图像端点断连）。多轮 proof 均被不同节点上的
  秒级网络断连打断（video create 偶发 + keyframe 偶发），每轮全量付费。已停止自动重试，
  等 Agnes 网络稳定时段。
- **P0 收口两个真实质量阻塞（2026-08-05，用户已决定暂缓）**：
  - **视频末帧漂移**：实测今日 Agnes 视频首帧 vs keyframe 0.909（首帧=keyframe 无误），
    但逐帧漂移，末帧 vs keyframe 仅 **0.109**（人物跑成别人）。drift 门禁用 mean≥0.40
    掩盖（start/mid/end 采样 0.779/0.577/0.112 → mean≈0.49 通过）。需 drift 门禁改动
    （加最低帧门禁或 mean+min 联合）才能拦住；产品负责人 2026-08-05 决定「先不做」。
  - **TTS 音质**：local_tts（eSpeak NG）机械；ADR-0003 为「零成本+私有部署」选型，非
    音质路线。产品负责人决定改开源 TTS，但「先不做」，未定选型（Piper/MeloTTS 候选）。
  - 这两个阻塞决定 formal proof 的 `ok:true`：不拦漂移则「gate 全绿但视觉不达标」，
    重跑无意义。故 P0 真实停留在「功能候选版」。
- **剩余运维演练实机可行性（2026-08-05 核实）**：Outbox 死信、SSE 续传、取消竞态、
  冷存储失败均已由单元/集成测试覆盖（`test_outbox_sse`/`test_events_sse`/取消竞态/
  Artifact 缺失 fail-closed，43 项相关测试通过）。实机演练可行性：SSE hub 当前**无生产
  调用者**（`publish` 仅测试喂入），无真实事件流；Outbox 死信与冷存储实机演练需停
  Redis/MinIO，会中断用户正在使用的运行栈，故不在活跃会话中执行。此三项以测试覆盖为
  当前证据。

## 4. 启动

```powershell
docker compose up -d
cd frontend; npm run dev -- --host 127.0.0.1 --port 5173
```

## 5. 当前唯一执行任务

```text
Task ID：P0-CURRENT-REVALIDATION
状态：READY（当前源码已在 2026-08-10 至 2026-08-11 发生 Provider/模型配置架构迁移；历史 10 Shot 成功与交付不能替代当前候选证据）
完成效果：
  1. 在 `dev@9e79ba4` 的干净候选上确认 Production Graph、统一 Provider 路径、Production Model Profile、LiteLLM 文本路径和审核 Gate 以固定 Beta 配置共同工作。
  2. 重新产生真实 10 Shot 的 Keyframe、Face、Video、Drift、Continuity、审核和交付证据；旧 `41def2f` 证据仅作对照。
  3. 形成同源 formal、Playwright、§3.1 Gate、ops、交付哈希和血缘报告；所有结论与 API/Worker `source_commit` 一致。
  4. 如实记录动态视频身份保持和本地 TTS 的限制，不以静态黄金样本外推产品能力。
范围：
  当前候选 Compose source 绑定；固定 Beta Provider/Profile/flag 配置；P0 浏览器回归、正式 Gate、交付校验和运维演练。
非范围：
  P1 候选治理/故事板/资产/精剪；多人协作；3D；OpenCut；增加 Provider/Slot；降低已批准阈值；review override；使用 Fake、人工注记或历史证据替代当前候选真实验收。
前置条件：
  P0 私有 Workspace 产品模型已锁定；`ADR-0003`（真实文本+图像+视频+本地 TTS 主黄金样本）和 ADR-0004（动态镜头能力边界）均已批准。发起真实运行前仍需逐轮费用授权；不得用手工媒体替代主黄金样本。
验收证据：
  当前全量质量、PG integration、真实 LiteLLM Proxy integration、frontend 和远端 CI；更新后的 P0 bootstrap 与真实 10 Shot Playwright；formal/gate/ops 同提交报告；四项交付哈希与 90 Artifact 血缘；`git diff --check` 和人工发布签字。
COMPLETED 条件：
  当前候选的所有 P0 Gate 均 PASS、无 BLOCKED，工作树干净、source 一致，并经用户批准进入封闭设计伙伴验证；Task 完成前仍保持“P0 功能候选版”。
```

## 6. 已完成的历史实现记录（默认不读）

以下内容记录当前 P0 候选之前的模型执行层迁移。它们不是并行待办，也不覆盖第 5 节的
`P0-CURRENT-REVALIDATION`。只有排查本轮重验失败根因时，才按相关任务读取。

### 6.1 Provider 模型能力插件化 — 阶段 A+B 统一抽象层

```text
Task ID：provider-unified-ab（合同见 docs/task-contracts/provider-unified-ab.md）
状态：A1–B5 已实现并本地全量质量通过；B6（删除旧 Flux/Kling 路由与 agnes 轮询特判）
      为生产切换后清理，须在 legacy 任务排空、新提交走统一路径稳定后执行。
决策依据：docs/模型能力插件化与视频生成抽象设计.md（2026-08-10 更新）——阶段 A+B
      作为统一抽象层一次闭环，不采用「先长期上线 A、正式执行继续读 Agnes Settings」双轨。
完成效果：
  A1 ✅ 不可变 Catalog：provider_model_catalog_entries（唯一键含 model_revision，无 RLS，
      GRANT SELECT/REVOKE 写）；冻结迁移种子 alembic/_seeds_0015.py（不 import 运行时）；
      manifest Pydantic + catalog_service 只读 + fixtures/providers/contracts/*.json 四份契约；
      迁移 20260810_0015 在隔离 PG 库验证 upgrade/downgrade/幂等。
  A2 ✅ Binding 级 Probe/Evidence + 回填：Probe 按 model_binding_id 用 invoke_model_value
      请求，只推进该 binding；create_model_binding 改 catalog entry 匹配；bind_project 收口
      keyframe/video + explicit_binding；0015 回填存量 32 binding（四层布尔不变）。
  A3 ✅ 共享 Eligibility + 只读候选 API：eligibility.evaluate_candidate 单实现；
      GET /projects/{id}/model-candidates 返回 eligible/issues/evidence。
  B1 ✅ Intent/Selection + shadow resolve：intents/normalizer/selection/runtime；
      product_path 旧路径 create 前写 shadow_selection（零 create）。
  B2 ✅ 唯一 Wire Compiler/Transport + Agnes：agnes.py 抽低层 transport + body builders，
      AgnesHubClient.create_* 改包装；AgnesImage/VideoCompiler + AgnesRuntime.submit_compiled。
  B3 ✅ Ark 迁移：volcengine.py 同构；Seedream image + Seedance content[first_frame]，
      不新增未验证字段。
  B4 ✅ submission 状态机 + Resume Token：ProviderOperation 扩展
      connection/binding/catalog/manifest/selection_plan/resume_token/execution_path_version；
      status 加 submission_started/rejected；_execute_unified_media_node_run 提交前持久化
      submission_started，恢复遇 submission_started 无远端 ID 转 unknown_submission；
      未绑定项目 fail-closed MODEL_BINDING_MISSING；恢复按持久化 token 不读 flag。
  B5 ✅ 切换准备：scripts/provider_unified_switch_report.py 生成 binding coverage 报告；
      PROVIDER_UNIFIED_PATH_ENABLED 默认 False，coverage 100% 后才翻转。
审查修复（2026-08-10，评审 c3ff6bd 后）：✅ 3 阻断 + 4 高 + 3 中全部修复——
  (1) seed manifest 视频能力细粒度 video.i2v.first_frame（与 normalizer 推导一致）+ 固定
      核验日期（documented_at 不再随 date.today() 漂移）；(2) SubmissionResult 增加
      artifact_uri，同步图片提交直接取 URL 跳过轮询；(3) 统一 Reference Resolver 按模型
      签发传输：Ark 走 issue_artifact_reference 短时 HTTPS、Agnes 走 bytes，引用缺失
      fail-closed；(4) 429 → op.status="rejected"+commit，重试复用同一 op 重新提交，
      submission_started 崩溃恢复仍转 unknown_submission；(5) Probe 除 auth_models 外强制
      model_binding_id，_mark_capability_verified 删除 connection 级证据扩散；(6) 质量证据
      通过 GraphEdge 追溯被 review 媒体的 producer op，model_binding_id 或 actual_model
      必须匹配目标 binding；(7) 切换报告要求 keyframe+video 双绑定全部 VERIFIED；
      (8) binding 唯一键改为含 catalog_entry_id（迁移 20260810_0016），eligibility 核对
      catalog lifecycle/provider/profile/media/manifest hash/invoke_model_value；
      (9)(10) documented_at 固定 + 审计时间逐处取 datetime.now(UTC)。
质量：backend unit 362 passed、PG integration 13 passed（隔离库迁移链，含 0016）、
  ruff + mypy 117 源码全绿、目录合规 OK。
范围：
  只开放 keyframe/video + explicit_binding；text/voice/canonical_image、auto 策略、
  MiniMax/ComfyUI（阶段 D/E）、Seedance duration/ratio/audio（阶段 C）、真实成本计量不开。
与 P0 关系：
  A+B 改变正式媒体执行路径。迁移前 Provider 执行证据只作历史对照，不能继承为迁移后发布证据；
  A+B 完成后必须在同一干净候选提交上重验付费 POST 防重、unknown_submission fail-closed、
  Worker 恢复、429/5xx 轮询、Reference 交付、产物下载、Face/Drift Gate、10 Shot 黄金样本、
  Playwright、§3.1 Gate 与运维演练。迁移完成且证据重新通过前，P0 仍不能标记完成。
```

### 6.2 模型能力插件化 V3（DramaForge_Model_Plugin_Architecture_V3_Full_Development_Spec.md）

```text
Task ID：model-plugin-v3
状态：P0 范围完成（Phase 0–12 实现 + 边界测试 + Implementation Report），B6 legacy 删除保持门控
执行依据：DramaForge_Model_Plugin_Architecture_V3_Full_Development_Spec.md + 78.5 最小迁移原则
Gap Analysis：docs/dev/model-plugin-v3-gap-analysis.md（Phase 0 强制产物）
Implementation Report：docs/dev/model-plugin-v3-implementation-report.md

已完成：
  P0 ✅ 现状审计 + Gap Analysis：docs/dev/model-plugin-v3-gap-analysis.md；
      修复 2 处基线（根级 spec 文档注册到 check_directory_compliance ALLOWED_ROOT、
      未提交 volcengine.py 行包裹语法断点）。基线 unit 362 passed。
  P1 ✅ 核心类型：capabilities.py（Capability + fine-grained 组映射）、contracts/（ArtifactRef/
      ExecutionContext/GenerationStatus/typed results/video/image/text 请求契约）、manifest.py
      扩展（ParameterSpec/InputSlotSpec/ConstraintSpec/CapabilitySpec/SubmissionSemantics/ModelManifest
      + to_v3_model_manifest 转换）、transport.py/connection.py/errors.py/translation.py。旧 flow 不变。
  P2 ✅ Registry + Bootstrap：ModelRegistry（get/list_models/find_by_capability）、
      TransportRegistry、bootstrap.build_v3_registry（agnes + volcengine 4 模型 + 4 transport）。
  P3 ✅ ModelAdapter V2 + LegacyAdapterBridge：adapter.py Protocol + adapters_v2.py 桥（纯
      translate + 委托 runtime）+ intent_bridge.py。核心验收 §69.2：同一 ImageToVideoRequest →
      Agnes 平铺 body 与 Ark content[] 两份不同 native payload，业务无 if/else。
  P4 ✅ Validator + CapabilityRouter + DefaultModelSelector：validator.py（输入槽/选项/约束严格
      校验）、router.py（resolve→select→validate→dispatch，无 fallback）。
  P5 ✅ Request Consistency：idempotency.py 规范语义指纹（§45，不变量：非 URL 工件身份）；
      现有 submit-once / resume-no-recreate / submission_started→unknown_submission 已由
      test_unified_path 覆盖。P0 无需 DB 迁移（unknown_submission/submission_started 已在 enum）。
  P6 ✅ Unified Generation API：GET /capabilities、GET /models?capability=、GET /models/{id}
      （manifest）、POST /projects/{id}/generations（NodeRun 引擎背书 + Idempotency-Key 复用
      UNIQUE(project_id, idempotency_key)）、GET + cancel 路由。P0 支持 image.generate 独立生成；
      video 模式保留 Shot gate 链、text 走 Agent Brief/Plan（422 显式拒绝）。
  P7 ✅ Agnes 身份分离验证（provider=agnes、model=agnes/<id>、无 flux/kling 标签）+ model_family
      不再与 model_revision 混淆。子包拆分按最小迁移记录为可选偏差。
  P8/9 ✅ Provider A（volcengine/ark_cn_v1 Seedance/Seedream）+ Provider B（agnes/agnes_cn_v1）
      均接入默认 registry 的真实 V2 bridge（CapabilityRouter 可实际提交）。
  P10 ✅ 前端 manifest 驱动 UI：listModels/getModelManifest 客户端 + ManifestOptionControls
      组件 + manifestOptions.ts 纯逻辑（switch/select/number/textarea/multi_select/input 渲染 +
      constraint 联动过滤 + violations 提示），无模型名分支；新增 7 项 vitest。
  P11 ✅ 角色 canonical 图像（api/v1/characters.py）从 flux getter 切到 CapabilityRouter +
      workspace_router.resolve_workspace_bridge（workspace BYOK 连接驱动 bridge）。
  P12 ✅ Architecture Boundary Test（test_v3_boundary.py）：业务目录禁止 import 具体 Provider
      模块（LEGACY_COMPAT 钉死 5 文件），providers 禁止 import app.api。B6（删 legacy getter +
      product_path legacy 分支 + 前端快捷模型按钮）按 A+B 检查点保持门控。

质量：backend unit 435 passed / 0 failed；ruff + mypy 全绿（app 138 源码无错）；
  frontend lint/typecheck 干净、vitest 22 passed。PG integration 发布前需在干净候选重跑。

剩余/风险：text.generate 未接 V3（P1 文本 bridge）；MiniMax 未接（P1，§69 偏差已记录）；
  video 独立生成需 face gate 链（保持 Shot pipeline）；B6 legacy 清理；ProviderOperation
  独立列迁移随 P1 fallback。
```

### 6.3 多模型协同制作 + LiteLLM Gateway（DramaForge_Multi_Model_Production_Profile_LiteLLM_DS_Development_Spec.md）

```text
Task ID：multi-model-production-profile
状态：M0–M11 完成（后端核心 + 前端 + 测试 + 迁移），M12 部分（完整 Playwright E2E 未跑）
执行依据：DramaForge_Multi_Model_Production_Profile_LiteLLM_DS_Development_Spec.md
Gap Analysis：docs/dev/multi-model-production-profile-gap-analysis.md（M0 强制产物）
Implementation Report：本地留存（不随仓库上传）

已完成：
  M0 ✅ 双仓审计 + Gap Analysis：10 问全答，确认本设计是 V3 之上的「模型角色配置层」。
  M1 ✅ ModelSlot 核心：9 Slot + MODEL_SLOT_DEFINITIONS + P0 集合 + 简单模式分组 + 不变式校验。
  M2 ✅ ProductionModelProfile：DB（workspace/project 两级 + version + is_default + bindings JSON +
      RLS + 部分唯一索引）+ Service（CRUD + 乐观锁 + copy_from 快照 + simple-mode 批量 patch）。
  M3 ✅ ModelBindingResolver：request → project → workspace → system 优先级，registry 校验，
      capability mismatch fail-fast（video.shot 派生能力按 spec §122 拦截）。
  M4 ✅ Profile 校验：model exists / slot accepts / model supports / native option（经 validate_parameter）。
  M6 ✅ Profile Snapshot：GraphVersion.definition 存 model_profile + NodeRun.input_snapshot 记
      slot/resolved model/profile id/version（spec §22/§92）。
  M7 ✅ LiteLLM Backend：ModelBackendBinding + GenericLiteLLMModelAdapter（httpx，无 litellm 依赖）
      + 默认 registry 注册 litellm/text-llm（text.generate）。
  M8 ✅ text.generate V3 桥：TextGenerateRequest 扩展 messages/response_format；creation/service.py
      切 ModelBindingResolver → CapabilityRouter（TEXT_V3_ROUTER_ENABLED 门控，默认 legacy 零回归）。
  M9 ✅ Image/Video Slot 集成：独立生成 API 按 slot 解析默认模型；keyframe/video/voice NodeRun
      记录 model_profile；video capability 推导辅助函数。媒体 wire 选择仍由 A+B binding 负责（渐进）。
  M10 ✅ 单元测试：新增 42 项（slots/profiles/resolver/snapshot/litellm bridge/API surface）。
  M11 ✅ 集成测试：隔离 PG 迁移链（upgrade/数据/唯一约束/downgrade/re-upgrade）+ RLS 验证。
  M5 ✅ 前端：ModelProfileSettings（简单 LLM/Image/Video + 高级 per-slot，模型来源于 registry）
      + api.ts 客户端 + 4 项 vitest。

质量：backend unit 511 passed / 0 failed（基线 469）；PG integration 14 passed（隔离库迁移链含 0017）；
  ruff + mypy 149 源码全绿；frontend typecheck/lint 干净、vitest 26 passed、production build 通过。
  开发库已应用 0017 迁移（alembic upgrade head）。

范围/遗留：完整 Playwright E2E（Idea→Brief→Script→Storyboard→Keyframe→Video 浏览器全流程）未跑；
  video.shot 执行级与 A+B binding 打通（渐进，spec §115）；visual.character/storyboard/image_edit/audio.tts
  为可配置但未驱动执行的扩展 Slot；LiteLLM 媒体能力未接；legacy 文本路径保持 TEXT_V3_ROUTER_ENABLED 门控。
```

### 6.4 当前 dev LiteLLM Proxy 集成修复（DramaForge_LiteLLM_Proxy_Current_Dev_Fix_Spec_for_DS.md）

```text
Task ID：litellm-proxy-current-dev-fix
状态：F0–F14 完成；F15（legacy 文本删除）保持 TEXT_V3_ROUTER_ENABLED 门控，待生产文本稳定。
执行依据：DramaForge_LiteLLM_Proxy_Current_Dev_Fix_Spec_for_DS.md（本地留存，不随仓库上传）
Gap Analysis：docs/dev/litellm-proxy-current-dev-gap-analysis.md（F0 强制产物）
BYOK Decision：docs/dev/litellm-byok-decision.md（F11）
Implementation Report：docs/dev/litellm-proxy-integration-report.md

已完成：
  F0 ✅ 当前状态审计 + Gap Analysis（§86 十二项全答；BLOCKER B1–B4 确认）。
  F1 ✅ docker-compose 新增 litellm-db（独立 postgres）+ litellm（官方 ghcr.io/berriai/litellm:v1.96.0，
      pinned + digest，healthcheck 仅 /health/liveliness）；api/worker 不硬依赖 litellm（§13）。
  F2 ✅ infra/litellm/config.yaml：script-quality / script-fast / legacy-text 逻辑别名
      （os.environ 密钥，不写真实 Key）+ router_settings + general_settings；README + compatibility.md。
  F3 ✅ .env.example 补全 LITELLM_GATEWAY_URL/API_KEY/MASTER_KEY/DB_PASSWORD/IMAGE + 逻辑别名
      + 每别名 deployment env；api/worker-default/worker-heavy 注入 Gateway URL/Key + TEXT_V3_ROUTER_ENABLED
      + LITELLM_LOGICAL_MODELS；NO_PROXY 加 litellm,litellm-db。
  F4 ✅ 抽取 LiteLLMGatewayClient（app/providers/litellm_gateway/：client/errors/metadata/model_catalog）；
      adapter 不再直接 httpx.post。
  F5 ✅ 删除 _MAX_ATTEMPTS=3 通用 blind retry；一次 POST；LiteLLM Router 负责 provider retry。
  F6 ✅ 错误分类：401/403→auth_failed、404/unknown-model-400→model_unavailable、429→rate_limited、
      5xx/connect→provider_unavailable、read timeout after submit→SUBMIT_UNKNOWN；
      op.status=unknown_submission 持久化。
  F7 ✅ 解析 x-litellm-response-cost(-original)/attempted-retries/fallbacks/response-duration-ms
      （allowlist，无 secret）；_text_call_cost 读真实成本进 ProviderOperation.provider_cost。
  F8 ✅ GET /v1/models + TTL cache + LiteLLMModelCatalogSyncService（best-effort，gateway down 不 raise）。
  F9 ✅ litellm/text-llm 标记 bootstrap bridge + LEGACY_COMPAT（gateway_model=legacy-text，解耦 TEXT_LLM_MODEL）；
      静态注册 litellm/script-quality、litellm/script-fast（settings LITELLM_LOGICAL_MODELS）。
  F10 ✅ Profile planning.script → litellm/script-quality 可经 CapabilityRouter 全链路由（验收场景 §131）。
  F11 ✅ docs/dev/litellm-byok-decision.md：Phase 1 Gateway-managed + Virtual Key；Phase 2 clientside
       credentials；旧 BYOK 不静默忽略（legacy 路径保留）。
  F12 ✅ 真实 LiteLLM Proxy container integration test（DramaForge → Real Proxy → Router → mock 多 deployment）
       6 项全过（含 proxy readiness/models/chat + DramaForge adapter + 未知 model 分类）。
  F13 ⚠️ 无真实 Provider Key：未做真实 LLM 调用；集成测试用 mock_response 证明 Proxy 运行时链路。
  F14 ✅ TEXT_V3_ROUTER_ENABLED=true 下 brief 经真实 Proxy 生成成功（CreationService → 真实容器，E2E）。
  F15 ⏳ legacy 文本路径保留（TEXT_V3_ROUTER_ENABLED 默认 false），稳定后删除。

质量：backend unit 537 passed / 0 failed；PG + real-proxy integration 20 passed；
  ruff + mypy 154 源码全绿；compose config 校验通过；新增 litellm-integration CI job。

范围/遗留：F13 真实 Provider E2E 未跑（无 Key，报告明确说明）；media（MiniMax/Volcengine）仍走 native A+B，
  未进 LiteLLM；Virtual Key / workspace attribution 为 P1（BYOK-2）；legacy 文本删除待 flag 翻转后稳定。
```
