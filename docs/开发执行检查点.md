# DramaForge 开发执行检查点

**状态：本地 READY 能力已尽量落地 / 真实环境项独立 PAUSED / P0 MVP Gate 未通过 / 不进入 P1/P2**

**最近更新：2026-07-21**

**唯一开发工作区：`D:\调研\dramaforge`**

## 1. 当前定位

| 项目 | 事实 |
|---|---|
| 当前唯一执行任务 | **无本地 READY 剩余**（见 §2 PAUSED 解锁列表） |
| RECOVERY | **不重跑**（已 COMPLETED） |
| P0 MVP | **未完成、不可宣称完成** |
| P1/P2 | **未开始** |
| Agnes 免费 API 真实冒烟 | **`full_flow_ok: true`**（见 `docs/acceptance/agnes_e2e_real_latest.json`）；**不等于** S2/S4/P0 Gate |

## 2. 本轮补齐的本地 READY（有测试）

| Task | 状态 | 提交/证据 |
|---|---|---|
| S1.3 Outbox lease/publish/dead-letter/replay + metrics | COMPLETED | `287933c`；`test_outbox_sse` |
| S1 SSE Last-Event-ID + reconnect metric | COMPLETED | 同上；`SseHub` + `/api/v1/events/stream` |
| S1 production_graphs 字段忠实 + 迁移 0003 | COMPLETED | `production_graphs`/`graph_versions`/`event_log`/`outbox_*` |
| S2 fake OpenAI+Flux 竖切 + face hook + materialization 白名单 | COMPLETED | `FirstFramePipeline`；`test_s2_fake_pipeline` |
| S3 cache/budget/cancel 本地不变量 | COMPLETED | `runtime_invariants` |
| S4 10-shot mock + 字幕局部返工 | COMPLETED | `f2f825e`；`test_s4_multi_shot` |
| S5 export timeline/SRT 哈希包 | COMPLETED | `export_local` |
| Agnes 免费 hub 真实冒烟（图+视频+Pipeline） | COMPLETED | `scripts/run_agnes_e2e_real.py`；`docs/acceptance/agnes_e2e_real_latest.json` `full_flow_ok=true` |

后端 pytest：**S2/S4 相关 5 passed**（本机）；全量以 venv pytest 为准。

### Agnes 真实冒烟证据（2026-07-21，免费 API）

| 步骤 | 结果 |
|---|---|
| GET /models | 200 |
| 真实图像 `agnes-image-2.1-flash` | succeeded + PNG URL |
| 真实视频 `agnes-video-v2.0` | create→poll completed + MP4 URL |
| FirstFramePipeline + AgnesImageAdapter | Graph/NodeRun/Artifact/face_review passed |
| 同项目 10-shot mock + export | timeline/SRT hash；MP4 仍为占位 |
| 费用 | 报告 amount=0.0（免费档） |

**诚实边界**：无 Docker PG/RLS、无 live FFmpeg 可重现 MP4、无 01§3.1 全量 E2E、无真实 Azure TTS；**不得宣称 P0 MVP 完成**。

### Skeptic 修复（本轮）

- Outbox `human_replay`：按 `event_id` 的 **published** 行做跨 `OutboxDispatcher` 实例幂等，禁止二次 publish。
- S2：`FirstFramePipeline` 写 `ProductionGraph` → `GraphNode` → `NodeRun` → `ProviderOperation` → `Artifact` → face_review。
- S3/S4/S5：NodeRun cache/stale、10-shot Graph 路径、导出引用 Artifact 行。

## 3. 仍 PAUSED（仅真实外部/人工）

| ID | 解锁条件 |
|---|---|
| `BOOT-0-CONTAINERS` | 安装 Docker CLI；`docker compose config && up`；PG/Redis/MinIO/API/Workers 健康 |
| `S1-PG-RLS` | 真实 PostgreSQL 上跑迁移 + 跨项目 RLS 拒绝与连接池泄漏测试 |
| `S1-REDIS-STREAMS-LIVE` | 真实 Redis Streams 投递与消费（本地已有 in-process publisher 假实现） |
| `GIT-REMOTE-0.1` | 配置并核验私有 GitHub origin + 认证（勿写令牌） |
| `S0-A-FIXTURE` | ≥20/20/10 合法人脸样本 + InsightFace 跑通 FAR/FRR（禁止编造） |
| `S2-BYOK-REAL` | Agnes 免费 hub 图/视频冒烟已通过（见 acceptance JSON）；正式 OpenAI 文本 BYOK 与 Flux 原厂/生产计费叙事仍待；不得用平台代付 |
| `S3-LIVE-RUNTIME` | Docker 上 Arq/Redis/PG 故障恢复与回调乱序全量演练 |
| `S4-GOLDEN-BYOK` | 冻结 10-shot 黄金样本 + 真实/可批准手工媒体 + Kling/TTS Key |
| `S5-FFMPEG-LIVE` | 本机/容器 FFmpeg 合成真实 MP4 可重现哈希 |
| `P0-E2E-18` | `01` §3.1 全部 18 条在集成环境通过 |

## 4. 明确未做的宣称

- 未宣称 S1 阶段 Gate 全部通过（缺 PG RLS / 真 Redis）
- 未宣称 S2–S5 产品 Gate 通过（仅本地 fake/mock 能力）
- 未宣称 P0 MVP 完成
- 未打开 P1/P2

## 5. 提交索引

- `98a4773` S1.1 session/CSRF/org
- `1b4e096` S1.2 projects
- `8f60d3b` S1.3–S1.5 shells
- `287933c` S1 residual + S2–S5 local
- `f2f825e` S4 multi-shot mock

## 6. 恢复

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\.agent-control\control.ps1 -Operation open
git log -10 --oneline
cd backend; .\.venv\Scripts\python.exe -m pytest -q
```
