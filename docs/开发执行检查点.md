# DramaForge 开发执行检查点

**日期 / HEAD：2026-08-26 / Phase 2（P2 结构化资产、版本与 `@资产`）已完成（七方案执行中）**

## 今日唯一目标

按 `docs/plans/professional-program-v2/README.md` 的唯一顺序串行推进：收口 MS5-IDENTITY-C → 完成 Phase 1（P1）→ 完成 Phase 2（P2）。P1/P2/P3 都是 07 §15 Phase 4 Merge Gate 的前置。

## 完成证据

### MS5-IDENTITY（A/B/C 已完成，见上期检查点）

- MS5-A：credential immutable revision（迁移 0041）；MS5-B：ProviderConnectionRevision 冻结（迁移 0042）；MS5-C：secret-free ExecutionIdentitySnapshot + rev-aware resume（focused 32 / 全量 717）。

### Phase 1 / P1 Professional Workspace Foundation（已完成，见上期检查点）

- workspace_state API/hook/恢复、Scene/Shot 专业字段（迁移 0043）、Shot Design PATCH 409、专业 shell 路由；后端 727 / 前端 vitest 62 / Playwright 8。

### Phase 2 / P2 结构化资产、版本与 `@资产`（已完成）

- P2-01 AssetVersion 生命周期：`version_service.py`（create_candidate/promote/reject/list_history/resolve_current），promote 原子（old formal→historical，candidate→formal，`current_version_id` 指向候选）；唯一 formal、旧版本不删、rejected 不可 promote、跨项目隔离。
- P2-02 AssetVersionReference + CharacterReference 兼容：迁移 `20260826_0044` 回填 Character→AssetVersion v1（formal）+ AssetVersionReference（canonical→primary、未知 kind→primary）并设置 `current_version_id`；`AssetCardReadService` 迁移期合并读取（version 优先、legacy 补缺、同一 Artifact 不重复）；`CharacterReference` 未删除。
- P2-03 AssetTag/Recycle：`asset_tags`/`asset_tag_links` + `tag_service.py`；`POST /asset-tags`、`PUT /assets/{id}/tags`、`POST recycle/restore`；`GET /assets` 支持 kind/status/name/tags 过滤。
- P2-04 Asset API / Asset Cards：`POST /assets/from-artifact` 显式“加入资产”；`frontend/src/features/assets/` 资产卡面板（标签过滤、版本、回收/恢复、候选提升）。
- P2-05 ShotReferenceBinding：ORM + `references.py` CRUD + resolve；来源 XOR、purpose/stage/resolution_mode CHECK、版本乐观锁 409；业务 purpose 词表 12 项，不存 provider role。
- P2-06 `@资产` 前端引用：`AssetMentionInput.tsx` / `AssetReferencePicker.tsx`；autocomplete 选中真实 Asset 才建立 Binding，未绑定手打 `@` 显示“未解析引用”；UUID 绑定，改名不失效。

## 验证结果

- 后端全量 unit：`738 passed / 1 warning`（+11 个 P2 测试）；P2 focused `11 passed`。
- `ruff check app tests alembic/versions`：All checks passed；`mypy app`：Success（193 source files）；`compileall`、`git diff --check`、`scripts/check_directory_compliance.py --root .`：通过。
- 迁移链单 head `20260826_0044`，offline `alembic upgrade head --sql` 生成/编译通过（含回填 SQL）。
- 前端：`tsc --noEmit`、`vite build` 通过；vitest `68 passed`（+6 个 P2 组件测试）；eslint 0 errors（仅既有 CreativeStage Fast Refresh warning）；Playwright `8 passed`。
- PostgreSQL 集成：`TEST_PG_ENABLED` 未设置且 `127.0.0.1:5432` 不可达，integration `6 passed / 17 skipped`（PG 用例如实 skip）。
- 真实 Provider 付费调用：未授权，全程零网络假 runtime 验证；`paid_provider_calls=0`。

## 已完成领域闭环（沿用此前专业工作台事实）

- 画布编辑已落到 `CanvasRevision` 持久化合同；AssetVersion、ExperimentBranch、ReviewAnnotation、OpenCut v2、DirectorBoardState 已落库并接入 UI。
- 专业镜头启动/局部重跑显式绕开旧预算 Gate；正式/实验 NodeRun 共享 Production Graph，正式线不消费未采纳实验产物。
- 旧 Director 预算 API/快照/表保留仅为历史回放兼容，专业入口不可达且不展示。

## P0/P1 缺陷与外部证据

- 当前未发现由专业切片引入的类型、构建或既有回归阻断。
- 加密备份已生成；数据库 dump 已验证可在隔离 PostgreSQL 中恢复；MinIO 对象恢复需在真实部署网络/端口条件下复核。
- Provider 真实付费媒体黄金样本、OpenCut 实际应用导入、离线/跨平台和三名目标用户无人代操作仍是外部发布证据，需绑定同一干净 release commit。

## 真实费用与外部调用

专业 live proof 明确 `paid_provider_calls=0`；真实付费媒体黄金样本仍需用户明确允许并绑定同一 release commit。

## 合同或 ADR 变化

新增 ADR 0006；新增迁移 0032–0039、0041–0044；MS5-IDENTITY-A/B/C、P1、P2 合同已按七方案完成并记录证据。

## 下一唯一目标

按 07 §4 补齐 **Phase 3**：Scene Wall（故事板墙）、Scene Workspace、Shot Workbench、Canvas、Asset bindings（Scene.design_state/Shot.director_state 的编辑工作区与 2D/3D 导演台已在 P1/P2 打底），随后进入**单独 Gate 审计**的 Phase 4 Merge Gate（不得自行宣布 merge gate 通过），再进入 P4 Manual Production Alpha。

## 当前 Task Contract

`docs/plans/professional-program-v2/task-contracts/P2-PHASE2-ASSET-REFERENCES.md` 已完成并记录证据。下一项 READY 合同为 Phase 3（P3），继续沿七方案顺序串行推进。