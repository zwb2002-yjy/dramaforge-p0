# DramaForge 开发执行检查点

**状态：P0 功能候选版 · 禁止宣称完整 P0 MVP**

**最近更新：2026-07-22（前端 baseUrl/测试修复 + P0 状态基线重置）**

## 0. 诚实基线

| 事实 | 说明 |
|---|---|
| 产品标签 | **P0 功能候选版**。不得标记「完整 P0 MVP 完成」或「§3.1 验收通过」。 |
| 旧 gate JSON | `docs/acceptance/p0_section31_gate_latest.json`（约 2026-07-21 12:26 UTC）**早于**完整性门禁修复，**不能**作为最终结论。 |
| 假 Adapter / Fake TTS | 仅 `APP_ENV=test`；**不可**作为生产验收。 |
| Docker / S5 | **DEFERRED**（部署、恢复、安全回归仍为冻结 P0 需求，本轮不纳入）。 |
| 正式本地栈 | `scripts/start_p0_wsl_stack.sh`：WSL 内 PG+Redis+MinIO+API+Arq；**不**设 `DRAMA_FORCE_MEMORY_STORE=1`。 |

## 1. 当前 P0 未完成项

### 1.1 实际未实现或被外部依赖阻塞

| 项 | 状态 |
|---|---|
| S0-A InsightFace | 模型/环境失败时 `hash_placeholder`；缺 512-d 实测、FAR/FRR、阈值与签字 |
| 正式语音 Provider | 生产 voice 节点未启用真实 Provider；测试 Fake 不算验收 |
| 真正交付 MP4 | 无可复现 FFmpeg + 真实媒体合成证据；历史为 placeholder |
| S5 Docker 部署与安全回归 | 冻结需求，**DEFERRED** |

### 1.2 代码已有，但无正式 WSL/PG 现场证据

- 10 Shot 九节点全链路：`prompt → keyframe → face_review → video → video_drift_review → voice → subtitle → composite → continuity_review`，逐 Shot 审核且零失败 NodeRun
- AgentRun → 多 ProviderOperation 聚合；Outbox lease / 恢复 / 死信重放 / 幂等
- 四层连续性、仅字幕下游失效、缓存命中零成本、Provider single-flight
- 预算不足、取消与迟到结果竞争
- SSE Last-Event-ID 恢复与 UI 去重
- PostgreSQL RLS：跨项目与 Worker 回源拒绝
- **新的**可解析 §3.1 gate 报告全部通过（必须在完整性修复之后重跑）

## 2. 本轮前端修复（2026-07-22）

| 项 | 内容 |
|---|---|
| `frontend/tsconfig.json` | 删除废弃 `baseUrl`；`paths` 为 `"@/*": ["./src/*"]` |
| `WorkstationShell.test.tsx` | 对齐当前品牌节点与 project-panel 文案 |
| 验证 | **已通过** `npm.cmd run typecheck`；`npm.cmd test -- --run`（3 passed）；`npm.cmd run build` |
| 不改 | Docker、WSL、Compose、后端部署、本机启动路径 |

## 3. 后端完整性修复（已合入，仍待现场复验）

| 项 | 状态 |
|---|---|
| commit-then-enqueue / 禁止假 queued | 代码已修（`74b2d4f` 等） |
| APPROVE_GATE / EXPORT_GATE / 角色 / package.zip 元数据 | 代码已修 |
| 相关 unit | 曾绿；**正式 WSL 链路未作为本轮验收** |

## 4. 口径

- 可标记：**P0 功能候选版**
- 不可标记：**完整 P0 MVP 完成** / 用旧 gate JSON 或假 Adapter 宣称验收
- 下一阶段优先级：InsightFace 校准 → 真实语音/媒体 + FFmpeg 交付 → 正式 WSL §3.1 重跑

## 5. 当前唯一执行任务

```text
Task ID：FE-TSCONFIG-P0-BASELINE
完成效果：前端 typecheck/test/build 全绿；检查点重置为诚实 P0 候选基线
范围：tsconfig paths、WorkstationShell 测试、检查点文档
不负责：Docker/S5、InsightFace 签字、真实 BYOK 费用、现场 10 Shot 验收
```

## 6. 启动（用户，未改路径）

```powershell
wsl -d Ubuntu-24.04 -- bash /mnt/d/调研/dramaforge/scripts/start_p0_wsl_stack.sh start
cd frontend; npm run dev -- --host 127.0.0.1 --port 5173
```

健康：`http://127.0.0.1:8010/health` → `status=ok` 且 `db=up`。
