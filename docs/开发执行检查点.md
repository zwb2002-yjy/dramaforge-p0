# DramaForge 开发执行检查点

**日期 / HEAD：2026-08-26 / 工作树待提交（七方案执行中，MS2）**

## 今日唯一目标

按 `docs/plans/professional-program-v2/README.md` 的唯一顺序执行 MS2：统一 canonical reference role，严格拒绝 Manifest 未声明的 input slot，并在 Provider/Compiler 前完成 media-type 与 cardinality 校验。

## 完成证据

- 已新增 ADR：`docs/adr/0006-professional-workbench-reframe.md`，记录“专业工作台取代四阶段向导作为产品主形态”的用户确认。
- 已更新根总纲、当前产品契约、路线图和当前合同导航，明确快速四阶段降级为兼容入口。
- 已新增 `frontend/src/features/production/ProfessionalWorkbench.tsx`：场景/镜头列表、中央画布、手动编辑、自动/手动助手模式、建议逐项采纳/拒绝、资产卡、导演台、实验比较、图片/视频审片和镜头生产链。
- 已把专业工作台挂载到 `frontend/src/routes/projects.$projectId.production.tsx`；原有节点状态、审片、局部重跑与导出区域仍保留；专业 UI、项目创建/读取和 Provider 面板已移除预算/计费交互，旧表列/Director 路由仅标记为历史兼容。
- 已新增迁移 `0032`–`0039`，覆盖画布修订、提案 RLS、资产版本、实验分支、审片空间坐标和导演台状态。
- 已完成 MS1-R + MS1-C：新增 `ExecutionModelResolver` / `ExecutionModelResolution`，ModelSelectionService 与统一 Professional execution path 已消费同一解析结果；Profile X 不可用时 fail-closed，不提交 Provider 请求。
- 已完成 MS2：新增 canonical role vocabulary 与 `UnsupportedInputSlotError`；plural request containers 归一化为 singular role，strict validator 执行未知 slot、required/min/max cardinality、resolved MIME 和 request/reference 数量一致性检查，Provider submission 前失败。

## 验证结果

- `backend -m ruff check app tests alembic/versions`、`backend -m mypy app`：通过。
- 后端全量：`676 passed / 16 skipped`；PostgreSQL 集成可达部分 `6 passed / 16 skipped`。
- 前端全量 Vitest：`62 passed`；typecheck、lint（仅既有 Fast Refresh warning）、build 均通过。
- Playwright：`8 passed`；当前 Docker 栈专业页面 DOM/网络/控制台验收通过，`/quick` 重定向专业入口，专业页面无预算/计费/费用/价格预估/充值/账单文案。
- PostgreSQL 真实迁移：`20260825_0039 (head)`。
- 认证 live-stack 证明：资产 v2、Canvas v2、提案 applied、粗 3D 导演台、实验 accepted、时间段批注、OpenCut v2、`paid_provider_calls=0`。

## 已完成领域闭环

- 画布编辑已落到 `CanvasRevision` 持久化合同：PATCH canvas 使用 `expected_version` 乐观锁，写入不可变修订并递增 Shot.version；助手建议先写入镜头级 `ShotChangeProposal`，用户保存画布后才进入正式事实。
- AssetVersion、ExperimentBranch（候选 Artifact/比较/执行/采纳范围）、ReviewAnnotation 图片点/区域与视频时间点/时间段、OpenCut v2 视频/音频/字幕轨、DirectorBoardState 2D/粗3D导演台已落库并接入 UI。
- 专业镜头启动/局部重跑显式绕开旧预算 Gate；正式/实验 NodeRun 共享 Production Graph，但正式线不会消费未采纳实验产物。
- 旧 Director 预算 API/快照/表保留仅为历史回放兼容，专业入口不可达且不展示。

## P0/P1 缺陷与外部证据

- 当前未发现由专业切片引入的类型、构建或既有回归阻断；当前真实栈迁移已执行至 `20260825_0039`。
- 加密备份已生成；数据库 dump 已验证可在隔离 PostgreSQL 中恢复。由于当前 Compose 未发布 MinIO 宿主端口，MinIO 对象恢复需要在真实部署网络/端口条件下复核，不能把本地转发失败标记为完整恢复通过。
- Provider 真实付费媒体黄金样本、OpenCut 实际应用导入、离线/跨平台和三名目标用户无人代操作仍是外部发布证据，需绑定同一干净 release commit。

## 真实费用与外部调用

专业 live proof 明确 `paid_provider_calls=0`；Provider 能力探测改为“直接调用供应商”确认，不再收集 DramaForge 预算金额。真实付费媒体黄金样本仍需用户明确允许并绑定同一 release commit。

## 合同或 ADR 变化

新增 ADR 0006；新增迁移 0032–0039；产品主形态、用户画像、验收和路线图切换为专业版，Provider、质量、Artifact 血缘和安全硬边界不变；预算/计费由模型 Provider 负责，DramaForge 专业层不再维护。

## 下一唯一目标

进入七方案顺序中的 MS3：保持 ordered multi-reference 在 `list[ResolvedReference]` 中传输，证明同 role 多 Artifact 不丢失、不覆盖、不按 role 去重；不得提前进入 MS4-LITE 或 Phase 4 合流。

## 当前 Task Contract

`docs/plans/professional-program-v2/task-contracts/MS2-STRICT-REFERENCE-SLOT-VALIDATION.md` 已完成并记录证据。下一项 READY 合同为 MS3；继续沿七方案顺序串行推进，暂不并行改写共享 Schema。
