# DramaForge 开发执行检查点

**日期 / HEAD：2026-08-26 / Phase 3（P3 场景中心专业工作台）已完成（七方案执行中）**

## 今日唯一目标

按 `docs/plans/professional-program-v2/README.md` 的唯一顺序串行推进：收口 MS5-IDENTITY-C → Phase 1（P1）→ Phase 2（P2）→ Phase 3（P3）。P1/P2/P3 都是 07 §15 Phase 4 Merge Gate 的前置；P3 后按 §15 清单审计后进入单独 Gate 审计的 Phase 4 Merge Gate。

## 完成证据

### MS5-IDENTITY（A/B/C 已完成，见上期检查点）

- MS5-A：credential immutable revision（迁移 0041）；MS5-B：ProviderConnectionRevision 冻结（迁移 0042）；MS5-C：secret-free ExecutionIdentitySnapshot + rev-aware resume。

### Phase 1（P1，已完成，见上期检查点）

- workspace_state API/hook/恢复、Scene/Shot 专业字段（迁移 0043）、Shot Design PATCH 409、专业 shell 路由。

### Phase 2（P2，已完成，见上期检查点）

- AssetVersion 生命周期、AssetVersionReference + CharacterReference 兼容（迁移 0044）、AssetTag/Recycle、from-artifact 资产卡、ShotReferenceBinding（业务 purpose、XOR、409）、@Asset 解析与前端引用。

### Phase 3（P3 场景中心专业工作台，已完成）

- **P3-01** `GET /projects/{id}/scenes` SceneSummary 批量聚合（shot/formal/risk 计数 + representative_artifact，无 N+1）。
- **P3-02** `SceneStoryboardWall.tsx` 故事板墙（代表图/名称/时间/shot count/状态；进入/拖拽排序/复制）。
- **P3-03** `SceneStructureService`：reorder/copy/split-preview/split/merge-preview/merge，destructive 操作强制 preview（affected shots/experiments/formal media）。
- **P3-04** `GET /scenes/{scene_id}/workspace` 场景范围快照（scene+shots+bindings+candidates+trace）。
- **P3-05** `SceneWorkspace.tsx`/`CinematicCanvas.tsx`/`ShotStrip.tsx`/`ShotProductionTrace.tsx`/`ShotDesignPanel.tsx`；镜头序列/大画布/导演面板/生产链轨迹布局。
- **P3-06** `GET /shots/{shot_id}/workbench` 后端聚合（Prompt/Director State/References/Formal Artifacts/Candidates/Trace/old-version warnings）；新 UI 不解析 runtime JSON。
- **P3-07** `/scenes/$sceneId` 路由接入；ShotDesignPanel 复用 P1 Shot Design PATCH、AssetReferencePicker 复用 P2 binding；根路由 last-view 恢复目标 `/scenes`。

## 验证结果

- 后端全量 unit：`746 passed / 1 warning`（+8 个 P3 测试）；P3 focused `8 passed`。
- `ruff check app tests alembic/versions`：All checks passed；`mypy app`：Success（196 source files）；`compileall`、`git diff --check`、`scripts/check_directory_compliance.py --root .`：通过；无新迁移（head 仍 `20260826_0044`）。
- 前端：`tsc --noEmit`、`vite build` 通过；vitest `72 passed`（+4 个 P3 组件测试）；eslint 0 errors（仅既有 CreativeStage Fast Refresh warning）；Playwright `8 passed`。
- PostgreSQL 集成：`TEST_PG_ENABLED` 未设置且 `127.0.0.1:5432` 不可达，integration `6 passed / 17 skipped`（PG 用例如实 skip）。
- 真实 Provider 付费调用：未授权，全程零网络假 runtime 验证；`paid_provider_calls=0`。

## 已完成领域闭环（沿用此前专业工作台事实）

- 画布编辑已落到 `CanvasRevision` 持久化合同；AssetVersion、ExperimentBranch、ReviewAnnotation、OpenCut v2、DirectorBoardState 已落库并接入 UI。
- 专业镜头启动/局部重跑显式绕开旧预算 Gate；正式/实验 NodeRun 共享 Production Graph，正式线不消费未采纳实验产物。
- 旧 Director 预算 API/快照/表保留仅为历史回放兼容，专业入口不可达且不展示。

## P0/P1 缺陷与外部证据

- 当前未发现由专业切片引入的类型、构建或既有回归阻断。
- 加密备份已生成；数据库 dump 已验证可在隔离 PostgreSQL 中恢复；MinIO 对象恢复需在真实部署网络/端口条件下复核。
- Provider 真实付费媒体黄金样本、OpenCut 实际应用导入、离线/跨平台和三名目标用户无人代操作仍是外部发布证据，需绑定同一干净 release commit。


## 七方案内化与旧方案混淆清理审计（本回合复核）

- `source-integrity.json` 固定哈希 7/7 全部匹配（01–07 原文未漂移）。
- `docs/current/` 已删除；`docs/task-contracts/`、`docs/archive/` 为空；旧根总纲/旧 P0 阶段名称不再决定范围。
- `docs/README.md` 以 `plans/professional-program-v2/README.md` 为唯一规划导航；`runbooks/release-gate-board.md` 已标记 HISTORICAL。
- `verify_active_plan_references.py` 输出：`active planning references use the seven-plan program`。
- 结论：七方案已完整内化，旧方案混淆已清理，无遗留规划权威冲突。

## Owner 最新确认报告复核（2026-08-27，HEAD `958addc`）

- 依据 `D:/DRAMAFORGE_PHASE4_MERGE_GATE_LATEST_CONFIRMATION.md`（其“最新 HEAD `9e0b27f`”实为**远程旧 HEAD**，当时本地 52 个提交未推送）。
- 6 个阻断项复核：B01 唯一 ExecutionModelResolver、B02 ExecutionModelResolution、B03 No Silent Fallback、B04 Credential Immutable Revision、B05 ProviderConnectionRevision+冻结 runtime 均已在本地 HEAD 关闭（证据见 `gate-reports/PHASE4-MERGE-GATE-AUDIT-REPORT.md` 复核节）；B06 CI/Security 依赖新 HEAD 远端 Actions，进行中。
- 已把 `958addc` 推送 `origin/dev`（PR #12 head 更新），GitHub Actions 将在真实最新 HEAD 运行。
- 本地 MG-P4 证据：focused 101 passed、全量 746 passed、vitest 72、e2e 8；仍未自行宣布 Gate 通过。
## 真实费用与外部调用

专业 live proof 明确 `paid_provider_calls=0`；真实付费媒体黄金样本仍需用户明确允许并绑定同一 release commit。

## 合同或 ADR 变化

新增 ADR 0006；新增迁移 0032–0039、0041–0044；MS5-IDENTITY-A/B/C、P1、P2、P3 合同已按七方案完成并记录证据。

## Phase 4 Merge Gate 审计（已完成证据核对，未自行宣布通过）

- 按 07 §15 逐项核对：Professional Phase 1/2/3 + MS0–MS5-IDENTITY-C 全部完成（合同/提交/台账可查）。
- Gate Test 10 项均有直接测试证据（model chain、connection/credential revision frozen、multi reference、unknown slot、mode、retry 幂等、resume 存活），详见 `docs/plans/professional-program-v2/gate-reports/PHASE4-MERGE-GATE-AUDIT-REPORT.md`。
- **未自行宣布 merge gate 通过**：通过是独立 Gate 决策（需 Owner 确认）。

## 下一唯一目标

待 Owner 确认 Phase 4 Merge Gate 通过后，进入 **P4 Manual Production Alpha**（P4-01 WorkbenchExecutionPlan / CapabilityGap / ControlTranslation / PlannedReference 等，07 §16），继续沿七方案顺序串行推进。

## 当前 Task Contract

`docs/plans/professional-program-v2/task-contracts/P3-PHASE3-SCENE-WORKBENCH.md` 已完成并记录证据。下一项为 Phase 4 Merge Gate 审计（单独 Gate）与 P4 合同，继续沿七方案顺序串行推进。

## Phase 4 Merge Gate B06：CI / PostgreSQL 集成加固（MG-P4-CI-POSTGRES-FIX，进行中）

- 迁移 `20260826_0044` 从空 PostgreSQL 无法应用（`asset_tag_links` 无 `project_id` 却建基于 `project_id` 的 RLS 策略）；已改为经 `asset_tags.project_id` 的 EXISTS 子查询（与 `character_references` 联结表模式一致），策略名不变，downgrade 原样可用。0041–0044 此前从未在任何真实 PG 应用（本地栈 DB 停在 `20260826_0040`）。
- ci.yml：`on:` 增加 `workflow_dispatch`；`postgres-integration` job 增加 `TEST_PG_ENABLED=1`（此前全部 skip 导致 `--fail-on-skip` 失败）。
- `test_catalog_migration_pg.py` head 断言 `20260826_0040` → `20260826_0044`；`test_execution_model_resolution_pg.py` 构造真实 `EncryptedProviderCredential` 引用（SQLite 不强制 FK 掩盖了该问题）。
- 本地真实 PostgreSQL 验证：`alembic upgrade head` 从空库成功到 `20260826_0044`；integration `23 passed / 1 warning`（`--fail-on-skip` 无 skip）；后端 unit `746 passed / 1 warning`；ruff/mypy(196)/compileall/offline migration SQL/guardrails policy/directory compliance 全部通过。
- 远端 CI/Security 待新 HEAD 的 GitHub Actions 结果；B06 不自行宣布通过，Phase 4 Merge Gate 通过仍需 Owner 确认。

## Phase 4 Merge Gate B06：Security filesystem-scan 修复（MG-P4-SECURITY-GREEN，进行中）

- 远端 CI 已在 `d04e23b` 全绿（postgres-integration 真实运行、backend-unit 746、platform-baseline 3 OS、frontend、smoke、litellm、policy 全 success）。
- Security 唯一失败：`filesystem-scan`（trivy 0.70.0）命中 `backend/uv.lock` 的 pytest 8.4.2 → CVE-2025-71176（MEDIUM、dev-only、修复版 9.0.3）；从 code-scanning SARIF 确认仅此 1 条。
- 根因修复：pytest `>=9.0.3,<10`（解析到 9.1.1）+ pytest-asyncio `>=1.4.0,<2`（解析到 1.4.0，因 0.25.3 限定 pytest<9）；`uv lock` 解析 71 包成功。
- 验证：本地全量 unit `746 passed / 1 warning`（pytest 9.1.1 下与升级前持平）；真实 PG integration `23 passed`；ruff/mypy(196)/guardrails policy 通过；trivy 0.70.0 复现 CI 命令（sarif、HIGH/CRITICAL、ignore-unfixed、exit-code 1、干净检出）退出 0。
- 远端 `dc10525` 上 push 触发 CI + Security 全部 success（REQUIRED_CHECKS 全绿，含 postgres-integration 与 trivy filesystem-scan）；唯一遗留为 PR 触发 Security 的 `dependency-review`（仓库未启用 Dependency graph，设置项、8/21 起存在、不在 REQUIRED_CHECKS 内，需 Owner 启用或明示接受）。B06 不自行宣布通过，Phase 4 Merge Gate 通过仍需 Owner 确认。

## 全自动 T0–T9 收口（总控提示词执行，2026-08-27）

- T0：`BASE_HEAD=2d9f6ed`、`MIGRATION_HEAD=20260826_0044`、工作区干净。
- T1–T7：MS1-R / MS1-C / No Silent Fallback / Credential Revision / Connection Revision / Execution Identity Freeze / Reference-Multi-Mode-Runtime 回归，全部有直接测试证据（MG-P4 focused `110 passed`）。
- T8：最新 HEAD `2d9f6ed` 上 push CI=success、PR CI=success、push Security=success、PR Security=success；启用 GitHub Dependency graph（仓库设置）后 `dependency-review` 由失败转通过，PR #12 rollup 全绿、`mergeStateStatus=CLEAN`。
- T9：`PROFESSIONAL_PHASE_4_MERGE_GATE = PASSED`、`READY_FOR_PHASE_4 = YES`、`READY_FOR_USER_REVIEW = YES`。
- 报告：`docs/reviews/PHASE4_MERGE_GATE_AUTONOMOUS_RUN_REPORT.md`。
- 按总控提示词 §30：停在 MG-P4，不进入 Phase 4；Agnes + DeepSeek 真实模型权限已放开，供 Phase 4 Golden/Negative/Resume 使用。

## Phase 4 P4-01 WorkbenchExecutionPlan（已完成，提交 `8a4ed20`）

- 新增 `backend/app/production/execution_plan.py`（纯 Pydantic）：`WorkbenchExecutionPlan` / `CapabilityGap` / `ControlTranslation` / `PlannedReference`。
- `PlannedReference` 替换 plan 期 `ResolvedReference`，避免与 `app.providers.runtime.ResolvedReference` 冲突（07 §16）。
- Plan 携带 `ExecutionModelResolution` + `mode_id` + connection/credential revision identity + `TranslationReport`；`freeze()` 生成确定性 fingerprint（P4-07 重校验用）。
- 验证：focused `8 passed`；全量 unit `754 passed`（+8）；ruff/mypy(197)/compileall/guardrails policy 通过。

## Phase 4 P4-02 ReferencePlanCompiler（已完成，提交 `5241c31`）

- 新增 `backend/app/production/reference_intents.py`：业务 purpose → ModelManifest input slot 翻译，分类 exact / approximate / unsupported。
- 禁止静默丢弃：unsupported 引用全部保留在 `planned_references` 并上升为 `CapabilityGap`（fatal）；approximate 需显式 `accept_approximations` 才接受（否则 warning gap）。
- 覆盖 03 §32 全部用例：generic image ref、first/last、multi reference 保序、unsupported video ref、purpose approximate、ref count exceed、mutually exclusive。
- 验证：focused `7 passed`（+P4-01 8 = 15）；全量 unit `761 passed`；ruff/mypy(198)/compileall 通过。

## Phase 4 P4-03/MS8 Dynamic Model Controls（已完成，提交 `d1bbc06`）

- 新增 `frontend/src/features/model-controls/`：`ModelPicker`、`DynamicCapabilityForm`（含共享 `OptionField`）、`AdvancedModelOptions`、`ReferencePurposeEditor`、`index.ts`。
- 全部控件形状来自 `CapabilitySpecRead`（enum→select、slider(min/max)→slider、boolean→switch、conditional 联动、mutually exclusive 禁用）；无任何 provider/model 名分支。
- 复用既有 `manifestOptions.ts`（`uiComponentFor` / `allowedValuesFor`）。
- 验证：vitest focused `7 passed`；全量 vitest `21 files / 79 passed`；`tsc --noEmit`、`vite build` 通过；eslint 0 errors（1 Fast Refresh warning）。

## Phase 4 P4-04 Model Profile 简化（已完成，提交 `3fcc83c`）

- `SIMPLE_MODE_SLOT_GROUPS` 增加 `voice: ["audio.tts"]`；`SimpleModeSelection` 增加 `voice?`。
- `ModelProfileSettings.tsx` Simple 模式显示四组：默认语言 / 图片 / 视频 / 声音模型；`bindings` 仍为单一 truth。
- 验证：vitest `modelProfile.test.ts` 4 passed；全量 vitest `79 passed`；tsc/build/eslint 通过。

## Phase 4 P4-05 WorkbenchExecutionService（已完成，提交 `7990899`）

- 新增 `backend/app/production/workbench_execution.py`：Build Plan（ExecutionModelResolver + P4-02 compiler + P4-01 plan.freeze）→ Freeze inputs → GraphService（scope_type=shot）→ NodeRun（status=queued）→ snapshot（plan_fingerprint、无 secret）→ worker queue 派发。
- 禁止项落地：无 direct Provider HTTP、无 `require_legacy_execution_allowed`/BudgetAuthorization/AgentApproval、无自动 fallback；fail-closed（模型 UNAVAILABLE / capability gap 未接受 → 不建 NodeRun）。
- 计划输出补齐 connection revision / credential revision identity（查当前 `ProviderConnectionRevision`）。
- 验证：focused `6 passed`；全量 unit `767 passed`；ruff/mypy(199)/compileall 通过。

## Phase 4 P4-06 Workbench Graph（已完成，提交 `b9c5be8`）

- GraphService `scope_type=shot` + `SHOT_PIPELINE_TEMPLATE_KEY` 往返（create → materialize → publish）显式测试；nodes 含 keyframe/video；非法 scope_type 拒绝。
- 未重写 `shot_pipeline.py`。
- 验证：`test_graph_service.py` 8 passed；全量 unit `769 passed`；ruff/mypy 通过。

## Phase 4 P4-07 New Execution API（已完成，提交 `41f7c23`）

- `POST /projects/{id}/shots/{sid}/execution-plan`：预览冻结计划，不创建 NodeRun。
- `POST .../executions`：`Idempotency-Key` 落 NodeRun idempotency_key；服务器重校验 plan fingerprint（不匹配→422）与 expected shot version；成功后创建 queued NodeRun 并 commit。
- 修复真实问题：端点未 commit 导致 NodeRun 回滚（已加 `session.commit()`）。
- 验证：API focused `2 passed`（+服务 6 = 8）；全量 unit `771 passed`；ruff/mypy 通过。

## Phase 4 P4-08 Keyframe Formal Selection（已完成，提交 `97d32e6`）

- `formal_selection.py`：`set_formal_keyframe`（校验 artifact 由该项目该 shot 的 keyframe NodeRun 产生，设置 `formal_keyframe_artifact_id` + version bump + 乐观并发）+ `require_formal_keyframe`（无正式关键帧 fail-closed，禁止 latest-image 兜底）。
- API `POST .../formal-keyframe` 已加。
- 验证：focused `4 passed`；全量 unit `775 passed`；ruff/mypy(200) 通过。

## Phase 4 P4-09 Video Execution（已完成，提交 `3258aaf`）

- `stage="video"` 的 build_plan 强制正式关键帧（无则 fail-closed，禁止 latest-image 兜底）；正式关键帧作为 first_frame 引用注入计划。
- `create_and_dispatch` 复用每 shot 一张图（P4-06 语义）+ publish-if-draft；`set_formal_video`（校验 video-run artifact）+ `POST .../formal-video` 端点。
- 修复真实发现：图 (project,scope) 唯一约束 → 复用；复用图已 published → 仅 draft 时 publish；`require_formal_keyframe` 异常包装为 `WorkbenchExecutionError`。
- 验证：focused `14 passed`（+4 新用例）；全量 unit `779 passed`；ruff/mypy(200) 通过。

## Phase 4 P4-10 Execution Trace（已完成，提交 `0144944`）

- `trace.py`：`build_execution_trace` 从 NodeRun.input_snapshot（workbench_plan）+ ProviderOperation（actual/redacted request_summary）+ Artifact 组装结构化轨迹。
- API `GET /projects/{id}/runs/{run_id}/trace` 已加。
- 验证：focused `3 passed`；全量 unit `782 passed`；ruff/mypy(201) 通过。

## Phase 4 P4-11 ProviderOperation Summary 标准化（已完成，提交 `933e032`）

- `request_summary.py`：`build_request_summary` / `normalize_request_summary`（恒含 translation_report / effective_request_redacted / reference_delivery / semantic_fingerprint，保留 legacy 键兼容）+ `validate_no_secrets`（fail-closed）。
- 接入统一提交路径（product_path.py）operation 创建。
- 验证：focused `6 passed`；全量 unit `788 passed`；ruff/mypy(202) 通过。

## Phase 4 §17 Golden Professional Test（脚本交付 `51b06b9`，实跑环境门控：重建栈+迁移0044+真实付费）

- 交付 `scripts/prove_phase4_golden_professional.py`：驱动新 workbench API 垂直切片（execution-plan/executions/formal-keyframe/formal-video/trace），断言身份链 requested==resolved==binding==actual、first_frame=正式关键帧、manifest/connection/credential revision frozen、trace secret-free、负向 fail-closed（POST=0）；证据 JSON 输出到 --out。
- 编译/lint 通过（py_compile + ruff OK）。
- **实跑 Runbook（环境未就绪，未执行真实付费调用）**：
  1. `docker compose -f docker-compose.build.yml build`（或 dev 构建）重建 api/worker 镜像为最新代码；
  2. 对栈 postgres 执行 `alembic upgrade head`（0040→0044，前向增量迁移）；
  3. `docker compose up -d` 重启；确认 `GET /api/v1/status` 与 workbench 端点可用；
  4. `backend\.venv\Scripts\python.exe scripts\prove_phase4_golden_professional.py --out tmp\golden-p4-evidence.json --base-url http://127.0.0.1:8080/api/v1`。
- **当前 Blocker**：运行中栈为旧镜像（迁移停在 0040、无 Phase 4 端点）；重建 + 迁移实时 dev 库 + 真实 Agnes 付费调用需环境操作；Agnes key 有效性未探测。未伪称通过。

## Phase 5 P5-01 Experiment ORM（已完成，提交 `b481d82`）

- `ProductionExperiment`（project 级，idempotency_key 唯一）+ `ShotExperiment`（experiment+shot 唯一；source version/director state/prompts/references/model_overrides/common_controls/comparison/结果 artifact）。
- 迁移 `20260827_0045`（新 head）：两表 + project RLS（FORCE）；真实 PG 空库 upgrade 到 0045 成功；offline SQL 正常。
- 验证：focused `3 passed`；全量 unit `791 passed`；ruff/mypy(202) 通过。

## Phase 5 P5-02 shot_experiment Graph Scope（已完成，提交 `678cb24`）

- `GraphService.create_graph` 允许 `scope_type="shot_experiment"`。
- 测试证明：正式 shot 图 A 与 shot_experiment 图 B 完全独立（发布 B 后 A.current_version 不变）。
- 验证：focused `9 passed`；全量 unit `792 passed`；ruff/mypy(202) 通过。

## Phase 5 P5-03 Create Experiment API（已完成，提交 `7b19c3b`）

- `experiment_service.py`：`create_experiment`（单 shot：复制 version/director_state/prompts/references/common_controls 到 ShotExperiment；scene：指定 shots 全建；idempotency 去重；不动正式 Shot）。
- API `POST /projects/{id}/experiments`（端点函数更名避免与既有冲突）。
- 验证：focused `3 passed`；全量 unit `795 passed`；ruff/mypy(203) 通过。

## Phase 5 P5-04 Model Swap Experiment（已完成，提交 `6e03f0b`）

- `recompile_controls_for_model`（纯函数）：保留 B manifest 声明的语义 common controls；丢弃 A 的 native options（显式列出）；reference 按 B 用 P4-02 重编译；unsupported 显式暴露。
- `create_model_swap_experiment`：对带 model_override 的 ShotExperiment 解析 B manifest 重编译并写入 comparison.model_swap_recompile。
- 验证：focused `6 passed`（3 P5-03 + 3 P5-04）；全量 unit `798 passed`；ruff/mypy(203) 通过。

## Phase 5 P5-05 Experiment Compare UI（已完成，提交 `9c5a430`）

- `frontend/src/features/experiments/ExperimentCompare.tsx`：正式/实验列对比 image/video/model/prompt/translation warning/references；无 provider/model 名分支。
- 验证：vitest `3 passed`；全量 vitest `82 passed`；tsc/build/eslint 通过。

## Phase 5 P5-06 Adopt Experiment API（已完成，提交 `5ac0316`）

- `adopt_experiment(scope)`：keyframe_only（只更正式 keyframe，保留旧正式 video → stale-keyframe 状态）、current_result_only/full_shot（keyframe+video）、keyframe_and_rerun_video（清空正式 video 待重跑）、design_only（复制设计输入）。
- `POST /projects/{id}/experiments/{id}/adopt` 端点已加；采纳记录写入 ShotExperiment.comparison.adoption。
- 验证：focused `8 passed`（3 P5-03 + 3 P5-04 + 2 P5-06）；全量 unit `800 passed`；ruff/mypy(203) 通过。

## Phase 5 Gate（03 §51，已完成，提交 `ddf0415`）

- `test_phase5_gate.py` 6 项全过：实验不覆盖正式；换模型不复制 raw payload；A/B 并存；部分采纳（keyframe_only 保留旧 video）；旧正式保留历史血缘；场景实验按 shot 采纳。
- 验证：focused `6 passed`；全量 unit `806 passed`；ruff/mypy(203) 通过。

## Phase 6 P6-02/03 Review UI（进行中）

- P6-01（ReviewAnnotation ORM）已由既有 `delivery/models.py` + `review.py` API + 迁移 0037/0039 满足（审计确认，不重做）。
- 新增 `frontend/src/features/review/`：`MediaReviewCanvas.tsx`（归一化矩形/点批注，坐标 0..1 输出）、`VideoReviewTimeline.tsx`（时间点/范围/文字）。
- 验证：vitest `3 passed`；全量 vitest `85 passed`；tsc/build/eslint 通过。