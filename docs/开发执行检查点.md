# DramaForge 开发执行检查点

**状态：P0 功能候选版（正式证据待按当前提交重跑）**

**最近更新：2026-07-22**

## 0. 诚实基线

| 事实 | 说明 |
|---|---|
| 产品标签 | **P0 功能候选版**。历史正式证据早于当前代码，且当前基线存在 Ruff 与 PostgreSQL Worker 并发缺口，不得继续声明完整 P0 MVP。 |
| 正式本地栈 | `scripts/start_p0_wsl_stack.sh`：WSL PG+Redis+MinIO+API+Arq；**不**设 `DRAMA_FORCE_MEMORY_STORE=1`。 |
| Docker Compose / S5 私有化部署演练 | 仍可按运维手册补跑；**本轮 MVP 证据以非 Docker 正式栈为准**（与 Goal 计划一致）。 |
| 假 Adapter | 仅 `APP_ENV=test` unit；正式路径 fail-closed 或受审计手工媒体。 |
| InsightFace | WSL venv `insightface+onnx` buffalo_l 已可用（512-d embedding smoke）。 |

## 1. 历史验收证据

以下记录仅说明 2026-07-21 的运行结果，不代表当前 `main` 已通过：

| 证据 | 结果 |
|---|---|
| `GET /health` | `status=ok` `db=up` |
| `scripts/prove_p0_mvp_formal.py --n 10` | **ok=true**：10 Shot 全必需节点、approve_ok=10、failed=0、package.zip 哈希一致、真实 MP4 |
| `scripts/run_p0_section31_gate.py --evidence multi_shot_chain.json` | **pass=23 fail=0 blocked=0 `p0_mvp_complete=true`** |
| 后端 integrity unit | 16 passed |
| 前端 typecheck / vitest / build | 全绿 |
| InsightFace | `docs/acceptance/insightface_status_latest.json` backend=insightface+onnx |

产物哈希（formal proof）：见 `docs/acceptance/multi_shot_chain_latest.json` 与 SCRATCH `export_hashes.txt`。

## 2. 本轮关键实现

- commit-then-enqueue；假 queued 禁止
- APPROVE_GATE / EXPORT_GATE / 角色门禁 / package.zip 元数据
- 手工媒体完成 NodeRun（零 Provider 成本）
- pure review 节点无需 Provider
- Artifact 同 hash 复用（uq 冲突）
- §3.1 gate：formal evidence + unit 证明 + InsightFace 状态文件
- FFmpeg 真实 MP4 导出

## 3. 口径

- **当前只可标记：P0 功能候选版**。
- 正式报告必须绑定精确源码提交且工作树干净；任意 `FAIL` 或 `BLOCKED` 都不能声明 P0 MVP 完成。
- Docker/S5 运维演练继续延期；非 Docker WSL 正式栈仍是发布前 Gate。

## 4. 启动

```powershell
wsl -d Ubuntu-24.04 -- bash /mnt/d/调研/dramaforge/scripts/start_p0_wsl_stack.sh start
cd frontend; npm run dev -- --host 127.0.0.1 --port 5173
```

## 5. 当前唯一执行任务

```text
Task ID：REPO-GUARDRAILS-BOOTSTRAP
状态：IN_PROGRESS
完成效果：
  1. 所有代码通过 GitHub PR、必需 CI 和用户审批后才能进入 main。
  2. 写入 Agent 使用 agent/<task-id> + .worktrees/<task-id>，直接推送 main 被本地 hook 拒绝。
  3. CI 的真实 PostgreSQL 测试不可静默跳过，并证明并发 Worker 只消费一次 NodeRun。
  4. 当前 42 项 Ruff 错误清零，后端、前端和 smoke 门禁全绿。
范围：
  GitHub workflow/模板/CODEOWNERS、本地 Agent 控制脚本、依赖锁、证据绑定规则、
  Ruff 基线修复、Worker 原子领取及其测试。
非范围：
  Docker 产品部署、付费 Provider 调用、P1/P2 功能、重新声明 P0 MVP 完成。
前置条件：
  main=b63c0a1，origin/main 同步；私有仓库启用 GitHub Pro 后配置 Ruleset。
验收证据：
  六个稳定 CI job 全绿；真实 PostgreSQL 集成无 skip；本地 guardrail 拒绝测试通过；
  git diff --check 通过；Bootstrap PR 由 @zwb2002-yjy 审批合并。
文件所有权：
  .github/**、.githooks/**、.agent-control/control.ps1、scripts/**、
  backend/**、frontend/package*.json、docs/开发执行检查点.md、
  AGENT_EXECUTION_PROTOCOL.md、agent.md、README.md。
COMPLETED 条件：
  上述自动化门禁和测试全部通过并形成可复核 commit；未配置远端 Ruleset 时只能记 PAUSED，
  不能记 MERGED 或流程启用完成。
```
