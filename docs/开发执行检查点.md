# DramaForge 开发执行检查点

**状态：P0 功能候选构建，待修复后复验 · 禁止宣称验收通过 / 完整 P0 MVP**

**最近更新：2026-07-21（检验不合格后的阻断项修复轮）**

## 0. 诚实基线

| 事实 | 说明 |
|---|---|
| 产品标签 | **P0 功能候选构建，待修复后复验**。不得标记「P0 功能候选版验收通过」或「完整 P0 MVP 完成」。 |
| Docker / S5 | **DEFERRED**，不纳入本轮修复。 |
| 正式本地栈 | `scripts/start_p0_wsl_stack.sh`：WSL 内 PG+Redis+MinIO+API+Arq+dispatcher；**不**设置 `DRAMA_FORCE_MEMORY_STORE=1`。 |
| 假 Adapter | 仅 `APP_ENV=test` / pytest；正式路径 fail-closed。 |
| S0-A InsightFace | 未校准盖章时 Gate **BLOCKED**，不得当验收。 |

## 1. 检验阻断项修复状态（本轮）

| # | 阻断 | 修复 |
|---|---|---|
| 1 | 审核门禁可被绕过（无 approved 导出全部） | `export_service`：`require_approved` 且零 `review_passed` → `EXPORT_GATE` 拒绝 |
| 2 | flush 后投递竞态 + 假 queued | `enqueue_node_run_only`：**先 commit 再 Arq**；失败 → NodeRun `failed`/`QUEUE_UNAVAILABLE`；`start`/`rerun` 不再吞错返回 200+queued |
| 3 | Redis Stream 静默回退 | `RedisStreamPublisher`：非 test 环境 fail-closed `OUTBOX_PUBLISH_FAILED` |
| 4 | 空 Shot 可 approve | `approve_shot`：`APPROVE_GATE` 要求必需节点完成 + 媒体 Artifact + face/continuity 未 blocked |
| 5 | Viewer 可写 / 手工上传无审计 | `require_project_role`：produce/review/export 角色集；manual `delete_reason` 保留 by/note |
| 6 | package Artifact 元数据错 | Artifact 指向 `package.zip`，mime=`application/zip`，hash/size 对齐 ZIP |
| 7 | multi-shot / Gate 偏弱 | `prove_multi_shot_worker` 默认全必需节点、零失败；Gate 3.1.10 要求 10 Shot 全节点+审核+产物 |

## 2. 验证（本机已跑）

- `pytest tests/unit/test_p0_integrity_gates.py` + formal/product 相关：**19 passed**（完整性回归）
- 全量 unit 与现场 WSL 复验：**待本轮提交后重跑**
- Docker/S5：**DEFERRED**

## 3. 口径

- 可标记：**P0 功能候选构建，待修复后复验**
- 不可标记：**P0 功能候选版验收通过** / **完整 P0 MVP 完成**
- `p0_mvp_complete` 仍须由 Gate 脚本全 PASS 且无 BLOCKED 才为 true

## 4. 当前唯一执行任务

```text
Task ID：P0-INTEGRITY-FIX-REVERIFY
完成效果：阻断项代码已修；回归测试通过后在正式栈复验；仍不得宣称验收通过，除非 Gate 全绿且用户确认
范围：enqueue 事务序、approve/export 门禁、角色、Redis 发布、package 元数据、Gate 收紧
不负责：Docker/S5、InsightFace 校准盖章、真实 BYOK 费用生产
验收证据：test_p0_integrity_gates + 相关 unit；可选 WSL multi-shot/export
完成定义：代码合并 + 回归绿 + 检查点诚实标签
```

## 5. 启动（用户）

```powershell
wsl -d Ubuntu-24.04 -- bash /mnt/d/调研/dramaforge/scripts/start_p0_wsl_stack.sh start
cd frontend; npm run dev -- --host 127.0.0.1 --port 5173
```

健康：`http://127.0.0.1:8010/health` → `status=ok` 且 `db=up`。
