# DramaForge 开发执行检查点

**状态：closable Gate 证据套件绿（unit + matrix + export download + SSE/Outbox）/ PG integration 因 Docker 引擎未起阻塞 / 明确不写「P0 MVP 完成」**

**最近更新：2026-07-21**

## Skeptic 缺口修复

| 缺口 | 处理 |
|---|---|
| 人脸 identity match | `pipeline.FirstFramePipeline` 改为 probe vs distinct canonical 字节；`face_review_hook` 迁入 `consistency/face_review.py` 消除循环依赖；产品路径 `shot_p0` 已是双源 |
| Worker/Export 空 store | 全程 `get_object_store()` 单例；export 默认 `store or get_object_store()`；matrix 断言 Worker 写入字节可被 export 读取 |
| 验收矩阵过窄 | 扩展 `test_p0_gate_matrix`：剧本导入、canonical 拒绝、budget、SSE resume、Outbox 死信重放、授权下载、single_flight、face mismatch |
| 证据 | `scripts/run_p0_gate_evidence.py` → implementer scratch |

## 验证命令与结果

```text
python scripts/run_p0_gate_evidence.py --scratch {SCRATCH}
# p0-async-keyframe.txt exit=0
# p0-10shot.txt exit=0
# p0-export.txt exit=0
# p0-gate-matrix.txt exit=0
# p0-backend-pytest.txt exit=0  (91+ unit)
# p0-frontend.txt exit=0
```

## §3.1 closable（unit/fake，非 PG RLS）

| 条件 | 证据 |
|---|---|
| 2 start_project 零文本 Op | matrix |
| 8 剧本导入 10 Shot | test_script_import + golden |
| 9 canonical 拒绝 | matrix + require_canonical |
| 10 10-shot Graph | golden_path |
| 11 双源人脸 | face_two_source + mismatch |
| 12 四层连续性 | test_continuity |
| 13 字幕局部重跑 | product_path_shipped |
| 14 cache | matrix |
| 15 single_flight | matrix |
| 16 budget | matrix |
| 17 SSE Last-Event-ID | matrix + outbox_sse |
| 18 export 哈希 + 授权下载 | matrix export + download grant |
| Outbox 死信重放 | matrix + outbox_sse |

## 残留（禁止盖「P0 MVP 完成」章）

1. **PostgreSQL RLS 集成** — Docker Desktop 引擎未就绪（`com.docker.service` Access Denied）  
2. **S0-A FAR/FRR** — 合法 InsightFace 样本不足  
3. **Playwright** 浏览器 E2E  
4. **真实 multi-provider BYOK 浸泡** — 需用户授权  

## 唯一下一步

管理员启动 Docker → compose postgres/redis/minio → alembic → `pytest tests/integration` → 更新本检查点。
