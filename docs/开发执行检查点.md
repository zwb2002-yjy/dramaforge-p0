# DramaForge 开发执行检查点

**状态：P0 未完成 · 按冻结合同标准推进 · 禁止假 Adapter 冒充验收 · 明确不写「P0 MVP 完成」**

**最近更新：2026-07-21（用户要求：按标准做 P0）**

## 0. 诚实基线（纠正此前演示）

| 事实 | 说明 |
|---|---|
| P0 MVP | **未完成**。`01` §3.1 端到端与产品工作台未达标。 |
| 假 Adapter 10-shot | 仅允许在 **pytest / 契约测试** 中使用；**不得**作为用户验收主路径。 |
| 当前前端 | 薄壳：手工 Brief/Plan 文本框 + 黄金路径批处理按钮，**不是**可试产工作台。 |
| 真 BYOK | 用户已授权 Agnes（图/视频）+ baizhi 文本 LLM（deepseek-v4-flash）；开发路径必须可用。 |
| S0-A | InsightFace 校准 **未通过**；不得盖章一致性 Gate。 |

## 1. 标准 P0 完成定义（唯一口径）

来自 `01` + 产品路线图 + `agent.md`：

1. 团队可从创意/剧本建 **正式 Project**，快速/专业 **同一项目**。
2. Brief/Plan：**Agent 生成（BYOK）或手工**，两级确认后白名单物化。
3. 资产与主角 canonical；`shot-p0-v1` 全节点可观察、可失败、可审核、可局部重跑。
4. 真实 Provider（BYOK）或 **受审计手工媒体**；禁止静默假成功。
5. 交付：可校验 MP4/SRT/素材包/timeline；成本与审计可回链。
6. 非功能演练与 §3.1 黄金样本全过 → 才写「P0 MVP 完成」。

## 2. 工程任务队列（标准顺序）

| ID | 状态 | 完成效果 |
|---|---|---|
| **P0-STD-0** | **COMPLETED** | 检查点纠正；禁止假 Adapter 冒充验收 |
| **P0-AGENT-1** | **COMPLETED（契约）** | API `brief/generate` + `plans/generate`；AgentRun+ProviderOperation；无 Key 显式错误 |
| **P0-UI-S2** | **COMPLETED（UI 壳+步骤）** | 快速模式：Agent/手工 Brief·Plan → 确认 → Worker 首帧 → 预览链接 |
| **P0-UI-S4** | **READY / 当前** | 专业模式：非假黄金主路径；Shot 操作、导出下载、与快速模式衔接 |
| **P0-STACK-1** | READY | development 常驻 + 真 BYOK 键加载（start_demo_api_wsl 已 source .env） |
| **P0-GATE-§3.1** | blocked | 真路径 10 Shot + 非功能；S0-A 残留 |

## 3. 本轮完成证据（P0-AGENT-1 / P0-UI-S2）

- API：`POST .../brief/generate`、`POST .../plans/generate`；`GET .../artifacts/{id}/content`
- 测试：`pytest tests/unit/test_agent_brief_plan.py` + product_path → **6 passed**
- UI：`projects.$projectId.quick.tsx` 分步工作流；专业页标注黄金路径非验收主路径
- **仍非 P0 MVP**：专业工作台、真 10 Shot 生产审核交付、S0-A、§3.1 未过

## 4. 当前唯一执行任务

```text
Task ID：P0-UI-S4 + P0-STACK-1
完成效果：专业模式可按剧本导入 → Shot 列表 → 导出下载；栈默认 development+BYOK。
```

## 4. 残留（禁止盖章）

| 残留 | 状态 |
|---|---|
| S0-A InsightFace 阈值 | 未通过 |
| §3.1 全量端到端 | 未通过 |
| 专业工作台完整体验 | 未通过 |
| FFmpeg 真 MP4 可重现 | 视环境 |

## 5. 唯一下一步（执行中）

实现 `P0-AGENT-1` + `P0-UI-S2` 竖切并跑测试。
