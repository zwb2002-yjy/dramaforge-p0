# DramaForge 开发执行检查点

**状态：LIVE / 当前实现事实与阻断项**

**最近更新：2026-08-20**

本文件只记录当前代码、验证结果和下一目标。产品方向看 [`../DramaForge总开发文档.md`](../DramaForge总开发文档.md)，实现合同看 [`current/`](current/)，发布证据看 [`runbooks/release-gate-board.md`](runbooks/release-gate-board.md)。删除的旧规划和旧检查点由 Git 历史保留，不再参与执行。

## 当前基线

| 项目 | 当前事实 |
|---|---|
| 分支 / 运行候选 | `dev` / `1d921fb5b332f0809f9b9924872e619030459de2` |
| 产品状态 | Director Workflow 第一轮功能候选，不是稳定发布候选 |
| 当前实现 | 三入口、四阶段、四确认、版本/变更影响、预算、试拍、正式生产、局部修复和四项交付命令 |
| 生产底座 | Production Graph、NodeRun、ProviderOperation、Artifact、Outbox/Arq、对象存储和 FFmpeg |
| Provider | DeepSeek 文本可走 LiteLLM V3；Director 图片/视频强制 Unified；非 Director 历史媒体兼容分支仍保留 |
| 真实媒资 | Agnes 历史 `shot-1`：121 帧、24fps、约 5.04 秒，因肢体归属/遮挡错误被 Q3 人工拒绝；当前候选 Agnes 图片 v2 `image_i2i` 账号 Probe 已单次通过，但尚无新的 Director I2I Artifact |
| 前端 | Visual 2.0 已覆盖项目大厅、项目总览、快速创作与专业生产；项目路由不再挂载旧 WorkstationShell；试拍页可直接审阅 Canonical、关键帧、视频、音频、首/中/末帧、ProviderOperation、费用状态和已知限制 |
| Docker | `1d921fb` 同源候选运行；API、Dispatcher、两个 Worker、Frontend、PostgreSQL、Redis、MinIO、LiteLLM 和 LiteLLM DB 均健康；数据服务容器/卷未重建 |

## 当前验证

| 验证 | 结果 | 边界 |
|---|---:|---|
| Unified/Provider 聚焦后端 | 115 passed | 证明强制路由、严格图片/视频参数、v1/v2 生命周期、请求证据、恢复、币种与费用状态；覆盖 Probe 定价币种、媒体元数据和 Snapshot 脱敏证据 |
| Director→Artifact G2 Spy | 通过 | 真实 Director 物化、Outbox、Worker、3 次 Unified submit、重复执行不重提、引用哈希、费用和 Artifact 血缘 |
| 前端 Vitest | 56 passed | 组件和状态逻辑，含历史 Binding 不可绑定新项目、试拍媒体/执行证据、项目壳迁移 |
| TypeScript / build / Ruff / mypy | 通过 | 前端 lint 有 1 个既有 fast-refresh warning |
| Chromium Playwright | 11 passed | 包含正式生产人工验收理由和项目创建 Provider 插件 Mock |
| 后端全量 pytest | 642 passed / 16 skipped | 从当前候选工作树执行；16 项为 PostgreSQL/真实 Provider/外部环境未配置的显式 skip |
| 隔离 PostgreSQL migration | 2 passed | `0028` upgrade → `0027` downgrade → `0028` re-upgrade，v1/v2 行均保留且 lifecycle 正确 |
| Compose 同源 smoke | 通过 | API/Worker/Frontend 镜像均标记 `1d921fb...`；部署栈单次受限 Agnes `image_i2i` 账号 Probe 已将 Provider evidence `6→7`，Binding 已 account-verified；费用仍为 `not_reported` / CNY |

## 架构确认状态

现行确认表在 [`current/02-运行时与领域架构.md`](current/02-运行时与领域架构.md#14-架构确认表)。

- 架构决定 AD-01 至 AD-09 已确认；单一人脸阈值 Gate 已明确否决；
- AP-01 至 AP-05 为 `PARTIAL`，需要当前干净候选和真实链证据；
- AP-06 真实完整作品为 `OPEN`；
- 旧资产库、故事板、候选治理和时间线保留为后续扩展层，不阻塞首版。

## 当前产品与效果缺口

1. 项目级 Canonical 已完成本地实现：试拍为每个角色生成一次，接受后提升为 CharacterReference，正式生产以 cached NodeRun 跨镜头复用；仍需真实 Provider 请求证明同一 Artifact 哈希进入三个镜头；
2. Director 图片和视频已强制走 Unified Provider Path；非 Director 兼容分支仍需等真实 Agnes 验收后清理；
3. Agnes 视频已严格限制为 9:16、24fps、121 帧/约 5 秒和无原生音频；图片 v2 已冻结官方 `1K + 9:16` 合同与 `736x1312` 参考输出；在 `2026-08-20` 的单次、`0.01 CNY` 上限虚构 Canonical `image_i2i` 账号 Probe 中，Binding 已获得 `account_verified`，但仍未质量门禁，且这不是 Director I2I 的 Artifact 证据；
4. `dialogue-post-dub-shot-v1` 没有真正 lip-sync 节点，当前只是视频、TTS、字幕合成后人工验收；
5. 试拍审阅 UI 已具备完整媒体、执行证据和限制呈现，但仍缺真实 Agnes 试拍数据的闭环验收；
6. 新 Director 尚无一部 15–30 秒完整真实作品，也没有三名目标用户无人代操作结果。

## 下一唯一目标

**M1：完成 Unified G4 的真实 Director I2I 证明。账号 Probe 已通过；下一阻断是单次独立费用授权下的真实 Unified 请求、Artifact、血缘与质量证据。**

完成条件：

1. 候选 `1d921fb` 已通过后端 `642/16`、前端 `56`、Chromium `11`、Ruff、mypy 和 build；
2. Visual 2.0 项目大厅、项目总览、快速创作与专业生产路由已经部署到 `http://localhost:8080/`；
3. Director 强制 Unified、Agnes 图片/视频严格合同、EffectiveRequest/TranslationReport、引用匹配和未报告费用语义已完成；
4. Director 授权物化到 Artifact 的完整 Spy smoke 与候选镜像内 Compose smoke 已通过；
5. 图片 v2 Binding `663ecf84-af9c-4316-81a7-dc1a8893e09c` 已绑定 active Catalog/hash；`2026-08-20` 已在用户明确授权的唯一一次、`0.01 CNY` 上限、无重试的虚构 Canonical `image_i2i` 账号 Probe 中通过，证据 `eb781bee-9875-48d6-aa76-50b4ce64d706`，Provider evidence `6→7`，Binding 已 account-verified。Provider 未报告成本，故为 `not_reported` / CNY，不能描述为免费；Probe 不产生 Director ProviderOperation、EffectiveRequest、TranslationReport 或输出 Artifact。脱敏证据位于 `tmp/p0-evidence/1d921fb.../real-provider/`。真实 Director I2I 仍需一份新的、明确的单次费用授权；未经该授权不调用 Provider。

G4/G5 完成后继续 Legacy 退出、Trial Evidence UI、真实局部修复与黄金样本：使用同一项目级 Canonical 完成 1 个主角、3 个镜头、15–25 秒的作品，并核对三个镜头的参考 Artifact ID/哈希一致。此前不增加第二模板、自由工作流或更多 Provider。
