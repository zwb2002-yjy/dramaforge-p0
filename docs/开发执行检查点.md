# DramaForge 开发执行检查点

**日期 / HEAD：2026-08-26 / Phase 1（P1 Professional Workspace Foundation）已完成（七方案执行中）**

## 今日唯一目标

按 `docs/plans/professional-program-v2/README.md` 的唯一顺序完成 MS5-IDENTITY 三段（A/B/C）后，进入 Phase 1（Workspace State / Scene-Shot 专业字段 / Shot Design API / Project Shell），因为 07 §15 的 Phase 4 Merge Gate 要求 Professional Phase 1/2/3 全部完成。

## 完成证据

### MS5-IDENTITY（已完成，含 A/B/C 三段）

- MS5-IDENTITY-A（迁移 `20260826_0041`）：credential 转为 immutable revision（`revision_no`/`supersedes_id`）；`read_credential_by_id` 与 Professional runtime/connection settings 按具体 `credential_id` 严格读取，缺失/跨 workspace fail-closed；key rotation 语义分离。
- MS5-IDENTITY-B（迁移 `20260826_0042`）：新增 immutable `ProviderConnectionRevision`；`ProviderOperation.provider_connection_revision_id` 冻结 revision；base_url/credential/protocol 变化创建新 revision，display/enabled 变化不创建。
- MS5-IDENTITY-C：typed、frozen、JSON-safe、secret-free 的 `ExecutionIdentitySnapshot`；首次提交前把同一 identity 写入 NodeRun snapshot、ProviderOperation selection_plan 与 request_summary；resume/poll/cancel 通过 `resolve_runtime_for_identity` 按冻结 revision + `read_credential_by_id` 重建 runtime，不重新提交、不重选模型；missing/mismatched fail-closed 且零 Provider 调用；legacy 无 identity 旧 operation 保留兼容路径。

### Phase 1 / P1 Professional Workspace Foundation（已完成）

- P1-01 Workspace State：`UserProjectPreference.workspace_state`（迁移 `20260826_0043`）；`backend/app/workbench/workspace_state_service.py`；`GET/PATCH /projects/{project_id}/workspace-state`（PATCH merge 语义）；前端 `useProjectWorkspaceState` hook 与根路由 last-view 恢复。
- P1-02 Scene/Shot 专业字段：`Scene.design_state`、`Shot.director_state/image_prompt/video_prompt/formal_keyframe_artifact_id/formal_video_artifact_id/formal_composite_artifact_id`、`Asset.current_version_id`；`backend/app/assets/schemas.py` 的 `SceneDesignState`/`ShotDirectorState`。
- P1-03 Shot Design API：`backend/app/workbench/shot_service.py`；`PATCH /projects/{project_id}/shots/{shot_id}/design` 带 `expected_version` 乐观并发（409 + 当前版本）；不生成媒体、不要求 Director Agent 批准。
- P1-04 Professional Project Shell：`ProjectWorkspaceShell.tsx` 在原文件上演进，导航 剧本/资产/场景/专业生产/剪辑，移除 StageStepper；新增 `/script` `/assets` `/scenes` `/edit` 路由（Phase 1 占位页），`/quick` 保持 legacy 重定向专业生产；根路由在存在 `last_view` 时恢复上次视图（`#model-settings` anchor 不重定向）。

## 验证结果

- 后端全量 unit：`727 passed / 1 warning`（含 10 个新增 P1 测试）；P1 focused（workspace/schema/concurrency）：`10 passed`。
- `ruff check app tests alembic/versions`：All checks passed；`mypy app`：Success（189 source files）；`git diff --check`、`compileall`、`scripts/check_directory_compliance.py --root .`：通过。
- 迁移链单 head `20260826_0043`，offline `alembic upgrade head --sql` 生成/编译通过。
- 前端：`tsc --noEmit`、`vite build` 通过；vitest `62 passed`；eslint 0 errors（仅既有 CreativeStage Fast Refresh warning）；Playwright `8 passed`（含新 shell 路由渲染、`/quick` 重定向）。
- PostgreSQL 集成：`TEST_PG_ENABLED` 未设置且 `127.0.0.1:5432` 不可达，integration `6 passed / 17 skipped`（PG 用例如实 skip，未伪称通过）。
- 真实 Provider 付费调用：未授权，全程零网络假 runtime 验证；`paid_provider_calls=0`。

## 已完成领域闭环（沿用此前专业工作台事实）

- 画布编辑已落到 `CanvasRevision` 持久化合同；AssetVersion、ExperimentBranch、ReviewAnnotation、OpenCut v2、DirectorBoardState 已落库并接入 UI。
- 专业镜头启动/局部重跑显式绕开旧预算 Gate；正式/实验 NodeRun 共享 Production Graph，正式线不消费未采纳实验产物。
- 旧 Director 预算 API/快照/表保留仅为历史回放兼容，专业入口不可达且不展示。

## P0/P1 缺陷与外部证据

- 当前未发现由专业切片引入的类型、构建或既有回归阻断。
- 加密备份已生成；数据库 dump 已验证可在隔离 PostgreSQL 中恢复；MinIO 对象恢复需在真实部署网络/端口条件下复核。
- Provider 真实付费媒体黄金样本、OpenCut 实际应用导入、离线/跨平台和三名目标用户无人代操作仍是外部发布证据，需绑定同一干净 release commit。

## 真实费用与外部调用

专业 live proof 明确 `paid_provider_calls=0`；真实付费媒体黄金样本仍需用户明确允许并绑定同一 release commit。

## 合同或 ADR 变化

新增 ADR 0006；新增迁移 0032–0039、0041–0043；MS5-IDENTITY-A/B/C 与 P1（Phase 1）合同已按七方案完成并记录证据。

## 下一唯一目标

审计 07 §15 Gate 清单中 Phase 2 / Phase 3 的完成度（AssetVersionReference、AssetTag、ShotReferenceBinding、@Asset 解析、Scene Wall、Scene Workspace、Shot Workbench、Canvas、Asset bindings），补足缺口后进入**单独 Gate 审计**的 Phase 4 Merge Gate（不得自行宣布 merge gate 通过），再进入 P4 Manual Production Alpha。

## 当前 Task Contract

`docs/plans/professional-program-v2/task-contracts/P1-PROFESSIONAL-WORKSPACE-FOUNDATION.md` 已完成并记录证据。下一项 READY 合同为 Phase 2（P2 Asset 版本化引用与 @Asset 解析）或按 07 §4 顺序补齐 Phase 2/3，继续沿七方案顺序串行推进。