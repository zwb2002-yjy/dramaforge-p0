# DramaForge 开发执行检查点

**状态：BOOT-0.1 完成（Docker CLI 仍缺）/ S0-A = `BLOCKED_BY_FIXTURE`**

**最近更新：2026-07-20**

**唯一开发工作区：`D:\调研\dramaforge`**

## 1. 任务目标与范围

依据根目录 `01`-`06` 冻结合同，按 `agent.md` 的 Gate 顺序将规格仓实现为 P0 MVP。

当前工程焦点：`S0-A` 视觉一致性 Spike（样本不足时诚实阻塞，不编造 FAR/FRR）。不写项目外代码（ArcReel / `D:\调研\项目` 等）。

## 2. 当前阶段

| 项目 | 状态 | 事实 |
|---|---|---|
| 代码仓边界 | 完成 | 仅 `D:\调研\dramaforge` |
| BOOT-0.1 骨架 | 完成 | API/Worker/前端/compose/质量门禁；见 git `96a9041` 起 |
| Docker 容器健康 | 环境阻塞 | 无 Docker CLI；不假装 healthy |
| GitHub 私有远端 | 待完成 | 无 `origin` |
| **S0-A 视觉一致性** | **`BLOCKED_BY_FIXTURE`** | 无合格样本；纯函数与入口已落地；报告见 `docs/spikes/s0a-face-consistency.md` |
| S0-B / S1–S5 | 未开始 | — |

## 3. S0-A 已完成操作与证据

### 3.1 路径与入口

- `scripts/run_s0_face_spike.py`：真实入口；盘点 manifest、探测 InsightFace/ONNX、不足时 exit 2 + `BLOCKED_BY_FIXTURE`
- `backend/app/consistency/face.py`：512 维 L2 normalize、pair_score、异常标签、FAR/FRR 阈值网格、fixture 充分性（供 spike 与后续 face_review）
- `fixtures/images/character_canonical/manifest.json`（空 pairs）、`manifest.schema.json`、`ACQUISITION.md`
- 报告：`docs/spikes/s0a-face-consistency.md`（脱敏；无 Embedding 向量）

### 3.2 命令与结果

| 命令 | 结果 |
|---|---|
| `python scripts/run_s0_face_spike.py`（×2，含 `--skip-model`） | exit **2**；stdout 含 `BLOCKED_BY_FIXTURE`；写入 spike 报告 |
| `backend` pytest（含 face helpers + spike entry） | **32 passed** |
| ruff / mypy（face 模块） | 通过（E501 已修） |
| InsightFace / onnxruntime | 本机 probe：`importable=False`（未装）；不阻断 `BLOCKED_BY_FIXTURE` 路径 |

Scratch 证据：`C:\Users\35344\AppData\Local\Temp\grok-goal-04916ef2ff7d\implementer\`  
（`cwd-git.txt`、`ledger-tail.txt`、`s0a-run-1.log`、`s0a-run-2.log`、`s0a-tests.txt` 等）

### 3.3 未完成（需样本后）

- ≥20 同角色 pair、≥20 异角色 pair、≥10 异常样本
- 真实 InsightFace 512-d 推理与 FAR/FRR 阈值候选
- **不得**在样本到位前宣布一致性 Gate 通过

## 4. 外部操作与成本

- 无真实 AI Provider 调用
- 无 GitHub 推送
- 无 InsightFace 模型下载费用（未安装）

## 5. 风险与禁止

- 不得编造 FAR/FRR 或阈值
- 不得把原图/Embedding 写入公开报告或日志
- 不得在仓库外写应用代码
- S0-A 阻塞不阻止 BOOT-0 补 Docker 或启动 S1（冻结合同允许）

## 6. 下一步唯一操作

1. **优先（解除 S0-A）**：按 `fixtures/images/character_canonical/ACQUISITION.md` 采集并登记样本 → 安装 `insightface`+`onnxruntime` → 重跑 `python scripts/run_s0_face_spike.py`  
2. **可并行**：有 Docker 时补 BOOT-0 容器健康；核验私有 GitHub `origin` 后推送  
3. **工程下一阶段**：S0-B（可选/可延后）或 **S1 可信基础骨架**（会话/组织/项目/RLS/Outbox/双模式壳）

## 7. 本地记录

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\.agent-control\control.ps1 -Operation open
powershell -NoProfile -ExecutionPolicy Bypass -File .\.agent-control\control.ps1 -Operation tail -Tail 20
git status --short
```
