# DramaForge 开发执行检查点

**状态：RECOVERY-0.1 COMPLETED / BOOT-0 本地质量可跑 / 容器与远端仍 PAUSED / S1-CONTRACT 已启动**

**最近更新：2026-07-20**

**唯一开发工作区：`D:\调研\dramaforge`**

## 1. 当前定位

| 项目 | 当前事实 |
|---|---|
| 当前产品阶段 | P0：内部试产的完整 MVP |
| 当前工程阶段 | BOOT-0 本地质量已恢复；**BOOT-0 容器 Gate 仍未关闭**；进入 **S1-CONTRACT** |
| 当前唯一执行任务 | `S1-CONTRACT`（`RECOVERY-0.1` 已 COMPLETED） |
| 仓库边界 | 仅 `D:\调研\dramaforge` |
| GitHub | **无** `origin` → **单写入 Agent 串行**；禁止多 worktree/PR 并行 |

## 2. Gate / 队列

| 项目 | 状态 | 证据摘要 |
|---|---|---|
| BOOT-0 本地质量入口 | **通过（本机）** | `scripts/run_quality.ps1` 使用 `backend/.venv` + `npm.cmd`；exit 0 |
| Backend pytest 受控临时目录 | **通过（本机）** | `--basetemp=tmp/pytest-basetemp` + `TMP/TEMP` 指向 `tmp/pytest-work`；34 passed |
| Playwright E2E | **通过（本机）** | `npm.cmd run test:e2e` exit 0；`reuseExistingServer:false` + `globalTimeout` |
| Ruff/mypy/pytest/前端 lint/tsc/test/build | **通过** | 见 `run_quality.ps1` 输出 |
| OpenAPI 二次生成 | **通过** | 两次 hash 一致 |
| 目录合规 | **通过** | 含 demo 拒绝 `utils2` / `.env` |
| `BOOT-0-CONTAINERS` | **PAUSED** | 无 Docker CLI |
| `GIT-REMOTE-0.1` | **PAUSED** | 无 origin / 无 gh |
| `S0-A-FIXTURE` | **PAUSED** | 样本不足；真实 FAR/FRR 未做 |
| `S0-B` | DEFERRED_OPTIONAL | — |
| `S1-CONTRACT` | **STARTED / READY** | 见 §4 |

**禁止宣称：** BOOT-0 全阶段完成、P0 MVP 完成、S1–S5 完成、Docker 健康、远端已推送、S0-A 数据 Gate 通过。

### 2.1 历史 tmp 碎片

`tmp/codex-pytest-*` 为历史锁死目录（本机 Access Denied，无法删除）。路径已在 `.gitignore` 的 `tmp/` 下，**不进入 Git**。质量脚本会尝试清理 `codex-pytest-*`；清理失败仅 Warning，不伪造“已删除”。

## 3. `RECOVERY-0.1` 完成摘要

### 改动

- `scripts/run_quality.ps1`：`Get-ProjectPython` / `Get-NpmCmd`；受控 pytest 临时目录；清理 `codex-pytest-*` 尝试
- `frontend/playwright.config.ts`：单 worker、`reuseExistingServer:false`、`globalTimeout` 180s
- `backend/tests/unit/test_run_quality_script.py`：断言质量脚本使用项目解释器
- 检查点 / README 状态语义纠正

### 验收命令（本机真实结果）

1. `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\run_quality.ps1` → **exit 0**（Python=`backend\.venv\Scripts\python.exe`）
2. pytest 受控 basetemp → **34 passed**；无新的可提交测试垃圾
3. `npm.cmd run test:e2e`（frontend）→ **exit 0**
4. OpenAPI gen ×2 → hash 相等；dir compliance OK
5. `docker` / compose / `git remote -v` / `gh` → **不可用或空** → 对应 PAUSED

## 4. 当前 Task 合同：`S1-CONTRACT`

**优先级：最高（RECOVERY 之后）**

**状态：STARTED**

### 完成效果

将 S1 Gate 拆成可执行纵向 Task，产出正式合同与受影响数据/API/测试清单，并启动第一个 `READY` 纵向切片（不在本 Task 内实现完整 S1）。

### 范围

- 对照 `01`–`06` 与 `agent.md` S1 Gate，列出最小可信切片顺序
- 定义每个切片的效果、证据、文件所有权与依赖（含 PAUSED 项不阻塞项）
- 将第一个切片标为 `READY` 并写入账本 STARTED 后移交实现

### 非范围

- 不实现完整会话/RLS/Graph 业务
- 不调用真实 Provider
- 不关闭 Docker/远端/S0-A fixture Gate

### 完成定义

- `docs/task-contracts/S1-CONTRACT.md` 已提交
- 检查点队列更新；第一个 S1 纵向 Task 合同就绪
- 账本 `S1-CONTRACT` COMPLETED，并可为 `S1.1-*` 写 STARTED

### S1 纵向切片草案（合同产出）

| Task | 完成效果（摘要） | 依赖 |
|---|---|---|
| `S1.1-access-session` | Cookie 会话、CSRF、User/Org/Membership 最小读写 + 测试 | 无 Docker 可用 SQLite? **否** — 需 PostgreSQL 测 RLS；无 Docker 时用本机 PG 或 PAUSED 集成、保留单元 |
| `S1.2-project-rls` | Project + 逐表 RLS + 跨项目拒绝测试 | S1.1；真实 PG |
| `S1.3-event-outbox` | EventLog + Outbox 同事务 + 重放骨架 | S1.2 |
| `S1.4-graph-model` | Graph/GraphVersion 不可变发布边界模型 | S1.2 |
| `S1.5-creation-shell` | Creation Interface 壳、同 Project 双入口、无 Provider | S1.2–S1.4 |
| `S1.6-frontend-dual-entry` | 快速/专业入口共享 ProjectSnapshot | S1.5 |

第一个实现 Task：`S1.1-access-session`（见 `docs/task-contracts/S1-CONTRACT.md`）。

## 5. 长期规则（摘要）

- `COMPLETED` ≠ 阶段完成；阶段完成必须有 Gate 证据
- PAUSED 只阻塞依赖路径；有 READY 就继续
- 无 origin 时单写入串行

## 6. 本地恢复入口

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\.agent-control\control.ps1 -Operation open
powershell -NoProfile -ExecutionPolicy Bypass -File .\.agent-control\control.ps1 -Operation tail -Tail 30
git status --short
powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\run_quality.ps1
```
