services:
  postgres:
    pull_policy: never
  redis:
    pull_policy: never
  minio:
    pull_policy: never
  litellm-db:
    pull_policy: never
  litellm:
    pull_policy: never
  migrate:
    pull_policy: never
  api:
    pull_policy: never
  frontend:
    pull_policy: never
  database-bootstrap:
    pull_policy: never
  maintenance:
    pull_policy: never
  dispatcher:
    pull_policy: never
  worker-default:
    pull_policy: never
  worker-heavy:
    pull_policy: never
