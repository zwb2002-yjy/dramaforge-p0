# DramaForge 项目总需求（冻结基线）

**状态：FROZEN / P0 实施唯一需求入口**  
**生效日期：2026-07-13**  
**同步基线：2026-07-14（双模式创作体验与受控混合 Agent 运行时已纳入 P0）**  
**适用对象：产品、研发、测试、运维及参与本项目的 AI 编码代理**

> 本文与 `02_全栈技术栈锁定表.md`、`03_全局目录规范.md`、`04_数据定义全集.md`、`05_模块落地约束.md`、`06_受控混合Agent运行时规范.md` 共同构成 P0 实施冻结包，并按各自主题构成唯一实现规范。`DramaForge架构决策与技术选型书.md` 保留决策理由、取舍和阶段路线；`DramaForge双模式产品与架构汇报方案.md` 是立项汇报摘要；`AI短剧工作台完整实施规划.md` 已归档，仅作历史需求来源。发生冲突时，以本冻结包为准。
>
> “冻结”指需求、技术、目录、数据与运行时合同已受控；S0-A、S0-B 和列出的 ADR 是接入真实 Provider、启用条件性交付和完成 P0 验收的实施 Gate，不是绕开本冻结包的临时例外。

## 1. 产品定位

DramaForge 是面向**短剧制作团队私有化部署**的镜头级 AI 生产工作台。它在同一组织、项目、资产库、Production Graph、成本账本和审计边界内，提供**快速出片模式**与**专业工作台模式**两种可切换入口。

它不是“一句话自动出片”工具，也不是通用非线性剪辑软件。产品核心是将剧本、资产、分镜、生成、质检和交付纳入可审计的 Production Graph，使团队能够知道：谁在何时以什么输入、使用哪个 Provider、花费多少成本、产生了哪个不可变产物，以及为什么需要重跑或人工审核。

### 1.1 目标用户与角色

| 角色 | P0 目标 | 必须具备的操作 |
|---|---|---|
| 制片/项目管理员 | 控制成本、权限和交付 | 创建项目、设预算、管理成员、发起导出、查看审计 |
| 快速出片创作者/运营人员 | 从创意、文本或模板快速形成可审核方案 | 在确认卡片中确认 Creative Brief、计划和预算，并可随时进入同一项目的专业工作台 |
| 编剧/策划 | 把剧本转为可生产数据 | 导入剧本、编辑剧集/场次/镜头、维护剧情规则 |
| 美术/资产管理员 | 锁定可复用资产 | 维护角色、场景、道具、参考图与提示词锁 |
| 导演/分镜师 | 审核镜头与重跑范围 | 编辑 Shot、发起局部重跑、采纳或驳回审核结果 |
| 审核员 | 处理无法自动修复的风险 | 查看一致性违规、确认/拒绝产物、采纳取消后完成的结果 |

P0 不允许匿名访问；每个用户必须属于组织和项目，所有业务读取均受项目权限与 PostgreSQL RLS 约束。

### 1.2 核心价值

1. **镜头级可控**：每个 Shot 都有独立生产图、版本、执行 Run、成本和产物。
2. **一致性强制**：角色人脸七层防线与剧情连续性四层检查均是执行门，而非仅展示分数。
3. **增量重跑**：只重跑输入或规则发生变化的节点及其正确下游，禁止全量重跑掩盖依赖错误。
4. **成本与审计闭环**：真实 Provider 调用、预算预占、缓存复用、取消后计费和导出均可追溯。
5. **可交付**：P0 稳定交付 MP4、SRT、素材包和时间线 JSON；剪映草稿仅在兼容性 Spike 通过后启用。

## 2. P0 业务闭环

### 2.1 固定流程

```text
快速出片入口：创意 / 文本 / 模板
  → 立即创建正式 Project 与可手工编辑的初始 Brief Revision
  → 展示文本规划范围、计价快照与最坏可接受费用并取得一次性授权
  → Agent 生成或人工编辑 Brief Revision，并完成人工确认
  → Agent 生成或人工编辑 Creation Plan，并完成预算/计划确认
  → 通过 `materialization-p0-v1` 白名单在单一事务内物化到同一 Project
  → 可随时进入同一专业工作台继续编辑；未配置文本 Key 时全程可走手工路径

组织/成员
  → 创建 Project 并设定预算、画幅、风格圣经
  → 导入剧本文本并形成 Episode / Scene / Shot 草稿
  → 创建并锁定 Character Reference、场景、道具与连续性规则
  → 审核并发布不可变 GraphVersion
  → 执行 Shot Production Graph
  → 人脸与剧情一致性检查、自动可恢复修复或人工审核
  → 合成、导出并下载交付包
  → 保留审计、成本、事件与产物生命周期记录
```

### 2.2 P0 必做能力

| 编号 | 能力 | 验收事实 |
|---|---|---|
| P0-01 | 项目与权限 | 可创建组织、项目、项目成员；越权 API、Worker 和对象 URL 均被拒绝。 |
| P0-02 | 剧本与分镜 | 可导入 UTF-8 `.txt` / `.md` 剧本，形成可人工编辑的 Episode、Scene、Shot；P0 不以 AI 自动解析正确率作为阻断条件。 |
| P0-02A | 双模式创作体验 | 快速模式先创建正式 Project 与手工 Brief，再经 Brief/Plan 两级确认和白名单物化形成正式对象；两个入口切换同一项目，不复制数据、不丢失成本、Run、产物或人工锁定。 |
| P0-02B | 受控混合 Agent 运行时 | Agent 仅完成受限结构化推理；所有文本 Provider 调用均先授权并形成 AgentRun、1:N ProviderOperation、预算/成本记录。Outbox→RuntimeScheduler→Arq 可恢复调度，取消、stale 写回拒绝、无 Key 手工降级和 Materialization 幂等均可验证。 |
| P0-03 | 资产与角色 | 可创建角色、场景、道具；主角必须有至少 1 张 canonical 参考图与可用 Reference Set，才能执行含该角色的镜头。 |
| P0-04 | Production Graph | 每个 Shot 使用固定 `shot-p0-v1` DAG；定义、版本、Run、ProviderOperation、Artifact 四层记录完整。 |
| P0-05 | 真实生成切片 | S2 仅启用 1 个真实图像 Provider，打通“首帧生成→人脸特征→比对→入库→SSE 展示”。 |
| P0-06 | 扩展生产链 | 通过固定节点生成或接入视频、配音、字幕、合成；任何 Provider 不可用时必须显式失败、降级或进入审核。 |
| P0-07 | 一致性引擎 | 所有实际生成图像均执行角色人脸检查；所有可交付镜头均执行四层剧情检查；视频执行固定采样漂移检查。 |
| P0-08 | 缓存与重跑 | 相同有效输入创建 `cached` NodeRun、成本为零；参数或上游 Artifact 改变时仅失效正确下游。 |
| P0-09 | 预算与取消 | 真实调用前预算预占；预算不足不调用 Provider；取消、完成竞争和晚到费用均有确定状态与账目。 |
| P0-10 | 事件与恢复 | 关键状态通过 Transactional Outbox 投递 Redis Streams；SSE 支持 `Last-Event-ID` 后重取快照。 |
| P0-11 | 交付 | 生成 MP4、SRT、素材包、时间线 JSON；交付包中的每个媒体项能回链到 Artifact、NodeRun 和成本。 |
| P0-12 | 审计与治理 | `event_log`、`cost_ledger`、Outbox、Provider Inbox、Artifact 生命周期事件均可查询；密钥、完整提示词和生物特征不进入事件 payload。 |

## 3. MVP 验收标准

### 3.1 端到端验收样本

验收使用一份**3–5 场、至少 10 个 Shot、至少 1 名主角**的冻结剧本样本。这是首版 MVP 唯一的端到端验收样本；S0–S5 的内部建设切片不得缩减、替代或提前宣布完成本节验收。样本和预期交付清单必须存入测试夹具，不得只用人工临时数据。

以下条件必须同时满足：

1. 项目管理员可创建项目、分配成员、设置预算和 9:16 或 16:9 画幅。
2. `start_project` 创建正式 Project、模式偏好和可手工编辑的初始 Brief Revision，但未经有效 `PlanningAuthorization` 不得创建文本 ProviderOperation 或调用文本 Provider。
3. Brief/Plan Agent 输出分别形成不可变 Brief Revision 与新 Creation Plan，二者都唯一回链产生它的 AgentRun；两级确认使用 revision/version、content/context hash 防止晚到覆盖。
4. 同一 AgentRun 的 primary、schema repair、transport retry 或 provider fallback 各自形成独立 ProviderOperation，实际 Provider/model/token/cost 只从 ProviderOperation/CostLedger 聚合。
5. Redis 暂时不可用、重复 Outbox/Arq Job 或 Worker lease 过期后，AgentRun 可由 RuntimeScheduler 补偿恢复且不重复副作用；取消或 stale 的晚到输出不得写回。
6. `confirm_plan` 仅解释 `materialization-p0-v1` 白名单命令，并在单一 Unit of Work 中幂等创建正式对象；任一步失败不得留下半成品或错误确认状态。
7. 未配置文本 Provider Key 时，项目、Brief、Plan 和专业工作台仍可手工使用，系统不得使用隐式平台 Key或无限排队。
8. 用户可导入剧本并人工完成 Episode、Scene、Shot 审核；每个 Shot 有唯一排序号、版本号和描述。
9. 主角拥有 canonical Reference 与锁定提示词；生成前系统能拒绝缺失角色参考的镜头。
10. 至少 10 个 Shot 能经 Production Graph 产生可审计结果；失败镜头显示统一错误码、ProviderOperation 和可重试建议。
11. 所有生成图像均产生 InsightFace 512 维特征并与角色阈值比较；低于阈值的可恢复问题进入有限次数修复，耗尽后进入人工审核。
12. 所有交付镜头均执行剧情连续性四层检查；每条违规能回链到规则、资产、镜头和修复建议。
13. 改动一个字幕节点的有效参数后，只允许 Subtitle、Composite 及正确下游重跑；Keyframe、Video、Voice 必须以缓存或未受影响状态保留。
14. 缓存命中创建新的 `NodeRun(status=cached)`，不创建 ProviderOperation，不产生真实成本，并引用源 Run 的同一不可变 Artifact。
15. 并发提交同一 `project_id + graph_version_id + node_id + input_hash` 时，最多出现一个真实 ProviderOperation。
16. 预算不足时 NodeRun 进入 `blocked_budget`；取消后完成的远端结果进入 `completed_after_cancel`，不会自动进入缓存或触发下游。
17. 前端断开 SSE 后以 `Last-Event-ID` 接续，并从 API 重取 Graph 快照；重复事件不会制造重复 UI 状态或重复副作用。
18. 导出包必须包含 MP4、SRT、素材包和时间线 JSON；每项的哈希、来源 Artifact、版本与下载授权可追溯。

### 3.2 性能与外部依赖口径

- 平台自身的 API 创建、权限校验、数据库提交和事件入队必须在本地验收环境中可观测；外部 Provider 的排队/生成时间单独记录，不作为平台同步接口超时的借口。
- 单镜头的 Provider 时间目标：图像生成不超过 2 分钟、视频生成不超过 10 分钟。超时后必须转为确定的失败分类或继续轮询状态，禁止无限等待。
- 人脸检查覆盖率必须为 100%；主角首帧相似度必须不低于该角色经校准后的阈值；视频抽样漂移率必须低于 10%，否则进入审核或修复流程。
- P0 验收必须至少演练：缓存命中、Outbox 重放、死信、预算不足、取消竞态、Provider 回调重放、Artifact 冷存储回温失败和 RLS 越权拒绝。

## 4. 业务边界

### 4.1 P0 支持范围

- 私有化单实例部署，组织与项目隔离。
- 中文短剧剧本的文本导入与人工结构化编辑；快速模式可从创意、文本或模板建立同一正式项目，并在受控 Agent 与完整手工路径之间切换。
- 人工可控的角色、场景、道具和风格资产。
- 镜头级图像/视频/语音/字幕/合成生产 DAG。
- 用户 BYOK；自动降级候选仅来自该用户已配置且具备能力的 Key。
- 可审计的自动修复和人工审核；所有自动重试受 `max_auto_attempts` 与预算约束。
- MP4、SRT、素材包、时间线 JSON 交付；剪映草稿经 S0-B 通过后作为可选 P0 能力。

### 4.2 P0 外部依赖边界

- Provider 只经固定 Adapter 调用；文本 Adapter 仅允许由 `creation.agent_runtime` 经预算授权、ProviderOperation 与成本审计调用，其他业务模块不得直接使用 SDK 或 HTTP 请求。
- MinIO 是唯一对象存储实现；所有下载通过限时、单对象、单动作预签名 URL。
- PostgreSQL 是唯一业务真相源；Redis 只用于队列、Streams、Pub/Sub 和短期分发。
- 任一外部服务故障必须转为标准错误、重试、死信或审核状态，不能修改既有成功 Artifact。

## 5. 禁止实现清单

以下功能在 P0 **禁止实现**。发现相关需求、分支、依赖或目录时，必须拒绝并记录为未来候选，而不是以“预留接口”名义落入主干。

| 禁止项 | 禁止原因与替代 |
|---|---|
| Adobe Premiere `*.prproj` / PR 工程导出 | 私有二进制格式、无稳定公开规范；只允许未来通过 FCPXML/EDL 中转，不实现逆向或兼容层。 |
| 通用非线性剪辑器 | 不实现多轨自由剪辑、逐帧编辑、调色台、完整时间线拖拽替代专业 NLE。P0 只输出交付包和时间线 JSON。 |
| 无人审核的全自动出片 | 不允许跳过预算、角色参考、质检、审核和导出确认。 |
| 插件市场、第三方插件执行、公开 SDK | 不实现插件沙箱、动态加载、市场、Webhook 扩展或开放平台。 |
| 多 Provider 并发竞价/跨账号代付 | P0 只允许固定 Adapter 和 BYOK 候选集合内的有序降级。 |
| 公有 SaaS 订阅、支付、用量计费、发票 | P0 不做 Stripe/支付宝/订阅/套餐/充值；只做项目预算与实际成本审计。 |
| 自由 Agent、Agent 框架、MCP Runtime、任意工具调用、用户自定义工作流 | P0 禁止 LangChain、通用 Agent SDK、MCP Runtime、Agent-to-Agent spawn 和写入型 Tool。仅允许 `06` 冻结的受控结构化 Agent；不得直接写库/文件、改变 NodeRun/ProviderOperation/成本/审核，或绕过确认、预算、RLS、Command 白名单。 |
| LoRA 训练、模型微调、模型托管 | LoRA 仅为未来可选增强；P0 不训练、不上传、不管理权重。 |
| 自研人脸识别模型、云端生物特征共享 | 固定使用 InsightFace ONNX 本地推理；Embedding 仅在项目授权和保留期内使用。 |
| WebSocket 实时层 | 固定使用 SSE + Redis Streams；不得另建 WebSocket 网关。 |
| Kubernetes、服务网格、微服务拆分 | P0 采用 Docker Compose 单体模块化部署；不得以“可扩展”为由提前拆分。 |
| 云对象存储替换、跨项目公共缓存 | 固定 MinIO；缓存只在同一 Project 内复用。 |
| 剪映草稿强制承诺 | S0-B 兼容性 Spike 未通过前，剪映草稿不得成为 P0 验收阻断项。 |

## 6. 变更与验收治理

1. 任何范围变更先归类为：需求、技术、目录、数据或运行时行为。
2. 变更必须同步更新 01–06 六份冻结文件中的对应事实，以及关联 ADR、迁移、模型、API schema、测试夹具和验收用例；Agent 运行时变更不得只改 `06`。
3. 不允许只改前端、只改 ORM 或只改数据库；数据契约变更必须同时更新 SQL、ORM、Pydantic 与 OpenAPI 类型生成。
4. 不允许以 feature flag 绕开本文禁止项；feature flag 只能控制已批准、已测试能力的启用状态。
5. P0 验收记录必须包含版本号、测试数据哈希、Provider 配置摘要、成本摘要、失败清单和人工批准人。
6. 不属于 MVP 闭环的能力必须登记到 [`docs/MVP能力延期台账.md`](docs/MVP能力延期台账.md)，写明阶段、领域前置、最早进入条件和验收标准；不得以“预留接口”、局部 UI 或临时依赖的形式进入 P0 主链。
