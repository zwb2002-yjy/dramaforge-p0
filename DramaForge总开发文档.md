# DramaForge 总开发文档

**状态：ACTIVE / 项目唯一总纲**

**版本：v1.0**

**日期：2026-08-12**

**代码审计基线：`dev@9e79ba40178f4bf29391d21240794b5cb377306d`**

**取代：`AI短剧工作台完整实施规划.md` 的总规划职责；旧文档保留为历史设计底稿**

> 本文只回答六个问题：为谁做、解决什么问题、现在完成到哪里、人物一致性是否有效、
> 什么能形成壁垒、下一步按什么顺序开发。实时任务和最新测试结果仍以
> [`docs/开发执行检查点.md`](docs/开发执行检查点.md) 为准；P0 实现细节仍受 `01` 至 `06`
> 冻结合同约束。

---

## 0. 总结论

### 0.1 产品方向

DramaForge 不再定位为“面向所有人的一键 AI 短剧生成器”，也不做通用剪辑器、团队协作
SaaS 或模型聚合门户。当前唯一方向是：

> **为已经使用多个生成模型、需要反复制作多镜头内容的独立 AI 短剧主创或小型工作室的
> 单一 AI 制作操作者，提供一个私有、镜头级、可审核、可局部返工、可追溯交付的生产工作台。**

P0 的可信承诺不是“自动生成可直接发布的完整短剧”，而是：

> **把剧本变成一份可审核的 10 Shot 粗剪和完整素材包；系统明确指出哪些镜头通过、哪些
> 镜头因人物或剧情连续性失败、应从哪里局部返工，并保留模型、输入、成本和产物来源。**

这是一个窄而真实的产品切口。当前视频模型对动态场景身份保持不足、本地 TTS 音质有限，
因此在这些问题被真实验证前，不得宣传“动态镜头人物稳定”或“成片无需后期”。

### 0.2 当前阶段

当前状态是 **P0 功能候选版，工程能力较完整，当前提交发布证据失效，市场价值尚未验证**。

- 代码层已有账号/私有 Workspace、Project、剧本/Shot、Production Graph、模型插件与配置、
  图像/视频执行、人物与视频漂移检查、审核、局部返工、成本血缘和四项交付。
- 2026-08-05 的旧提交曾完成静态构图黄金样本 `10/10` 全链和交付，但 2026-08-10 至
  2026-08-11 又落地了 Provider A+B、Model Plugin V3、Production Model Profile 和真实
  LiteLLM Proxy。旧证据不能证明当前 `9e79ba4`。
- 最新记录的开发质量是 backend unit `537 passed`、PG + real-proxy integration
  `20 passed`、ruff/mypy 通过；这是实现报告中的开发证据，不是当前提交的 P0 发布 Gate。
- 还没有真实用户访谈、设计伙伴项目、复用率、付费意愿或留存数据。因此不能声称已经找到
  产品市场匹配，也不能把继续加架构当成降低产品风险。

### 0.3 当前唯一优先级

```text
停止扩架构
  -> 在当前代码上恢复 P0 同源证据
  -> 做人物一致性扩展评估
  -> 让 3 名设计伙伴完成真实 10 Shot 项目
  -> 用复用和付费事实决定是否进入 P1.1
```

P0 发布验收和封闭试用可以准备，但 **P1 功能开发、更多 Provider、3D、OpenCut、团队协作、
通用 NLE 和新的架构规格全部暂停**，除非本文件定义的进入 Gate 已满足。

---

## 1. 事实、假设与禁止混淆的结论

| 类型 | 当前结论 | 证据或处理方式 |
|---|---|---|
| 工程事实 | P0 的主要领域结构和生产链已经实现 | 代码、迁移、单元/PG/Proxy 测试、历史 10 Shot 证据 |
| 工程事实 | 当前 HEAD 已不同于最后一次 P0 黄金样本提交 | `dev@9e79ba4` 包含 8 月 10 至 11 日模型执行架构改动 |
| 工程事实 | InsightFace 能运行并输出 512 维人脸特征 | Compose CPU 运行状态与 S0-A 报告 |
| 工程事实 | 现有视频漂移 `mean >= 0.40` 会漏过“末帧已经变脸”的动态视频 | ADR-0004 的逐帧真实样本 |
| 工程事实 | 当前 UI 和部署方式更适合有技术能力的早期用户 | Docker Compose、BYOK、模型 Binding/Profile 和运行栈要求 |
| 产品假设 | 高频多模型创作者愿意用可控性换取更长的设置流程 | 必须通过用户访谈和设计伙伴试用验证 |
| 产品假设 | 一致性门禁、局部返工和血缘能显著降低废片与人工整理时间 | 必须对比用户原工作流测量 |
| 产品假设 | 私有化/BYOK 是购买理由而不只是部署负担 | 必须询问真实付费用户，不能从架构反推 |
| 未成立结论 | “代码多、测试多，所以产品有用” | 禁止这样判断 |
| 未成立结论 | “使用 InsightFace，所以人物一致性已解决” | 禁止这样宣传 |
| 未成立结论 | “支持多个 Provider，所以形成壁垒” | Provider 抽象是必要基础设施，容易被复制 |

所有后续汇报必须明确使用“工程事实”“产品假设”或“已验证产品结论”三种标签之一。

---

## 2. 目标用户与核心任务

### 2.1 首批 ICP

首批目标用户不是泛个人创作者，而是以下更窄的人群：

| 属性 | 定义 |
|---|---|
| 角色 | 独立 AI 短剧主创，或 1 至 5 人小工作室中唯一负责 AI 生成的制作操作者 |
| 频率 | 每月至少制作 1 个多镜头项目，或持续管理 30 个以上 Shot |
| 当前工具 | 已经使用 2 个以上文本/图像/视频平台，手工维护 Prompt、参考图、结果和返工记录 |
| 核心损耗 | 人物变脸、镜头状态不连续、失败后整段重做、候选素材散落、成本和来源说不清 |
| 技术门槛 | 能配置 API Key；封闭试用期可接受一次部署协助 |
| 购买动机 | 减少废片和重复生成、缩短审核与返工、保护项目数据、稳定交付素材 |

### 2.2 暂不服务的人群

- 只想输入一句话立即得到完整短剧的新手用户。
- 需要成员、审批人、评论、指派和实时协作的中大型制作团队。
- 需要成熟 NLE、专业调色、复杂多轨音频或广播级交付的传统后期团队。
- 只偶尔生成单张图或单条视频，不存在跨镜头治理问题的用户。
- 不愿配置模型账号且要求平台垫付所有模型费用的用户。

这些用户不是永久排除，但现在服务他们会重开托管、支付、协作、权限、编辑器等大范围，
使当前产品失去焦点。

### 2.3 用户要完成的核心任务

```text
当我有一份短剧剧本并需要用多个 AI 模型制作时，
我希望系统把它拆成可追踪的镜头，稳定使用角色与剧情约束，
自动拦截明显变脸或连续性错误，只重做失败部分，
最后交付一份我能继续剪辑且说得清来源和成本的粗剪与素材包。
```

核心工作流固定为：

```text
剧本/创意
  -> Brief / Plan 确认
  -> 角色、场景、道具与 Canonical Reference
  -> Shot 与冻结 Production Graph
  -> Keyframe -> Face Review -> Video -> Drift Review
  -> Voice / Subtitle -> Composite -> Continuity Review
  -> 人工审核与局部返工
  -> MP4 + SRT + 素材包 + timeline_json
```

任何新功能若不能提高这条工作流的完成率、质量、速度、成本透明度或复用率，不进入当前排期。

---

## 3. 产品价值与不可替代性

### 3.1 用户价值

DramaForge 的价值不在“能调用模型”，而在管理模型生成的不确定性：

1. **知道哪里错**：每个 Shot 的人物、视频漂移和剧情连续性有明确 Gate 与证据。
2. **只重做错误部分**：Graph、输入哈希和 Artifact 血缘确定正确的局部失效范围。
3. **不丢生产事实**：Prompt、模型、Provider、费用、失败、审核和交付来源可回链。
4. **可以换模型**：模型能力、连接、业务 Slot 和执行协议分离，避免项目被单一供应商锁死。
5. **可以继续后期**：交付粗剪、字幕、素材和时间线，而不是只返回一个不可解释的 MP4。

### 3.2 什么不是壁垒

- InsightFace、LiteLLM、FFmpeg、FastAPI、React 或任何单个开源组件。
- 接入 Agnes、Ark、MiniMax、可灵等 Provider 的数量。
- 一个更大的配置页面或更复杂的通用工作流画布。
- 文档数量、领域对象数量、测试数量或“私有化部署”四个字。

这些都是可购买、可替换或可复制的能力。

### 3.3 真正需要积累的壁垒

| 壁垒层 | 形成方式 | 当前状态 |
|---|---|---|
| 可追溯生产协议 | 将 Shot、Graph、Run、ProviderOperation、Artifact、Review、Delivery 形成不可变血缘 | 主体已实现 |
| 一致性评估资产 | 持续积累角色、构图、光线、动作和视频漂移的标注评估集，并按模型版本复测 | 只有小型 S0-A 与少量真实视频样本 |
| 失败到返工的数据闭环 | 记录失败原因、修改了什么、换了哪个模型、成本多少、最终是否通过 | 部分实现；候选处置与结构化复用待 P1.1 |
| 项目级创作记忆 | 把经审核的角色、场景、道具、状态和 Prompt 变成可复用生产资产 | P0 有基础事实，完整治理未完成 |
| 模型质量/成本路由数据 | 在同一 Shot 意图上积累不同模型的成功率、身份保持、延迟和成本 | 架构已具备，真实数据尚少 |
| 工作流嵌入与信任 | 用户连续项目都在同一系统完成，依赖历史资产、审核与交付血缘 | 尚未通过真实用户验证 |

因此当前的准确判断是：**DramaForge 已有构建壁垒的基础设施，但尚未形成不可替代性。**
不可替代性必须由真实项目数据、评估集和用户复用产生，不能由继续设计架构产生。

---

## 4. 人物一致性与 InsightFace 结论

### 4.1 InsightFace 到底有效吗

**有效，但只对一个窄问题有效：在能检测到清晰人脸时，用 Face Embedding 判断两张脸是否
像同一身份。它是质检器，不是生成控制器，也不是人物一致性的完整解决方案。**

InsightFace 能做：

- 检测画面中是否存在可评分的人脸。
- 比较生成关键帧与 Canonical Reference 的脸部相似度。
- 在阈值以下阻断下游，触发有限返工或人工复核。
- 对视频抽样帧做身份漂移评分。

InsightFace 不能单独做：

- 让生成模型更稳定地画出同一个人。
- 判断服装、发型、体型、年龄感、伤痕、道具和场景是否一致。
- 可靠处理远景、侧脸、遮挡、强表情、低光、风格化或多人同框。
- 保证视频中间帧和末帧不变脸。
- 判断一个镜头是否符合剧本、动作和导演意图。

### 4.2 当前证据能证明什么

S0-A 使用 20 对同角色、20 对异角色和 10 个无脸异常样本，批准阈值为 `0.60`。结果中：

- FRR 为 `0%`，说明该小样本中的同角色没有被误拒。
- FAR 为 `5%`，即 20 对异角色中仍有 1 对被误接收。
- 样本只足以证明流程可运行并提供一个 P0 有界阈值，不足以证明跨角色、跨画风、跨景别的
  通用准确率，更不足以作为商业宣传指标。

真实项目还证明两点：

- 调整镜头构图，让主角脸成为清晰主体后，静态黄金样本可达到 `10/10` face 首轮通过。
- 动态视频会从首帧约 `0.90` 衰减到末帧 `0.11`；现有 `mean >= 0.40` 可能仍判通过。
  因此现有 Video Drift 规则对末帧变脸存在已知漏检。

结论：**当前方案对“静态或低运动、脸部清晰的主角镜头”有实际价值；对动态镜头、多角色、
遮挡和风格化画面尚未证明。**

### 4.3 完整人物一致性必须是多层系统

```text
角色设定与 Canonical / Reference Set
  -> 生成前锁定 Prompt、状态、参考图和模型能力
  -> 生成模型真实消费参考图
  -> Keyframe Face Gate
  -> Video 多帧/分段 Drift Gate
  -> 服装、道具、场景和剧情连续性检查
  -> 有上限的自动返工
  -> 无法可靠判断时 needs_human，保持 fail-closed
```

InsightFace 只是其中的 Keyframe Face Gate 和 Video Drift 特征提取部分。真正的产品能力是整条
链路和返工闭环。

### 4.4 下一轮一致性评估 Gate

在扩大产品承诺或更换视频 Provider 前，建立版本化评估集：

| 评估项 | 最低范围 | 输出 |
|---|---|---|
| 角色 | 至少 5 个经授权或合成角色 | 每个角色独立统计，不只报总体平均 |
| 图像条件 | 正脸、3/4 侧脸、侧脸、近景、中景、低光、强表情、局部遮挡 | 可评分率、FAR、FRR、`needs_human` 率 |
| 图像 pairs | 至少 250 同角色 pairs、500 异角色 pairs、100 异常样本 | 阈值候选和置信区间 |
| 视频 | 每个候选模型至少 20 条静态/低运动和 20 条动态镜头 | 首/中/末及分位数分布、最差帧、误放行样本 |
| Provider | 对相同 Shot/Reference/构图运行可比请求 | 首轮通过率、返工后通过率、成本、延迟 |

视频规则是否从 `mean` 改为 `mean + min`、低分位数或连续低分段，必须先由该评估集校准并通过
新 ADR；不得为了让 Gate 变绿临时调阈值。

对外允许的表述仅为：“系统提供基于参考图的人脸相似度检查、视频抽帧漂移检查和人工复核
闭环。”在扩展评估通过前，禁止表述“保证人物一致”或给出无边界准确率。

---

## 5. 当前完成情况

### 5.1 已完成或主体已完成

| 领域 | 当前能力 |
|---|---|
| 账号与隔离 | User -> owned Workspace -> Project；Cookie/CSRF、RLS、Worker 上下文重建 |
| 双入口 | 快速创作和专业生产消费同一 Project 与事实源 |
| 创作规划 | 受控 Brief/Plan、确认与物化；文本 legacy/V3 双路径受 flag 管理 |
| 生产模型 | Episode/Scene/Shot、资产、Canonical Reference、状态与连续性规则 |
| 执行图 | 固定 `shot-p0-v1`、持久化 GraphEdge、DAG 上游阻断、NodeRun、局部重跑 |
| 模型执行 | Provider A+B 统一层、Model Plugin V3、CapabilityRouter、Agnes/Ark、Generation API |
| 模型配置 | Workspace/Project ProductionModelProfile、业务 Slot、快照与有效模型解析 |
| 文本网关 | 独立真实 LiteLLM Proxy、逻辑模型、错误分类、提交未知、成本 Header、真实 Proxy 测试 |
| 媒体质检 | Keyframe Face Review、Video Drift、连续性 Review、`needs_human` fail-closed |
| 血缘与成本 | ProviderOperation、Artifact、输入快照、哈希、预算/成本和审计事件 |
| 交付 | MP4、SRT、素材包、timeline JSON、ExportItem 与下载授权 |
| 运行栈 | PostgreSQL、Redis、MinIO、API、dispatcher、default/heavy Worker、LiteLLM Proxy |

### 5.2 部分完成或需要重新证明

| 项 | 当前状态 | 完成前缺口 |
|---|---|---|
| 当前提交 P0 | 历史链路通过，当前源码已迁移 | 当前干净提交的 formal、Playwright、Section 3.1、ops、人工批准 |
| Provider 统一切换 | 新路径已实现，legacy 仍在 flag 后 | 固定 Beta 配置、验证存量任务、稳定后再删 legacy |
| Text V3 | 真实 Proxy 与 Brief E2E 已用 mock deployment 证明 | 真实 LLM Key 的低成本 E2E、生产 flag 决策、workspace BYOK 迁移 |
| Model Profile | 9 个 Slot 可配置，P0 主路径已消费核心 Slot | 未驱动 Slot 不得宣传为已支持；完整浏览器 E2E 未跑 |
| 人物一致性 | 静态/清晰脸镜头有效 | 扩展评估、多角色/侧脸/遮挡/动态视频能力 |
| TTS | eSpeak NG 可完成本地闭环 | 音质不适合最终商业成片；真实用户是否接受未验证 |
| 前端产品化 | 主要页面和模型配置存在 | 新用户独立完成项目、错误恢复、设置复杂度尚未验证 |
| 市场验证 | 无 | ICP 访谈、设计伙伴、复用、付费意愿、相对原流程收益 |

### 5.3 明确未开始或暂停

- P1.1 多候选治理、结构化采用/拒绝、项目内复用。
- P1.2 富故事板、统一返工队列、正式资产库和模板。
- P1.3 版本化精剪与 FCPXML/OpenCut 适配。
- P2 3D 导演台和专业后期互操作。
- 多人协作、邀请、评论、指派、空间共享。
- 公有 SaaS 托管、支付、平台代付和商业计费。

---

## 6. 架构基线与冻结原则

### 6.1 当前稳定架构

```text
React Workstation
  -> FastAPI Route -> Domain Service -> Repository/PostgreSQL + Outbox
  -> Dispatcher -> Arq Worker
  -> Production Graph / NodeRun
  -> ModelSlot -> ModelBindingResolver -> CapabilityRouter -> ModelRegistry
  -> Native Provider Runtime 或 LiteLLM Proxy
  -> Artifact/Review/Cost -> MinIO/PostgreSQL
  -> FFmpeg Delivery
```

### 6.2 必须保留的原则

1. PostgreSQL 是业务真相源；Redis 只做队列和临时调度。
2. GraphVersion、Artifact 和正式交付来源不可变；重跑创建新 attempt，不覆盖历史。
3. 业务只表达 Slot/Capability，不按 Provider 名写分支。
4. 模型选择、Provider 连接、凭证和业务工作流彼此分离。
5. 每次外部调用先有授权、幂等和 ProviderOperation；提交结果不确定时 fail-closed。
6. 快速/专业模式共用同一 Project、资产、Run、审核、成本和交付事实。
7. 一致性检查不能因 Provider 不稳定或演示需要而关闭。

### 6.3 立即停止的架构行为

- 为尚未验证的 P1/P2 功能预建表、API、抽象层或隐藏 UI。
- 再写一份覆盖全项目的新架构规格，而不更新本总纲和 ADR。
- 为追求“全 Provider 支持”继续接模型，而没有同一评估集的质量/成本数据。
- 在 legacy 和新路径长期双写或产生两套执行真相。
- 以重构、拆包或命名统一替代当前发布和用户验证。

---

## 7. 开发路线与阶段 Gate

路线按证据顺序执行，不按功能想象并行铺开。

### 阶段 A：P0 当前源码重验

**目标**：证明 `9e79ba4` 之后的当前候选仍可完成已批准的静态构图 10 Shot 闭环。

必须完成：

1. 将当前文档变更形成可审计候选，确认工作树和运行栈 source 一致。
2. 运行当前全量质量：backend unit、PG integration、real LiteLLM Proxy、frontend、build、CI。
3. 明确 Beta 的 `PROVIDER_UNIFIED_PATH_ENABLED`、`TEXT_V3_ROUTER_ENABLED` 和 Profile 默认配置；
   不允许测试时一套、交付时另一套且没有记录。
4. 在同一提交运行 P0 bootstrap、真实 10 Shot Playwright、formal proof、Section 3.1 Gate 和 ops。
5. 验证四项交付哈希、Artifact/ProviderOperation 血缘、成本和人工审核。
6. 记录当前限制：静态/低运动镜头、动态视频变脸风险、本地 TTS 音质。

**退出条件**：同一干净提交全部 Gate `PASS`，无 `FAIL/BLOCKED/source mismatch`，并经产品负责人批准。

### 阶段 B：封闭设计伙伴验证

**目标**：验证产品是否比用户当前手工流程更有用，而不是继续证明系统能运行。

样本：8 名符合 ICP 的访谈对象，其中选择 3 名设计伙伴，各自使用自己的剧本和参考图完成至少
一个 10 Shot 项目。不要只使用仓库黄金样本。

记录：

- 用户原流程使用哪些模型、表格、目录和剪辑工具。
- 从剧本到粗剪的人工操作时间、等待时间、模型费用和废片数。
- 首轮/返工后通过率，变脸、动作、构图、连续性和 Provider 失败原因。
- 是否需要开发者改库、补队列或解释系统内部概念。
- 是否愿意在第二个项目继续使用，以及愿意为软件或部署服务支付什么。

**进入 P1.1 的产品 Gate**：

1. 至少 5/8 访谈对象把一致性、返工或素材治理列为前三痛点。
2. 至少 2/3 设计伙伴无需开发者修改数据库或队列即可完成交付。
3. 与其原流程相比，至少 2/3 的人工整理与返工时间下降 `30%` 以上。
4. 至少 2/3 在 14 天内开始第二个项目，或明确愿意付费继续使用。
5. 没有出现会迫使产品立即转向团队协作、通用 NLE 或一键黑盒生成的共同阻断。

若 Gate 不通过，停止 P1 开发，基于访谈决定收窄场景、调整部署形态或终止该方向。

### 阶段 C：P1.1 候选结果治理

只在阶段 B 通过后启动。目标是把真实试用中散失的候选、废卡和返工原因变成受控事实，形成
第一个可累积的数据闭环。范围以 ADR-0002 为准，不包括相似检索、AI 推荐和跨项目共享。

**退出条件**：真实用户能采用、拒绝、归档、恢复、标记可复用和提升候选；重跑不覆盖已审核
选择，所有操作可回链成本和来源。

### 阶段 D：P1.2 创作组织工作台

只解决设计伙伴已证明的几十至上百 Shot 管理问题：故事板、审核/返工队列、正式资产和模板。
所有视图消费同一事实源，不新增执行真相。

### 阶段 E：P1.3 受控精剪与后期衔接

提供版本化排序、裁剪、字幕、音量和有限转场；FFmpeg 可重现。FCPXML/OpenCut 只有在真实目标
工具往返 Spike 通过后接入。

### 阶段 F：P2 或新的商业路线

3D 导演台、更多专业格式、托管 SaaS、付费和团队协作都需要新的产品证据与正式合同，不能从
当前路线自动继承。

---

## 8. 当前执行顺序

| 优先级 | 工作 | 负责人 | 完成证据 |
|---|---|---|---|
| P0 | 总文档与文档入口收敛 | 产品/工程 | 本文成为唯一总纲；旧总规划降级为历史 |
| P0 | 当前 HEAD 无费用质量回归 | 工程 | unit、PG、real-proxy mock、frontend、build、CI |
| P0 | 固定 Beta 执行配置 | 工程/产品 | flags、Profile、Provider 组合和迁移策略有 ADR/报告 |
| P0 | 当前提交 P0 同源验收 | 工程/产品 | formal、Playwright、Section 3.1、ops、交付和批准 |
| P0 | 扩展人物/视频一致性基准 | 产品/工程 | 版本化评估集、逐模型报告、新 Drift 决策 |
| P0 | 8 名 ICP 访谈与 3 个设计伙伴项目 | 产品负责人 | 访谈记录、基线对比、第二项目/付费结论 |
| P1 | 候选治理合同与实现 | 工程 | 仅在产品 Gate 通过后启动 |

当前不以“再接一个模型”“再写一个架构版本”作为 READY 工作。

---

## 9. 指标与产品决策

### 9.1 北极星指标

> **每小时人工操作完成的、已通过质量 Gate 且可追溯交付的 Shot 数。**

只统计有正式 Artifact、Review 和交付血缘的镜头。生成了多少文件或调用了多少次模型不算价值。

### 9.2 支撑指标

| 维度 | 指标 |
|---|---|
| 完成 | 10 Shot 项目完成率、无需工程师介入完成率、四项交付成功率 |
| 一致性 | 可评分率、首轮通过率、返工后通过率、错误身份误放行率、`needs_human` 率 |
| 效率 | 用户操作时长、从失败到合格结果时长、局部重跑节省的节点数和时间 |
| 成本 | 每个最终采纳 Shot 的 Provider 成本、失败成本、缓存/复用节省 |
| 可靠性 | 提交未知、429/5xx、恢复、重复收费、悬空任务和交付失败率 |
| 产品 | 第二项目率、14/30 天复用、付费意愿、主动推荐、退出原因 |

不得把注册数、页面数、模型数、生成总量或测试总数单独作为产品成功指标。

### 9.3 继续、收窄或停止

- **继续 P1**：阶段 A、B 及一致性扩展评估全部满足最低 Gate。
- **收窄**：用户认可审核/返工，但不接受完整生产工作台时，收敛为“多模型短剧质检与返工台”。
- **改变部署形态**：价值成立但 Docker/BYOK 阻碍多数用户时，再评估托管或安装服务，不提前开发。
- **停止当前方向**：目标用户不把一致性/返工视为高频高成本问题，或真实项目无法产生明确效率收益。

---

## 10. 文档治理

### 10.1 权威顺序

1. 用户当前明确指令。
2. 本文：产品方向、目标用户、壁垒、阶段和进入 Gate。
3. `docs/开发执行检查点.md`：当前 HEAD、最新证据、唯一执行任务。
4. 当前阶段合同：P0 为 `01` 至 `06`；未来 P1/P2 必须新建版本化合同。
5. 已接受 ADR：具体决策及理由。
6. 运行手册、验收方案和测试证据。
7. Gap Analysis、Implementation Report、专题设计和调研。
8. Historical/Superseded 文档。

### 10.2 文档职责

| 文档类型 | 只回答什么 | 更新规则 |
|---|---|---|
| 本总纲 | 为什么做、为谁做、做到哪里、下一阶段凭什么开始 | 产品方向或阶段 Gate 变化时更新 |
| 开发检查点 | 当前真实状态和唯一任务 | 每个候选/验收/重大迁移后更新 |
| 阶段合同 | 当前阶段必须实现和禁止什么 | 评审后冻结，变更需版本化 |
| ADR | 一个重要决策为何这样选 | 决策变化新建/替代，不改写历史 |
| 验收证据 | 某提交实际通过了什么 | 绑定提交、环境、时间和哈希 |
| 实现报告 | 某专题改了什么、测试什么、还缺什么 | 完成专题时写，不提升为总纲 |
| 调研 | 外部事实和候选方案 | 只作输入，不能直接派生实现 |

### 10.3 新文档规则

- 根目录不再新增“完整规划”“全架构规范”类并行总文档。
- 新专题必须在 `docs/README.md` 登记状态、职责和被谁引用。
- 实现报告不得宣布产品阶段完成；历史测试数不得覆盖当前检查点。
- 文档中的完成状态必须能指向代码、测试、运行或用户证据。
- 发现冲突先更新本总纲或 ADR，再开发；不能通过增加第三份解释文档解决冲突。

---

## 11. 已批准与待验证决策

### 11.1 已批准

- 单一所有者的私有 Workspace，当前不做团队协作。
- 快速/专业双入口共享一个 Project 和事实源。
- P0 主黄金样本使用真实文本、图像、视频 Provider + 本地 TTS + 本地审核/合成。
- Face threshold `0.60` 作为当前有界 P0 策略；`needs_human` fail-closed。
- Agnes 动态视频身份保持限制按 ADR-0004 方案 B 如实接受；P0 只承诺已验证的静态/低运动范围。
- P0 完成后若产品 Gate 通过，优先做 P1.1 候选治理，而不是画布、协作或 3D。

### 11.2 不阻断当前重验、但必须由真实用户决定

- 私有部署是核心购买理由，还是只应作为可选部署方式。
- 用户是否接受“可审核粗剪 + 素材包”而非直接发布成片。
- 本地 TTS 是否足够用于粗剪；商业使用需要哪个声音路径。
- 用户更愿意为软件订阅、一次性部署、项目服务还是模型路由节省付费。
- 候选治理、故事板、审核队列三者中哪个才是 P1 的真实第一痛点。

这些问题不再通过内部讨论或继续写代码推断，统一进入阶段 B 的访谈和试用记录。

---

## 12. 当前里程碑完成定义

当前里程碑固定为 `P0-CURRENT-REVALIDATION`：

```text
完成效果：
  当前 dev 候选在固定 Beta 配置下，从真实 Brief/Plan、真实图像/视频到 10 Shot
  审核、局部返工和四项交付全链通过；证据与当前源码、运行栈和模型配置同源。

非目标：
  新 Provider、新 Slot、候选池、故事板、编辑器、团队协作、3D、UI 大重构。

完成条件：
  1. 当前全量质量和远端 CI 通过；
  2. formal、Playwright、Section 3.1、ops 全 PASS；
  3. 交付哈希、血缘和费用记录完整；
  4. 已知动态视频/TTS 限制在产品中如实呈现；
  5. 产品负责人批准进入封闭设计伙伴验证。
```

里程碑完成后，不自动开始 P1.1；先执行阶段 B 的真实用户验证。
