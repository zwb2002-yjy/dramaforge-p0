# DramaForge 总开发文档

**状态：ACTIVE / 专业版产品与研发唯一总纲**
**版本：v3.0**
**生效日期：2026-08-25**
**对应决策：[docs/adr/0006-professional-workbench-reframe.md](docs/adr/0006-professional-workbench-reframe.md)**

## 1. 一句话定义

DramaForge 是一个以场景和镜头为核心、以项目资产为稳定底座、以 Production Graph 为执行内核、允许用户与一个受控导演助手共同操作，并能把导演意图透明编译到不同 AI 模型上的专业 AI 影视制作工作台。

它的产品感知是：AI 时代的 Storyboard Pro、虚拟片场、生成生产台和 OpenCut 交付桥，而不是聊天机器人加几个生成按钮，也不是自由自治的多 Agent 黑盒。

## 2. 产品方向（已由用户确认）

首要用户调整为 AI 短剧、漫剧、广告/MV、独立影视创作者以及已经使用 ComfyUI、LibTV、可灵、即梦、海螺、火山等工具、但需要更透明和可控生产链的专业用户。

V1 仍是单用户全流程，不建设多人审批链。首版支持真人写实或稳定风格、多场景、多镜头、角色动作与对白驱动的完整作品。

## 3. 核心产品原则

1. 导演意图贯穿生产链：角色、场景、动作、机位、表演、风格、参考资产和模型能力不能被平台静默改写。
2. 画布是事实源：用户在场景/镜头画布中的正式修改决定下一次执行；聊天和助手建议不是事实源。
3. 助手可建议、可编译、可解释，但不能未经确认修改正式画布；建议支持逐项采纳/拒绝，拒绝后不纠缠。
4. 正式线与实验线分离：换模型、换关键帧或创意变体默认创建实验分支，用户选择后才可采纳。
5. 每一步透明：显示用户语义、模型执行表达、实际请求快照、参考资产、参数、结果和质量证据。
6. 失败可局部修复：按镜头、节点、时间点或时间段重跑，保留可复用产物和版本血缘。
7. 专业 UI 不包含预算/计费模块；它仍不得绕过用户确认、Provider 能力校验、质量 Gate、人工审片、导出与安全边界。Provider 成本仅作为内部审计元数据保留。

## 4. 计费边界

专业版不建设预算、计费、价格估算、充值或平台账单。模型供应商负责价格与结算；DramaForge 只保留 Provider 返回的原始审计字段，不将其转成平台费用或执行 Gate。

## 5. 默认工作台

项目打开后默认进入专业工作台，包含：

- 左侧项目导航与场景列表；
- 中央大幕布（关键帧、视频、剪辑随状态变化）；
- 底部故事板/时间轴混合镜头区；
- 右侧导演助手、资产引用、模型能力和执行证据；
- 可展开的生产链、审片批注、实验分支与导出面板。

四阶段快速模式保留为兼容入口，与专业模式共享同一个 Project、Graph、NodeRun、Artifact 和质量事实源；旧模式的预算字段/授权逻辑仅为兼容历史链路，不属于专业版用户体验。

## 6. 领域与运行时底座

继续保留并演进现有 Production Graph、Graph Version、NodeRun、Provider Plugin、Manifest、Production Profile、Provider Compiler、EffectiveRequest、Artifact、Outbox/Redis/Arq/Worker、幂等/恢复/取消、项目隔离、质量与交付设施。

导演助手是受控 Workflow/Skill Runtime：LLM 只能生成结构化建议、Change Proposal、Impact Report 或编译输入，不能直接写正式画布、直接绕过确认和能力校验调用 Provider，也不能形成自由聊天式多 Agent 自治。

推荐领域对象：Project、Scene、Shot、Asset、AssetVersion、CanvasRevision、DirectorSuggestion、ChangeProposal、ImpactReport、ExperimentBranch、NodeRun、ProviderOperation、Artifact、ReviewAnnotation、Export。

## 7. V1 必须完成

1. 导演助手关闭时，用户可手动完成资产 → 场景 → 镜头 → 关键帧 → 视频 → 审片 → OpenCut/导出的链路。
2. 场景/镜头画布是正式事实源，用户手动修改可保存并影响后续执行。
3. 助手建议支持自动/手动模式、逐项采纳/拒绝、拒绝后停止重复建议。
4. 每一步显示实际输入和结果，支持局部重跑；失败提供原因、影响范围和复用资产；耗时与 Provider 成本只写入内部执行审计，不在专业工作台展示计费。
5. 换模型或改关键帧不覆盖正式线；正式/实验双线可比较和采纳。
6. 角色/场景资产有正式版本与历史版本，资产升级不自动重跑旧作品。
7. 图片通过与视频控制适用性分开判断；视频支持时间点/时间段批注；V1 修复支持整镜重做。
8. OpenCut 可接收正式镜头进入剪辑；每个结果可追溯模型、提示词、参考资产与参数。

## 8. 明确不做

V1 不做托管 SaaS、平台充值支付、多人审批体系、隐藏第二套导演方案、未经确认的自动画布修改、真人参考照片上传、声音克隆、静默降级、第二条媒体执行路径，以及为了“功能更多”而牺牲可追溯性的模型大一统抽象。

## 9. 质量与发布

质量必须同时覆盖确认/能力完整性、有效请求、产物完整性、人物/外观/时序一致性、声音/口型/表演、叙事/镜头/成片连续性和用户验收。所有媒体请求必须受确认、Provider 能力和质量 Gate 约束；Provider 成本记录仅用于内部审计。

发布前必须完成专业手动链、助手建议链、模型冲突解释、局部修复、正式/实验隔离、真实 Provider 黄金样本、离线/安装/恢复、安全与交付验收。详细合同见 [docs/current/README.md](docs/current/README.md)。
