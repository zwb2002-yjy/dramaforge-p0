#!/bin/sh
set -eu

while read -r local_ref local_sha remote_ref remote_sha
do
  if [ "$remote_ref" = "refs/heads/main" ]; then
    echo "Direct pushes to main are blocked. Push an agent/* branch and merge through a PR." >&2
    exit 1
  fi
done

exit 0
