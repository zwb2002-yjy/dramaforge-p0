# DramaForge 全栈技术栈锁定表（P0）

**状态：FROZEN / 替换须走 ADR**  
**同步基线：2026-07-14（双模式与受控混合 Agent 共享同一技术底座）**  
**适用范围：本仓库所有可执行代码、Docker 镜像、CI、脚本和 AI 生成代码**

> 本文是 P0 冻结包的技术事实源；Agent 运行时的状态、授权、调度与物化事实以 `06_受控混合Agent运行时规范.md` 为准；双模式只可作为同一 React、FastAPI、PostgreSQL、Arq、Outbox 与 SSE 底座上的不同入口。`DramaForge架构决策与技术选型书.md` 提供选型理由，不能以其中历史示例覆盖本表。
>
> 本文锁定“同一职责只能有一种实现”。版本号以锁文件/镜像 digest 的实际解析结果为准；本文锁定技术、主版本和责任边界。不得以“更熟悉”“生态更大”“先用一下”为理由增加同类框架。

## 1. 后端与运行时

| 层级 | 固定技术 | 锁定要求 | 禁止替换或并存 |
|---|---|---|---|
| 语言 | Python 3.12 | 所有后端、Worker、迁移和工具脚本使用 Python 3.12。 | Python 3.11/3.13 混用、Node 后端、Go 服务。 |
| Web 框架 | FastAPI 0.115.x | API、SSE、依赖注入、OpenAPI 只由 FastAPI 提供。 | Flask、Django、Starlette 裸写、Sanic、Litestar。 |
| 校验与设置 | Pydantic v2 + pydantic-settings v2 | 请求/响应、内部命令、配置统一使用 Pydantic v2。 | Pydantic v1、marshmallow、attrs 作为 API schema。 |
| ORM | SQLAlchemy 2.0 async | 仅使用 `Mapped`、`mapped_column`、`AsyncSession` 和 `async_sessionmaker`。 | SQLModel、Tortoise ORM、Django ORM、同步 SQLAlchemy 写路径。 |
| PostgreSQL 驱动 | asyncpg | 应用运行时数据库 DSN 仅使用 `postgresql+asyncpg`。 | psycopg2、同步 `psycopg` 业务连接。 |
| 迁移 | Alembic 1.14+ | Schema/RLS/索引/枚举变更必须经 Alembic。 | 手工在生产库改表、另建迁移框架。 |
| HTTP 客户端 | httpx 0.28.x | Provider Adapter 的外部请求使用 `httpx.AsyncClient`。 | requests、aiohttp、各模块直接 SDK 调用。 |
| 密钥加密 | cryptography / Fernet | 用户 BYOK 必须用 Fernet 密文保存。 | 明文、可逆自制加密、把 key 放 Redis/日志。 |
| 图像处理 | Pillow 11.x | 仅用于解码、缩略图、格式转换等轻量图像操作。 | ImageMagick 服务、OpenCV 作为通用图像管线。 |
| 媒体处理 | FFmpeg 6+ | 音视频抽帧、拼接、字幕烧录、合成由受控 FFmpeg 包装器执行。 | MoviePy、浏览器端合成、任意 shell 拼接。 |
| 人脸一致性 | InsightFace 0.7+ + ONNX Runtime | 仅在 heavy Worker 启动时加载，CPU Execution Provider；输出 512 维归一化向量。 | 云端人脸 API、dlib、DeepFace、自训练识别模型。 |

### 1.1 任务执行与并发

| 职责 | 固定技术 | 强制规则 | 禁止项 |
|---|---|---|---|
| 异步任务队列 | Arq 0.26+ | 唯一队列框架；`default` 为 I/O 队列，`heavy` 为媒体/推理队列。 | RQ、Celery、Dramatiq、Huey、Sidekiq。 |
| 队列载体 | Redis 7+ | Arq 使用 Redis；不得为另一队列另加 Broker。 | RabbitMQ、Kafka、SQS。 |
| 可靠任务派发 | Transactional Outbox + 注册 Scheduler Interface | `runtime.scheduler` 负责 AgentRun、`production.scheduler` 负责 NodeRun；均从已提交 Outbox 派发，使用稳定 job ID、queued 补偿扫描、lease/CAS 恢复。业务 Module 禁止直接 enqueue。 | 提交事务后顺手入队、业务 Service 直调 `arq.enqueue_job()`、第二调度框架。 |
| Worker DB 会话 | `async_sessionmaker` | 每个 Job 自己创建 `AsyncSession`，`expire_on_commit=False`；只信任 run ID/dispatch generation，从数据库重读项目、发起者、成员关系和 Key，并在同一事务重建 `SET LOCAL` RLS 上下文。 | 在 Arq `ctx` 中共享 Session，或信任队列 payload 声称的 project/user/provider。 |
| 关键事件 | Redis Streams | `node.completed`、`node.failed`、`export.ready` 等关键事件从 Outbox 投递。 | 仅 Pub/Sub、仅内存事件。 |
| 高频进度 | Redis Pub/Sub | `node.progress` 可丢；不得承载业务最终状态。 | 把最终状态仅发送给 SSE。 |
| 实时客户端 | SSE + 原生 EventSource | 关键事件可由 `Last-Event-ID` 续传，随后从 API 重取快照。 | WebSocket、Socket.IO、轮询替代关键事件流。 |

## 2. 前端技术栈

| 层级 | 固定技术 | 锁定要求 | 禁止替换或并存 |
|---|---|---|---|
| UI 框架 | React 18 + TypeScript 5.x | 函数组件、严格 TypeScript、React 18 并发特性。 | Vue、Nuxt、Angular、Svelte、Solid。 |
| 构建 | Vite 6+ | 仅 Vite 负责开发与生产构建。 | Next.js、CRA、Webpack 自定义主构建。 |
| 路由 | TanStack Router 1.x | 路由文件、搜索参数和路径参数使用类型化 Router。 | React Router、手写 URL 分派。 |
| 服务端状态 | TanStack Query 5.x | REST 读取、变更、失效、重取全部经 Query/Mutation。 | Redux Toolkit Query、SWR、组件内重复 fetch。 |
| 客户端 UI 状态 | Zustand 5.x | 只存放布局、选中项、临时草稿和连接状态。 | Redux、MobX、Recoil、把服务端实体复制进 Store。 |
| 表单 | React Hook Form 7.x + Zod 3.x | 复杂表单和浏览器端输入校验统一使用。 | Formik、Yup、手写集中表单状态。 |
| 基础组件 | Radix UI + shadcn/ui | 无障碍 primitive 与项目封装均基于此组合。 | Ant Design、MUI、Chakra、Element、Mantine。 |
| 样式 | Tailwind CSS 4.x | 原子样式和设计 token 统一由 Tailwind 管理。 | CSS-in-JS、Styled Components、LESS 主样式体系。 |
| API 类型 | 后端 OpenAPI 生成 TypeScript | `frontend/src/types/api.ts` 由 FastAPI OpenAPI 生成。 | 手写重复 DTO、复制 Pydantic 字段。 |

## 3. 数据、存储与基础设施

| 层级 | 固定技术 | 锁定要求 | 禁止项 |
|---|---|---|---|
| 业务数据库 | PostgreSQL 15+ | 所有持久业务真相、审计、Outbox、预算和 RLS 都在 PostgreSQL。 | MySQL、MongoDB、SQLite 生产替代、事件作为唯一真相。 |
| 数据类型 | PostgreSQL 原生 UUID/JSONB/REAL[] | 人脸向量用受约束的 `REAL[512]`；不引入向量数据库。 | pgvector、Pinecone、Redis Vector、JSON 字符串向量。 |
| 对象存储 | MinIO（S3 兼容） | Artifact、导出包、Outbox Parquet 归档仅存 MinIO。 | 本地磁盘作为生产存储、S3/OSS 直接替换。 |
| 对象访问 | MinIO SDK + 预签名 URL | URL 限时、单对象、单动作；服务端只存 object key，不存永久外链。 | 公共 Bucket、永久 download URL。 |
| 本地推理 | ComfyUI（GPU profile，可选） | 仅在部署方显式启用、许可审计完成且用户允许时作为受控 Adapter。 | 默认启用、未审计模型权重、绕过 Adapter。 |
| 容器编排 | Docker Compose v2 | P0 使用 `docker-compose.yml` 和 `docker-compose.gpu.yml`。 | Kubernetes、Helm、Nomad、服务网格。 |
| 反向代理 | Nginx 1.26+ | 生产 HTTPS、反向代理、SSE 缓冲配置统一由 Nginx 管理。 | Traefik、Caddy、应用自己处理 TLS。 |
| 可观测性 | Prometheus + Grafana | 采集 API、Worker、Outbox、成本、队列、死信和对象生命周期指标。 | 仅第三方 APM、无指标的日志排障。 |

## 4. Provider 与 Adapter 固定边界

Provider 可通过用户 BYOK 启用，但调用入口固定，不得让业务模块感知具体 SDK。

| 能力 | P0 Adapter 名称 | P0 启用策略 | 禁止项 |
|---|---|---|---|
| 文本/结构化生成 | `openai` | 可选；仅 `creation.agent_runtime` 可经有效 PlanningAuthorization、BudgetReservation、AgentRun 1:N ProviderOperation 和 CostLedger 调用，用于严格 Schema 的 Brief/Plan 任务。 | Route/Service 直接调用 SDK、无授权调用、把对话/模型输出当生产图真相。 |
| 图像生成 | `flux` | S2 唯一真实图像 Provider；先打穿垂直切片。 | S2 同时接入多个图像 Provider。 |
| 视频生成 | `kling` | P0 后续链路中的受控视频 Adapter；用户有 Key 才可候选。 | 未经 Adapter 的 API 调用或隐式代付。 |
| 语音合成 | `azure_tts` | P0 固定 TTS Adapter；用户有 Key 才可候选。 | 火山/Azure 并行实现、浏览器端 TTS。 |
| 本地生成 | `comfyui` | 仅满足授权与部署条件时作为最后候选。 | 作为默认 Provider 或跨项目共享模型输出。 |

### 4.1 Adapter 接口锁定

所有 Adapter 必须实现 `app/providers/base.py` 中的异步接口。业务代码只能依赖能力接口，不依赖供应商类名。

```python
class ProviderAdapter(Protocol):
    provider: str

    async def create(self, request: ProviderRequest) -> ProviderCreateResult: ...
    async def poll(self, remote_task_id: str) -> ProviderPollResult: ...
    async def cancel(self, remote_task_id: str) -> ProviderCancelResult: ...
    async def fetch_cost(self, remote_task_id: str) -> ProviderCostResult: ...
    def verify_webhook(self, headers: Mapping[str, str], body: bytes) -> VerifiedWebhook: ...
```

不得在接口外加入同步方法、全局客户端或可被业务层绕过的 Provider 专用参数。供应商特有参数必须先规范化到 `ProviderRequest.options` 白名单。

### 4.2 受控混合 Agent 技术边界

- P0 不引入 LangChain、通用 Agent SDK、MCP Runtime、Agent-to-Agent spawn、向量数据库、跨项目长记忆、第二队列框架或第二实时通道。
- Agent 输入/输出、MaterializationPlan 和 Provider 摘要均使用 `extra='forbid'` 的 Pydantic v2 严格 Schema；未知字段拒绝，格式错误最多允许一次有预算的 `schema_repair`。
- 一个 AgentRun 可对应多个真实 ProviderOperation attempt；每次 primary、repair、retry、fallback 分别记账，AgentRun 不保存 actual provider/model/token/cost 的第二真相。
- 没有可用文本 Key 时必须返回 `TEXT_PROVIDER_CONFIGURATION_REQUIRED` 并提供手工 Brief/Plan 与进入工作台路径；禁止隐式平台 Key和跨账号代付。
- Agent context/hash 与 active singleflight 严格限定当前 Project、发起用户、操作和目标 revision/version；禁止跨项目、跨用户复用与 `cached AgentRun`。
- 模型不获得写入型 Tool；任何副作用只由确认后的领域 Command 和 `materialization-p0-v1` 白名单解释器完成。

## 5. 测试、质量与工具链

| 领域 | 固定技术 | 规则 |
|---|---|---|
| Python 测试 | pytest + pytest-asyncio + httpx TestClient | 单元、集成、数据库/RLS、Worker 契约测试均使用 pytest。 |
| 前端单元测试 | Vitest + React Testing Library | 组件和 hook 测试不访问真实 Provider。 |
| 前端 E2E | Playwright | 覆盖登录、规划费用授权、Brief/Plan 两级确认、Agent 取消与 stale、无 Key 手工路径、快速/专业模式无副本切换、SSE 恢复、审核、导出下载和越权拒绝。 |
| Python 格式/静态检查 | Ruff + mypy | CI 必须检查格式、lint 和类型。 |
| TypeScript 格式/静态检查 | ESLint + Prettier + `tsc --noEmit` | CI 必须检查。 |
| API 契约 | FastAPI OpenAPI + 生成 TypeScript 类型 | 修改 Pydantic 后必须重新生成类型并提交。 |
| 数据库测试 | 临时 PostgreSQL 15 容器 | 不得用 SQLite 冒充 PostgreSQL/RLS 行为。 |

## 6. 技术变更流程

以下情况必须新增 ADR 并同时修改 01–06 六份冻结文件中的相关事实：替换表中任一技术、增加同职责库、升级主版本、添加 Provider、改变对象存储/队列/事件协议、引入新数据库扩展，或改变创作体验 Module 的稳定 Interface、确认门槛或模式切换数据语义。

未走 ADR 的依赖新增视为不合规。AI 编码代理发现需求与本文冲突时，必须停止实现、指出冲突位置并请求变更决定；不得自行引入替代框架。